---
phase: 22-bot-thin-relay
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - src/vcompany/bot/cogs/strategist.py
  - src/vcompany/bot/cogs/health.py
  - src/vcompany/bot/cogs/task_relay.py
  - src/vcompany/bot/cogs/workflow_master.py
  - src/vcompany/bot/cogs/question_handler.py
  - src/vcompany/daemon/runtime_api.py
  - tests/test_import_boundary.py
autonomous: true
requirements: [BOT-02, BOT-03, BOT-04, BOT-05]

must_haves:
  truths:
    - "strategist.py has zero prohibited imports -- StrategistConversation and DecisionLogger references removed"
    - "task_relay.py routes messages through RuntimeAPI.relay_channel_message instead of accessing container._pane_id and TmuxManager"
    - "health.py uses RuntimeAPI.health_tree() instead of direct company_root.health_tree()"
    - "alerts.py daemon event subscription formats health/transition/escalation events as Discord embeds via build_alert_embed (BOT-03)"
    - "All 9 cog files pass import boundary test with strict prohibited prefixes (xfail removed)"
  artifacts:
    - path: "src/vcompany/bot/cogs/strategist.py"
      provides: "Clean strategist cog using RuntimeAPI only"
      contains: "_get_runtime_api"
    - path: "src/vcompany/bot/cogs/task_relay.py"
      provides: "Message relay through RuntimeAPI"
      contains: "relay_channel_message"
    - path: "src/vcompany/bot/cogs/health.py"
      provides: "Health display via RuntimeAPI"
      contains: "_get_runtime_api"
    - path: "src/vcompany/bot/cogs/alerts.py"
      provides: "Daemon event formatting as Discord embeds (BOT-03)"
      contains: "build_alert_embed"
    - path: "tests/test_import_boundary.py"
      provides: "All xfail markers removed -- tests pass strict"
      contains: "test_no_function_level_prohibited_imports"
  key_links:
    - from: "src/vcompany/bot/cogs/task_relay.py"
      to: "src/vcompany/daemon/runtime_api.py"
      via: "runtime_api.relay_channel_message()"
      pattern: "relay_channel_message"
    - from: "src/vcompany/bot/cogs/strategist.py"
      to: "src/vcompany/daemon/runtime_api.py"
      via: "runtime_api.relay_strategist_message()"
      pattern: "relay_strategist_message"
    - from: "src/vcompany/bot/cogs/alerts.py"
      to: "src/vcompany/bot/embeds.py"
      via: "build_alert_embed for daemon event formatting"
      pattern: "build_alert_embed"
---

<objective>
Clean up the remaining 5 cog files (strategist.py, health.py, task_relay.py, workflow_master.py, question_handler.py) to remove all prohibited imports, verify daemon event formatting in alerts.py (BOT-03), then remove xfail markers from import boundary tests.

Purpose: Completes BOT-02 (zero prohibited imports across ALL cogs), BOT-05 (message relay via daemon), BOT-03 (daemon event formatting verified), and BOT-04 (pure I/O adapters). After this plan, every cog is a pure I/O adapter.
Output: 5 clean cog files, verified event formatting, fully passing import boundary tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bot-thin-relay/22-RESEARCH.md
@.planning/phases/22-bot-thin-relay/22-01-SUMMARY.md
@.planning/phases/22-bot-thin-relay/22-02-SUMMARY.md

@src/vcompany/daemon/runtime_api.py
@src/vcompany/bot/cogs/strategist.py
@src/vcompany/bot/cogs/health.py
@src/vcompany/bot/cogs/task_relay.py
@src/vcompany/bot/cogs/workflow_master.py
@src/vcompany/bot/cogs/question_handler.py
@src/vcompany/bot/cogs/alerts.py
@tests/test_import_boundary.py
</context>

<interfaces>
<!-- RuntimeAPI methods available after Plan 01 -->

From src/vcompany/daemon/runtime_api.py:
```python
class RuntimeAPI:
    async def relay_strategist_message(self, user_message: str, channel_id: str, user_name: str = "user") -> None
    async def relay_strategist_escalation_reply(self, reply_text: str, escalation_message_id: str) -> None
    async def relay_channel_message(self, agent_id: str, content: str) -> bool
    async def health_tree(self) -> dict
    async def get_agent_states(self) -> list[dict]
    async def handle_plan_approval(self, agent_id: str, plan_path: str) -> None
    async def handle_plan_rejection(self, agent_id: str, plan_path: str, feedback: str) -> None
```

<!-- Helper pattern -->
```python
def _get_runtime_api(bot) -> RuntimeAPI | None:
    daemon = getattr(bot, "_daemon", None)
    if daemon is None:
        return None
    return getattr(daemon, "runtime_api", None)
```

<!-- AlertsCog event formatting (BOT-03 -- already implemented, verify clean) -->
From src/vcompany/bot/cogs/alerts.py:
```python
class AlertsCog(commands.Cog):
    async def alert_agent_dead(self, agent_id: str) -> None
    async def alert_agent_stuck(self, agent_id: str) -> None
    async def alert_circuit_open(self, agent_id: str, crash_count: int) -> None
    async def alert_hook_timeout(self, agent_id: str, request_id: str, fallback: str) -> None
    async def alert_plan_detected(self, agent_id: str, plan_path: Path) -> None
    def make_sync_callbacks(self) -> dict  # bridges sync supervision callbacks to async Discord sends
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clean strategist.py, health.py, task_relay.py, workflow_master.py, question_handler.py and verify alerts.py event formatting (BOT-03)</name>
  <files>
    src/vcompany/bot/cogs/strategist.py,
    src/vcompany/bot/cogs/health.py,
    src/vcompany/bot/cogs/task_relay.py,
    src/vcompany/bot/cogs/workflow_master.py,
    src/vcompany/bot/cogs/question_handler.py,
    src/vcompany/daemon/runtime_api.py
  </files>
  <read_first>
    - src/vcompany/bot/cogs/strategist.py (full file -- understand StrategistConversation usage, CompanyAgent reference)
    - src/vcompany/bot/cogs/health.py (full file -- understand MessagePriority/QueuedMessage usage, company_root access)
    - src/vcompany/bot/cogs/task_relay.py (full file -- understand _relay_to_pane, TmuxManager, container._pane_id)
    - src/vcompany/bot/cogs/workflow_master.py (full file -- understand StrategistConversation usage)
    - src/vcompany/bot/cogs/question_handler.py (full file -- understand strategist.models usage)
    - src/vcompany/bot/cogs/alerts.py (full file -- verify BOT-03 event formatting is clean and wired)
    - src/vcompany/daemon/runtime_api.py (available methods)
  </read_first>
  <action>
Clean each file to remove ALL prohibited imports. After cleanup, each cog should ONLY import from vcompany.bot.*, vcompany.daemon.comm, vcompany.shared.*, discord, and stdlib.

**strategist.py:**
1. Remove `from vcompany.strategist.conversation import StrategistConversation` -- conversation runs in daemon now, cog just relays messages via runtime_api.relay_strategist_message()
2. Remove `from vcompany.strategist.decision_log import DecisionLogger` -- decision logging runs in daemon
3. Remove TYPE_CHECKING import of `CompanyAgent` -- cog no longer needs to know about CompanyAgent
4. Remove `_company_agent` attribute and `set_company_agent()` method -- backward compat path is gone
5. Remove any direct `CompanyAgent.post_event()` calls -- replace with runtime_api.relay_strategist_message()
6. Keep the on_message handler that detects strategist channel messages, but route them through RuntimeAPI
7. Keep Discord formatting (embed creation, message splitting) in the cog

**health.py:**
1. Remove `from vcompany.resilience.message_queue import MessagePriority, QueuedMessage` -- if health display needs these types, either:
   - Use plain dicts from RuntimeAPI.health_tree() (already returns model_dump dict)
   - Or move the types to vcompany.shared if they're just data classes
2. Remove `getattr(self.bot, "company_root")` access -- use `_get_runtime_api(self.bot).health_tree()`
3. Remove TYPE_CHECKING import of HealthReport if unused after refactor
4. Health cog becomes: call RuntimeAPI.health_tree(), format dict as Discord embed

**task_relay.py (BOT-05):**
1. Remove `from vcompany.tmux.session import TmuxManager` lazy import in _relay_to_pane
2. Remove all `self.bot.company_root._company_agents` and `._projects` and `container._pane_id` access
3. Replace _relay_to_pane with: `await runtime_api.relay_channel_message(agent_id, content)`
4. The on_message handler that detects agent/task channel messages should:
   - Extract agent_id from channel name (existing logic)
   - Call runtime_api.relay_channel_message(agent_id, message.content)
5. This is the core BOT-05 implementation -- Discord messages go to daemon, daemon sends to tmux pane

**workflow_master.py:**
1. Remove `from vcompany.strategist.conversation import StrategistConversation` -- if WorkflowMaster uses conversation, route through RuntimeAPI
2. Remove lazy import of `vcompany.strategist.workflow_master_persona` -- if persona loading is needed, move to RuntimeAPI or load from file path in shared config
3. If WorkflowMaster is primarily a Discord interaction handler (slash commands or views), keep the Discord parts and route business logic to RuntimeAPI
4. If WorkflowMaster is deeply coupled to StrategistConversation internals, consider whether it should be moved to daemon layer entirely (and the cog becomes a thin relay like StrategistCog)

**question_handler.py:**
1. Remove TYPE_CHECKING import of `vcompany.strategist.pm.PMTier` if used -- replace with a string or dict type
2. Remove `from vcompany.strategist.models import DecisionLogEntry` lazy import -- route decision logging through RuntimeAPI
3. If question_handler needs to log decisions, add runtime_api.log_decision() method to RuntimeAPI

**BOT-03 verification (alerts.py daemon event formatting):**
1. Read alerts.py and confirm it is already clean (no prohibited imports, uses build_alert_embed from vcompany.bot.embeds)
2. Verify alerts.py has methods for all daemon event types: agent_dead, agent_stuck, circuit_open, hook_timeout, plan_detected
3. Verify make_sync_callbacks() bridges sync supervision tree callbacks to async Discord sends
4. If alerts.py needs any daemon event types that are missing (e.g., agent_transition, escalation), add the alert methods following the existing pattern (build_alert_embed + _send_or_buffer)
5. If the daemon needs to subscribe alerts.py to events via CommunicationPort rather than sync callbacks, wire that subscription in client.py on_ready (if not already done)

For any new RuntimeAPI methods needed (log_decision, workflow_master operations), add them directly to runtime_api.py in this task. The daemon layer is allowed to import from any vcompany.* module.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "
import sys
files = [
    'src/vcompany/bot/cogs/strategist.py',
    'src/vcompany/bot/cogs/health.py',
    'src/vcompany/bot/cogs/task_relay.py',
    'src/vcompany/bot/cogs/workflow_master.py',
    'src/vcompany/bot/cogs/question_handler.py',
    'src/vcompany/bot/cogs/alerts.py',
]
prohibited = [
    'vcompany.agent', 'vcompany.container', 'vcompany.supervisor',
    'vcompany.tmux', 'vcompany.resilience', 'vcompany.strategist',
    'vcompany.communication', 'vcompany.integration', 'vcompany.monitor',
    'vcompany.models.agent_state', 'vcompany.models.config',
    'vcompany.git', 'vcompany.cli',
]
for fname in files:
    with open(fname) as f:
        content = f.read()
    in_tc = False
    for i, line in enumerate(content.split('\n'), 1):
        stripped = line.strip()
        if 'TYPE_CHECKING' in stripped and stripped.startswith('if'):
            in_tc = True
            continue
        if in_tc and stripped and not line.startswith((' ', '\t')):
            in_tc = False
        if in_tc:
            continue
        for p in prohibited:
            if f'from {p}' in stripped or f'import {p}' in stripped:
                print(f'FAIL: {fname}:{i}: {stripped}')
                sys.exit(1)
    print(f'PASS: {fname}')
# Verify alerts.py has daemon event formatters (BOT-03)
with open('src/vcompany/bot/cogs/alerts.py') as f:
    alerts_content = f.read()
for method in ['alert_agent_dead', 'alert_agent_stuck', 'alert_circuit_open', 'make_sync_callbacks', 'build_alert_embed']:
    if method not in alerts_content:
        print(f'FAIL: alerts.py missing {method} (BOT-03)')
        sys.exit(1)
print('PASS: alerts.py has all BOT-03 event formatters')
print('All clean')
"</automated>
  </verify>
  <acceptance_criteria>
    - strategist.py has zero imports from vcompany.strategist.* (not in TYPE_CHECKING)
    - strategist.py has no _company_agent attribute or set_company_agent method
    - health.py has zero imports from vcompany.resilience.* or vcompany.container.*
    - task_relay.py has zero imports from vcompany.tmux.* and no container._pane_id access
    - task_relay.py routes messages through runtime_api.relay_channel_message()
    - workflow_master.py has zero imports from vcompany.strategist.*
    - question_handler.py has zero imports from vcompany.strategist.*
    - alerts.py has zero prohibited imports and formats daemon events as Discord embeds (BOT-03)
  </acceptance_criteria>
  <done>All 5 remaining cog files are pure I/O adapters with zero prohibited imports, alerts.py daemon event formatting verified (BOT-03)</done>
</task>

<task type="auto">
  <name>Task 2: Remove xfail markers from import boundary tests and verify full suite</name>
  <files>tests/test_import_boundary.py</files>
  <read_first>
    - tests/test_import_boundary.py (check which tests have xfail markers from Plan 01)
  </read_first>
  <action>
1. Remove ALL `@pytest.mark.xfail` markers from test_import_boundary.py tests. After Plans 02 and 03, all cog files should be clean.

2. Run the full import boundary test suite to confirm:
   - test_no_container_imports_in_bot passes (all 10 files, expanded prefixes)
   - test_no_function_level_prohibited_imports passes (all files, all methods clean)
   - test_no_company_root_attribute_access passes (no company_root access in any cog)
   - test_no_discord_in_daemon passes (unchanged)

3. If any test fails, identify the remaining violation and fix it in the cog file. This task is the final cleanup sweep.

4. Run the full test suite (`python -m pytest tests/ -x`) to make sure nothing else broke.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -m pytest tests/test_import_boundary.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Zero xfail markers remain in test_import_boundary.py
    - All 4 tests pass (not xpass, not xfail -- genuine PASSED)
    - Full test suite passes: `python -m pytest tests/ -x`
  </acceptance_criteria>
  <done>Import boundary tests pass strict for all cog files, confirming BOT-02 complete</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_import_boundary.py -v` -- all 4 tests PASSED (no xfail)
- `grep -rn "from vcompany\." src/vcompany/bot/cogs/ --include="*.py" | grep -v "__pycache__" | grep -v "vcompany.bot\.\|vcompany.daemon\.\|vcompany.shared\.\|TYPE_CHECKING"` returns empty
- `grep -rn "company_root\|_find_container\|_get_company_root" src/vcompany/bot/cogs/ --include="*.py" | grep -v "__pycache__" | grep -v "#"` returns empty
- `python -c "import ast; [ast.parse(open(f).read()) for f in ['src/vcompany/bot/cogs/alerts.py']]"` -- alerts.py parses cleanly
- `python -m pytest tests/ -x` -- full suite green
</verification>

<success_criteria>
- All 9 cog files have zero prohibited imports (module-level and function-level)
- task_relay.py uses RuntimeAPI.relay_channel_message() for message relay (BOT-05)
- health.py uses RuntimeAPI.health_tree() (BOT-03 formatting path)
- alerts.py formats daemon health/transition/escalation events as Discord embeds (BOT-03)
- strategist.py is fully decoupled from StrategistConversation (BOT-04)
- Import boundary tests pass strict (no xfail markers)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-bot-thin-relay/22-03-SUMMARY.md`
</output>
