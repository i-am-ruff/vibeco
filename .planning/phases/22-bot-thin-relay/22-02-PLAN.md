---
phase: 22-bot-thin-relay
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/vcompany/bot/cogs/commands.py
  - src/vcompany/bot/cogs/plan_review.py
  - src/vcompany/bot/cogs/workflow_orchestrator_cog.py
autonomous: true
requirements: [BOT-01, BOT-02, BOT-04]

must_haves:
  truths:
    - "All slash commands in commands.py delegate to RuntimeAPI -- no direct CompanyRoot or container access"
    - "plan_review.py has zero prohibited imports (module-level or function-level)"
    - "workflow_orchestrator_cog.py has zero _find_container calls -- all go through RuntimeAPI"
  artifacts:
    - path: "src/vcompany/bot/cogs/commands.py"
      provides: "Slash commands as pure RuntimeAPI delegates"
      contains: "_get_runtime_api"
    - path: "src/vcompany/bot/cogs/plan_review.py"
      provides: "Plan review cog using RuntimeAPI for container access"
      contains: "_get_runtime_api"
    - path: "src/vcompany/bot/cogs/workflow_orchestrator_cog.py"
      provides: "Workflow orchestrator using RuntimeAPI"
      contains: "_get_runtime_api"
  key_links:
    - from: "src/vcompany/bot/cogs/commands.py"
      to: "src/vcompany/daemon/runtime_api.py"
      via: "self.bot._daemon.runtime_api"
      pattern: "runtime_api\\."
    - from: "src/vcompany/bot/cogs/plan_review.py"
      to: "src/vcompany/daemon/runtime_api.py"
      via: "runtime_api method calls"
      pattern: "runtime_api\\."
---

<objective>
Rewrite the three heaviest-violation cog files (commands.py, plan_review.py, workflow_orchestrator_cog.py) to delegate all business logic to RuntimeAPI with zero prohibited imports.

Purpose: These 3 files account for ~70% of all import violations. Cleaning them is the core of BOT-01 (slash commands via RuntimeAPI) and BOT-02 (zero prohibited imports).
Output: Three cog files that are pure Discord I/O adapters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bot-thin-relay/22-RESEARCH.md
@.planning/phases/22-bot-thin-relay/22-01-SUMMARY.md

@src/vcompany/daemon/runtime_api.py
@src/vcompany/bot/cogs/commands.py
@src/vcompany/bot/cogs/plan_review.py
@src/vcompany/bot/cogs/workflow_orchestrator_cog.py
</context>

<interfaces>
<!-- RuntimeAPI methods available after Plan 01 -->

From src/vcompany/daemon/runtime_api.py (after Plan 01):
```python
class RuntimeAPI:
    async def hire(self, agent_id: str, template: str = "generic") -> str
    async def give_task(self, agent_id: str, task: str) -> None
    async def dismiss(self, agent_id: str) -> None
    async def status(self) -> dict
    async def health_tree(self) -> dict
    async def dispatch(self, agent_id: str) -> None
    async def kill(self, agent_id: str) -> None
    async def relaunch(self, agent_id: str) -> None
    async def remove_project(self, project_name: str) -> None
    async def relay_channel_message(self, agent_id: str, content: str) -> bool
    async def get_agent_states(self) -> list[dict]
    async def checkin(self) -> dict
    async def standup(self) -> dict
    async def run_integration(self) -> dict
    async def new_project(self, project_config, project_dir, persona_path=None) -> None
    async def relay_strategist_message(self, user_message, channel_id, user_name="user") -> None
    async def handle_plan_approval(self, agent_id: str, plan_path: str) -> None
    async def handle_plan_rejection(self, agent_id: str, plan_path: str, feedback: str) -> None
```

<!-- Helper pattern established in Phase 20 -->
```python
def _get_runtime_api(bot) -> RuntimeAPI | None:
    daemon = getattr(bot, "_daemon", None)
    if daemon is None:
        return None
    return getattr(daemon, "runtime_api", None)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite commands.py to delegate all operations to RuntimeAPI</name>
  <files>src/vcompany/bot/cogs/commands.py</files>
  <read_first>
    - src/vcompany/bot/cogs/commands.py (full file -- understand every slash command and what it does)
    - src/vcompany/daemon/runtime_api.py (available methods after Plan 01)
    - .planning/phases/22-bot-thin-relay/22-01-SUMMARY.md (what methods were actually added)
  </read_first>
  <action>
Rewrite commands.py to remove ALL prohibited imports. The cog should ONLY import from:
- stdlib (asyncio, logging, typing)
- discord / discord.ext.commands
- vcompany.bot.* (channel_setup, embeds, permissions, views)
- vcompany.daemon.comm (if needed for payload types)
- vcompany.shared.* (if needed for pure utilities)

For each command, replace the direct logic with RuntimeAPI calls:

1. **Remove module-level prohibited imports:**
   - `from vcompany.communication.checkin import gather_checkin_data` -> delete, use runtime_api.checkin()
   - `from vcompany.communication.standup import StandupSession` -> delete, use runtime_api.standup()
   - `from vcompany.integration.pipeline import IntegrationPipeline` -> delete, use runtime_api.run_integration()

2. **Rewrite /new-project command:**
   - Remove all lazy imports (vcompany.models.config, vcompany.cli.clone_cmd, vcompany.git, vcompany.shared.paths, vcompany.shared.file_ops, vcompany.shared.templates)
   - Call `runtime_api.new_project(...)` -- the RuntimeAPI already handles project init
   - The cog should only handle Discord interaction (defer reply, send success/error message)

3. **Rewrite /dispatch command:**
   - Remove `_get_company_root()` usage
   - Call `runtime_api.dispatch(agent_id)`

4. **Rewrite /kill command:**
   - Remove `_get_company_root()` and `container.stop()` calls
   - Call `runtime_api.kill(agent_id)`

5. **Rewrite /relaunch command:**
   - Remove `_get_company_root()` usage
   - Call `runtime_api.relaunch(agent_id)`

6. **Rewrite /remove-project command:**
   - Remove `from vcompany.tmux.session import TmuxManager` lazy import
   - Remove `from vcompany.shared.paths` lazy import
   - Call `runtime_api.remove_project(project_name)`

7. **Rewrite /checkin command:**
   - Remove `from vcompany.communication.checkin import gather_checkin_data, post_checkin` lazy import
   - Call `runtime_api.checkin()` and format result as Discord embed

8. **Rewrite /standup, /integrate commands similarly.**

9. **Remove `_get_company_root()` helper entirely** -- replace all usages with `_get_runtime_api(self.bot)`.

10. **Remove `_on_checkin` callback** if it accesses containers directly -- route through RuntimeAPI.

Keep all Discord formatting (embeds, views, ephemeral messages) in the cog -- only move business logic to RuntimeAPI.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "
import ast, sys
with open('src/vcompany/bot/cogs/commands.py') as f:
    content = f.read()
# Check no prohibited module-level imports
prohibited = ['vcompany.communication', 'vcompany.integration', 'vcompany.models.config', 'vcompany.cli', 'vcompany.git', 'vcompany.tmux', 'vcompany.container', 'vcompany.supervisor', 'vcompany.agent']
for p in prohibited:
    # Check module-level (non-indented) imports
    for line in content.split('\n'):
        if line.startswith('from ') and p in line:
            print(f'FAIL: module-level prohibited import: {line}')
            sys.exit(1)
# Check no _get_company_root
if '_get_company_root' in content:
    print('FAIL: _get_company_root still present')
    sys.exit(1)
print('PASS: commands.py clean')
"</automated>
  </verify>
  <acceptance_criteria>
    - Zero module-level imports from prohibited prefixes
    - Zero function-level imports from prohibited prefixes
    - _get_company_root helper removed entirely
    - All slash commands use _get_runtime_api pattern
    - Discord formatting (embeds, views) preserved in cog
  </acceptance_criteria>
  <done>commands.py is a pure Discord I/O adapter delegating all business logic to RuntimeAPI</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite plan_review.py and workflow_orchestrator_cog.py</name>
  <files>src/vcompany/bot/cogs/plan_review.py, src/vcompany/bot/cogs/workflow_orchestrator_cog.py</files>
  <read_first>
    - src/vcompany/bot/cogs/plan_review.py (full file)
    - src/vcompany/bot/cogs/workflow_orchestrator_cog.py (full file)
    - src/vcompany/daemon/runtime_api.py (available methods)
    - .planning/phases/22-bot-thin-relay/22-01-SUMMARY.md (what was added)
  </read_first>
  <action>
**plan_review.py cleanup:**

1. Remove module-level `from vcompany.monitor.safety_validator import validate_safety_table` -- either move validate_safety_table to RuntimeAPI or to a vcompany.shared utility (it's a stateless function). If it has no container/supervisor dependencies, it can stay as vcompany.shared import. Otherwise add a RuntimeAPI method.

2. Remove all function-level lazy imports:
   - `from vcompany.git import ops as git_ops` in _verify_agent_execution -> add RuntimeAPI.verify_agent_execution() or move git ops to RuntimeAPI
   - `from vcompany.models.agent_state import AgentsRegistry` in _send_tmux_command -> route through RuntimeAPI.relay_channel_message()
   - `from vcompany.tmux.session import TmuxManager` in _send_tmux_command -> route through RuntimeAPI.relay_channel_message()
   - `from vcompany.strategist.models import DecisionLogEntry` in _log_plan_decision -> add RuntimeAPI.log_plan_decision() or move to shared
   - `from vcompany.agent.gsd_agent import GsdAgent` in _handle_review_response -> use RuntimeAPI.handle_plan_approval/rejection (already exists)

3. Remove `self.bot.company_root._find_container()` calls -- use RuntimeAPI methods.

4. Remove TYPE_CHECKING import of PlanReviewer if no longer referenced.

5. Keep safety_validator if it's truly a pure utility with no side effects -- but move import to allowed namespace.

**workflow_orchestrator_cog.py cleanup:**

1. Remove all 4 `self.bot.company_root._find_container()` calls.

2. For each call site, determine what the cog is doing:
   - If querying container state -> use RuntimeAPI.get_agent_states() or RuntimeAPI.status()
   - If sending a signal/event to container -> add a RuntimeAPI method (e.g., `signal_stage(agent_id, stage)`)
   - If checking container type -> include type info in RuntimeAPI response dicts

3. Remove TYPE_CHECKING import of PMTier if unused.

4. If WorkflowOrchestratorCog needs to send stage signals to agents, add `async def signal_workflow_stage(self, agent_id: str, stage: str, data: dict) -> None` to RuntimeAPI in this task (it's fine to add a method to runtime_api.py since Plan 01 is complete and we own that file knowledge).

5. Ensure the cog only imports from vcompany.bot.*, vcompany.daemon.comm, vcompany.shared.*, discord, and stdlib.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "
import sys
for fname in ['src/vcompany/bot/cogs/plan_review.py', 'src/vcompany/bot/cogs/workflow_orchestrator_cog.py']:
    with open(fname) as f:
        content = f.read()
    prohibited = ['vcompany.agent', 'vcompany.container', 'vcompany.supervisor', 'vcompany.tmux', 'vcompany.models.agent_state', 'vcompany.git', 'vcompany.strategist', 'vcompany.monitor']
    for p in prohibited:
        if f'from {p}' in content:
            # Allow TYPE_CHECKING
            in_tc = False
            for line in content.split('\n'):
                if 'TYPE_CHECKING' in line:
                    in_tc = True
                    continue
                if in_tc and not line.startswith((' ', '\t')):
                    in_tc = False
                if not in_tc and f'from {p}' in line:
                    print(f'FAIL: {fname}: prohibited import: {line.strip()}')
                    sys.exit(1)
    if 'company_root' in content and 'TYPE_CHECKING' not in content:
        # Quick check -- more thorough check needed
        pass
    if '_find_container' in content:
        print(f'FAIL: {fname}: still has _find_container')
        sys.exit(1)
    print(f'PASS: {fname} clean')
"</automated>
  </verify>
  <acceptance_criteria>
    - plan_review.py has zero prohibited imports (module-level and function-level)
    - plan_review.py has zero _find_container calls
    - workflow_orchestrator_cog.py has zero prohibited imports
    - workflow_orchestrator_cog.py has zero _find_container calls
    - Both cogs use _get_runtime_api pattern exclusively
  </acceptance_criteria>
  <done>plan_review.py and workflow_orchestrator_cog.py are pure I/O adapters using RuntimeAPI</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_import_boundary.py::test_no_container_imports_in_bot -v` passes for commands.py, plan_review.py, workflow_orchestrator_cog.py
- `grep -rn "from vcompany\." src/vcompany/bot/cogs/commands.py src/vcompany/bot/cogs/plan_review.py src/vcompany/bot/cogs/workflow_orchestrator_cog.py | grep -v "vcompany.bot\.\|vcompany.daemon\.\|vcompany.shared\.\|TYPE_CHECKING"` returns empty
- `grep -n "_find_container\|_get_company_root\|company_root" src/vcompany/bot/cogs/commands.py src/vcompany/bot/cogs/plan_review.py src/vcompany/bot/cogs/workflow_orchestrator_cog.py` returns empty (or only in comments)
</verification>

<success_criteria>
- 3 heaviest cog files rewritten to use RuntimeAPI exclusively
- Zero prohibited imports (module-level or function-level) in these files
- All slash commands (/new-project, /dispatch, /kill, /relaunch, /health, /checkin, /standup) delegate to RuntimeAPI
- Import boundary test passes for these files (xfail removed if applicable)
</success_criteria>

<output>
After completion, create `.planning/phases/22-bot-thin-relay/22-02-SUMMARY.md`
</output>
