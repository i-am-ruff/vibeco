---
phase: 10-rework-gsd-agent-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/templates/gsd_config.json.j2
  - tools/patch_gsd_workflows.py
  - tests/test_gsd_patches.py
  - tests/test_gsd_config_template.py
autonomous: true
requirements: [D-12, D-13, D-14, D-18]

must_haves:
  truths:
    - "GSD config template deploys with skip_discuss false and discuss_mode discuss"
    - "A patch tool exists that adds vco report calls to discuss-phase.md and auto-selects non-AskUserQuestion prompts in plan-phase.md and execute-plan.md"
    - "Patches are idempotent -- running the tool twice produces the same result"
  artifacts:
    - path: "src/vcompany/templates/gsd_config.json.j2"
      provides: "Updated GSD config with autonomous workflow settings"
      contains: "skip_discuss.*false"
    - path: "tools/patch_gsd_workflows.py"
      provides: "GSD workflow patcher for autonomous operation"
      exports: ["patch_all", "verify_patches"]
    - path: "tests/test_gsd_patches.py"
      provides: "Tests for GSD patch application and idempotency"
    - path: "tests/test_gsd_config_template.py"
      provides: "Tests for GSD config template values"
  key_links:
    - from: "tools/patch_gsd_workflows.py"
      to: "~/.claude/get-shit-done/workflows/"
      via: "Path.read_text / Path.write_text on workflow .md files"
      pattern: "GSD_WORKFLOWS_DIR.*get-shit-done"
---

<objective>
Update GSD config template for autonomous operation and create a GSD workflow patcher tool that adds missing vco report signals to discuss-phase and auto-selects non-AskUserQuestion interactive prompts in plan-phase and execute-plan workflows.

Purpose: Agents must run GSD stages without blocking on interactive prompts that are not AskUserQuestion (those route through Discord). The config template must switch from assumptions mode to full discussion mode so PM answers design questions via the hook.

Output: Updated gsd_config.json.j2, tools/patch_gsd_workflows.py, and tests confirming correct behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rework-gsd-agent-dispatch-to-bypass-all-interactive-prompts-research-context-discuss-for-fully-autonomous-operation/10-CONTEXT.md
@.planning/phases/10-rework-gsd-agent-dispatch-to-bypass-all-interactive-prompts-research-context-discuss-for-fully-autonomous-operation/10-RESEARCH.md

<interfaces>
<!-- Current GSD config template -->
From src/vcompany/templates/gsd_config.json.j2:
```json
{
  "mode": "yolo",
  "granularity": "standard",
  "model_profile": "balanced",
  "workflow": {
    "research": true,
    "plan_check": true,
    "verifier": false,
    "nyquist_validation": false,
    "discuss_mode": "assumptions",
    "skip_discuss": true,
    "auto_advance": false
  },
  "git": {
    "branching_strategy": "milestone",
    "milestone_branch_template": "gsd/{milestone}-{slug}"
  }
}
```

From src/vcompany/cli/report_cmd.py:
```python
# vco report posts messages in format: "{timestamp} {agent_id}: {status_text}"
# to #agent-{agent_id} channel via Discord HTTP API
```

GSD workflow vco report patterns (from RESEARCH audit):
- research-phase.md: `vco report "starting research-phase $PHASE"` / `vco report "research-phase complete"`
- plan-phase.md: `vco report "starting plan-phase $PHASE"` / `vco report "@PM plan-phase complete - ready for review"`
- execute-phase.md: `vco report "starting execute-phase $PHASE_ARG"` / `vco report "@PM execute-phase complete - verify"`
- discuss-phase.md: NONE (must add)
- discuss-phase-assumptions.md: NONE (must add)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GSD config template and create config test</name>
  <files>src/vcompany/templates/gsd_config.json.j2, tests/test_gsd_config_template.py</files>
  <read_first>
    - src/vcompany/templates/gsd_config.json.j2
    - .planning/phases/10-rework-gsd-agent-dispatch-to-bypass-all-interactive-prompts-research-context-discuss-for-fully-autonomous-operation/10-CONTEXT.md
  </read_first>
  <action>
    Update src/vcompany/templates/gsd_config.json.j2 per D-18:
    1. Change `"discuss_mode": "assumptions"` to `"discuss_mode": "discuss"`
    2. Change `"skip_discuss": true` to `"skip_discuss": false`
    3. Add `"_auto_chain_active": false` to the workflow section (explicitly prevent auto-chaining per D-05)
    4. Keep all other values unchanged: `"research": true`, `"plan_check": true`, `"auto_advance": false`, `"mode": "yolo"`

    Create tests/test_gsd_config_template.py:
    - Test that loading the template produces JSON with `workflow.discuss_mode == "discuss"`
    - Test that `workflow.skip_discuss == false`
    - Test that `workflow.auto_advance == false`
    - Test that `workflow._auto_chain_active == false`
    - Test that `workflow.research == true` and `workflow.plan_check == true` (unchanged)
    - Use json.loads on the template file content directly (no Jinja2 rendering needed since template has no variables in workflow section)
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_gsd_config_template.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"discuss_mode": "discuss"' src/vcompany/templates/gsd_config.json.j2 returns 1
    - grep -c '"skip_discuss": false' src/vcompany/templates/gsd_config.json.j2 returns 1
    - grep -c '"_auto_chain_active": false' src/vcompany/templates/gsd_config.json.j2 returns 1
    - grep -c '"auto_advance": false' src/vcompany/templates/gsd_config.json.j2 returns 1
    - tests/test_gsd_config_template.py exists and all tests pass
  </acceptance_criteria>
  <done>GSD config template has discuss_mode=discuss, skip_discuss=false, _auto_chain_active=false, auto_advance=false. Tests verify all values.</done>
</task>

<task type="auto">
  <name>Task 2: Create GSD workflow patcher tool and patch tests</name>
  <files>tools/patch_gsd_workflows.py, tests/test_gsd_patches.py</files>
  <read_first>
    - ~/.claude/get-shit-done/workflows/discuss-phase.md (first 50 lines and last 50 lines, look for step names and auto_advance)
    - ~/.claude/get-shit-done/workflows/plan-phase.md (grep for context_gate, ui_gate, AskUserQuestion)
    - ~/.claude/get-shit-done/workflows/execute-phase.md (grep for cross_phase_regression, AskUserQuestion)
    - ~/.claude/get-shit-done/workflows/execute-plan.md (grep for previous_phase_check, AskUserQuestion)
    - .planning/phases/10-rework-gsd-agent-dispatch-to-bypass-all-interactive-prompts-research-context-discuss-for-fully-autonomous-operation/10-RESEARCH.md
  </read_first>
  <action>
    Create tools/patch_gsd_workflows.py with the following structure:

    1. Constants:
       - `GSD_WORKFLOWS_DIR = Path.home() / ".claude" / "get-shit-done" / "workflows"`
       - `PATCH_MARKER = "# VCOMPANY-PATCHED"` (added as comment to mark patched files)

    2. Function `patch_discuss_phase()`:
       - Read discuss-phase.md
       - If PATCH_MARKER not present, add it at the top
       - Insert `vco report "starting discuss-phase $PHASE" 2>/dev/null || true` after the parse_args/init step (before check_existing step)
       - Insert `vco report "discuss-phase complete" 2>/dev/null || true` before the auto_advance step
       - Also patch discuss-phase-assumptions.md with same pattern if it exists
       - Idempotent: skip if PATCH_MARKER already present

    3. Function `patch_plan_phase()`:
       - Read plan-phase.md
       - Patch context_gate: where it prompts about existing CONTEXT.md, add a condition that auto-selects "Continue without context" when `$SKIP_CONTEXT_GATE` env var is set (or simpler: add instruction text that says "If running in autonomous mode (no interactive terminal), auto-select 'Continue' and skip this prompt")
       - Patch ui_gate: add instruction to auto-select "Skip" for UI spec generation
       - These are instruction-level patches (modify the markdown prompt text that Claude reads), not code patches

    4. Function `patch_execute_phase()`:
       - Read execute-phase.md
       - Patch cross_phase_regression: add instruction to auto-select "Fix regressions" as default

    5. Function `patch_execute_plan()`:
       - Read execute-plan.md
       - Patch previous_phase_check: add instruction to auto-select "Proceed anyway"

    6. Function `verify_patches() -> dict[str, bool]`:
       - Check each workflow file for PATCH_MARKER
       - Check discuss-phase.md contains "vco report.*discuss-phase complete"
       - Return dict of file -> patched status

    7. Function `patch_all() -> dict[str, bool]`:
       - Call all patch functions, return results

    8. `if __name__ == "__main__":` block that runs patch_all() and prints results

    IMPORTANT per D-12: Patches are to the actual GSD source files at ~/.claude/get-shit-done/workflows/. These are global -- all agents inherit.

    The patch strategy for plan-phase, execute-phase, execute-plan is to add markdown instruction blocks that tell the Claude executor to auto-select specific options when in yolo mode. The pattern is:
    ```
    **AUTONOMOUS MODE**: When `mode` is `yolo` in config, skip this prompt and auto-select "{option}".
    ```
    Insert this text right before or within the relevant AskUserQuestion prompt sections.

    Create tests/test_gsd_patches.py:
    - Use tmp_path to create mock workflow files with representative content
    - Test patch_discuss_phase adds vco report start and end calls
    - Test patch_plan_phase adds autonomous mode instructions for context_gate and ui_gate
    - Test patch_execute_plan adds autonomous mode instruction for previous_phase_check
    - Test idempotency: running patch twice produces same output
    - Test verify_patches returns correct status
    - Mock GSD_WORKFLOWS_DIR to point at tmp_path
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_gsd_patches.py -x --timeout=10</automated>
  </verify>
  <acceptance_criteria>
    - tools/patch_gsd_workflows.py exists
    - grep -c "def patch_all" tools/patch_gsd_workflows.py returns 1
    - grep -c "def verify_patches" tools/patch_gsd_workflows.py returns 1
    - grep -c "def patch_discuss_phase" tools/patch_gsd_workflows.py returns 1
    - grep -c "PATCH_MARKER" tools/patch_gsd_workflows.py returns at least 2
    - tests/test_gsd_patches.py exists and all tests pass
  </acceptance_criteria>
  <done>GSD workflow patcher tool creates idempotent patches for discuss-phase (vco report), plan-phase (context_gate, ui_gate), execute-phase (cross_phase_regression), and execute-plan (previous_phase_check). Tests verify all patches and idempotency.</done>
</task>

</tasks>

<verification>
1. `cd /home/developer/vcompany && uv run pytest tests/test_gsd_config_template.py tests/test_gsd_patches.py -x --timeout=10` -- all tests pass
2. `grep '"discuss_mode": "discuss"' src/vcompany/templates/gsd_config.json.j2` returns a match
3. `python tools/patch_gsd_workflows.py` runs without error (if GSD workflows exist)
</verification>

<success_criteria>
- GSD config template updated with all D-18 values (discuss_mode=discuss, skip_discuss=false, _auto_chain_active=false)
- Patch tool handles all non-AskUserQuestion interactive prompts identified in RESEARCH audit
- discuss-phase gets vco report start/end signals
- All patches are idempotent
- Tests cover config values, patch application, and idempotency
</success_criteria>

<output>
After completion, create `.planning/phases/10-rework-gsd-agent-dispatch-to-bypass-all-interactive-prompts-research-context-discuss-for-fully-autonomous-operation/10-01-SUMMARY.md`
</output>
