---
phase: 29-transport-channel-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/transport/channel/__init__.py
  - src/vcompany/transport/channel/messages.py
  - src/vcompany/transport/channel/framing.py
  - tests/test_channel_protocol.py
autonomous: true
requirements:
  - CHAN-01
must_haves:
  truths:
    - "All 5 head-to-worker message types (start, give-task, message, stop, health-check) are defined as typed Pydantic models"
    - "All 5 worker-to-head message types (signal, report, ask, send-file, health-report) are defined as typed Pydantic models"
    - "Any message can be serialized to NDJSON bytes and deserialized back to the same model with identical field values"
    - "Head messages and worker messages are separate discriminated unions -- decoder knows direction"
    - "Protocol module depends only on pydantic + stdlib (no discord.py, no anthropic, no libtmux)"
  artifacts:
    - path: "src/vcompany/transport/channel/messages.py"
      provides: "All 10 message Pydantic models + HeadMessage/WorkerMessage discriminated unions + StrEnum types"
      contains: "HeadMessage"
    - path: "src/vcompany/transport/channel/framing.py"
      provides: "encode(), decode_head(), decode_worker() NDJSON serialization functions"
      exports: ["encode", "decode_head", "decode_worker"]
    - path: "src/vcompany/transport/channel/__init__.py"
      provides: "Public API re-exports"
      contains: "encode"
    - path: "tests/test_channel_protocol.py"
      provides: "Round-trip serialization tests for all 10 message types + error cases"
      min_lines: 80
  key_links:
    - from: "src/vcompany/transport/channel/framing.py"
      to: "src/vcompany/transport/channel/messages.py"
      via: "TypeAdapter on HeadMessage/WorkerMessage unions"
      pattern: "TypeAdapter.*HeadMessage"
    - from: "tests/test_channel_protocol.py"
      to: "src/vcompany/transport/channel/framing.py"
      via: "encode/decode round-trip calls"
      pattern: "decode_head.*encode"
---

<objective>
Define the bidirectional transport channel protocol as typed Pydantic v2 models with NDJSON framing.

Purpose: This protocol is the foundation for all head-worker communication in v4.0. Every subsequent phase (worker runtime, head refactor, transport implementations) depends on these message definitions.
Output: `src/vcompany/transport/channel/` package with message models, NDJSON encode/decode, and a comprehensive test suite proving round-trip fidelity for all 10 message types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-channel-protocol/29-RESEARCH.md

@src/vcompany/daemon/protocol.py
@src/vcompany/transport/protocol.py
@src/vcompany/transport/__init__.py

<interfaces>
<!-- Existing NDJSON pattern from daemon/protocol.py that channel protocol mirrors -->

From src/vcompany/daemon/protocol.py:
```python
PROTOCOL_VERSION: int = 1

class Request(BaseModel):
    def to_line(self) -> bytes:
        return self.model_dump_json().encode() + b"\n"

    @classmethod
    def from_line(cls, line: bytes) -> Request:
        return cls.model_validate_json(line.strip())
```

From src/vcompany/transport/__init__.py:
```python
__all__ = ["AgentTransport", "DockerTransport", "LocalTransport", "NoopTransport"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define channel protocol message models and NDJSON framing</name>
  <files>
    src/vcompany/transport/channel/__init__.py,
    src/vcompany/transport/channel/messages.py,
    src/vcompany/transport/channel/framing.py,
    tests/test_channel_protocol.py
  </files>
  <read_first>
    src/vcompany/daemon/protocol.py,
    src/vcompany/transport/protocol.py,
    src/vcompany/transport/__init__.py,
    .planning/phases/29-transport-channel-protocol/29-RESEARCH.md
  </read_first>
  <behavior>
    - Round-trip: StartMessage(agent_id="a1", config={"handler": "session"}) -> encode -> decode_head -> identical model
    - Round-trip: GiveTaskMessage(task_id="t-1", description="Build") -> encode -> decode_head -> identical model
    - Round-trip: InboundMessage(sender="human", channel="dev", content="hi") -> encode -> decode_head -> identical model
    - Round-trip: StopMessage(reason="done", graceful=True) -> encode -> decode_head -> identical model
    - Round-trip: HealthCheckMessage() -> encode -> decode_head -> identical model
    - Round-trip: SignalMessage(signal="ready") -> encode -> decode_worker -> identical model
    - Round-trip: ReportMessage(channel="dev", content="Done") -> encode -> decode_worker -> identical model
    - Round-trip: AskMessage(channel="strat", question="Which?") -> encode -> decode_worker -> identical model
    - Round-trip: SendFileMessage(channel="dev", filename="out.txt", content_b64="aGVsbG8=") -> encode -> decode_worker -> identical model
    - Round-trip: HealthReportMessage(status="healthy", uptime_seconds=120.5) -> encode -> decode_worker -> identical model
    - All encoded bytes end with b"\n" (NDJSON line terminator)
    - decode_head rejects worker message JSON (wrong discriminator)
    - decode_worker rejects head message JSON (wrong discriminator)
    - decode_head rejects malformed JSON (raises ValidationError)
    - Discriminated union resolves to correct concrete type (type(decoded) is type(original))
  </behavior>
  <action>
    Create `src/vcompany/transport/channel/` package with three files:

    **messages.py** -- All Pydantic v2 message models:

    1. Define `HeadMessageType(StrEnum)` with values: "start", "give-task", "message", "stop", "health-check"
    2. Define `WorkerMessageType(StrEnum)` with values: "signal", "report", "ask", "send-file", "health-report"
    3. Define 5 head-to-worker message models (each with `type: Literal[HeadMessageType.X] = HeadMessageType.X`):
       - `StartMessage`: agent_id (str), config (dict, default_factory=dict)
       - `GiveTaskMessage`: task_id (str), description (str), context (dict, default_factory=dict)
       - `InboundMessage`: sender (str), channel (str), content (str), message_id (str | None = None)
       - `StopMessage`: reason (str = ""), graceful (bool = True)
       - `HealthCheckMessage`: no extra fields
    4. Define 5 worker-to-head message models (each with `type: Literal[WorkerMessageType.X] = WorkerMessageType.X`):
       - `SignalMessage`: signal (str), detail (str = "")
       - `ReportMessage`: channel (str), content (str), task_id (str | None = None)
       - `AskMessage`: channel (str), question (str), context (dict, default_factory=dict)
       - `SendFileMessage`: channel (str), filename (str), content_b64 (str), description (str = "")
       - `HealthReportMessage`: status (str), agent_state (str = ""), uptime_seconds (float = 0.0), detail (dict, default_factory=dict)
    5. Define discriminated unions:
       - `HeadMessage = Annotated[Union[StartMessage, GiveTaskMessage, InboundMessage, StopMessage, HealthCheckMessage], Field(discriminator="type")]`
       - `WorkerMessage = Annotated[Union[SignalMessage, ReportMessage, AskMessage, SendFileMessage, HealthReportMessage], Field(discriminator="type")]`
    6. Use `from __future__ import annotations` at top. Import from typing: Annotated, Literal, Union. Import from enum: StrEnum. Import from pydantic: BaseModel, Field.

    **framing.py** -- NDJSON encode/decode:

    1. Import TypeAdapter from pydantic, import HeadMessage and WorkerMessage from .messages
    2. Define `PROTOCOL_VERSION: int = 1`
    3. Create module-level TypeAdapter instances:
       - `_head_adapter: TypeAdapter[HeadMessage] = TypeAdapter(HeadMessage)` (NOTE: must be after HeadMessage is fully resolved; if forward ref issues, call TypeAdapter at module level after all imports)
       - `_worker_adapter: TypeAdapter[WorkerMessage] = TypeAdapter(WorkerMessage)`
    4. `def encode(msg: BaseModel) -> bytes:` -- returns `msg.model_dump_json().encode("utf-8") + b"\n"`
    5. `def decode_head(data: bytes) -> HeadMessage:` -- returns `_head_adapter.validate_json(data.strip())`
    6. `def decode_worker(data: bytes) -> WorkerMessage:` -- returns `_worker_adapter.validate_json(data.strip())`

    **__init__.py** -- Public API:

    Re-export all 10 message classes, both StrEnum types, both union types, PROTOCOL_VERSION, encode, decode_head, decode_worker. Set `__all__` explicitly.

    **tests/test_channel_protocol.py** -- TDD test suite (write FIRST, before production code):

    1. Import all message classes, encode, decode_head, decode_worker from vcompany.transport.channel
    2. Define `ALL_HEAD_MESSAGES` list with one instance of each head message type (use concrete values, not empty)
    3. Define `ALL_WORKER_MESSAGES` list with one instance of each worker message type
    4. Use `@pytest.mark.parametrize("msg", ALL_HEAD_MESSAGES, ids=lambda m: m.type)` for head round-trip test
    5. Use `@pytest.mark.parametrize("msg", ALL_WORKER_MESSAGES, ids=lambda m: m.type)` for worker round-trip test
    6. Each round-trip test: encode -> decode -> assert equal, assert type match, assert bytes end with b"\n"
    7. Test cross-direction rejection: decode_head on worker message JSON raises ValidationError
    8. Test cross-direction rejection: decode_worker on head message JSON raises ValidationError
    9. Test malformed JSON: decode_head on b"not json\n" raises ValidationError
    10. Import ValidationError from pydantic for error assertions
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -m pytest tests/test_channel_protocol.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/transport/channel/messages.py contains `class HeadMessageType(StrEnum):`
    - src/vcompany/transport/channel/messages.py contains `class WorkerMessageType(StrEnum):`
    - src/vcompany/transport/channel/messages.py contains `class StartMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class GiveTaskMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class InboundMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class StopMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class HealthCheckMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class SignalMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class ReportMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class AskMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class SendFileMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `class HealthReportMessage(BaseModel):`
    - src/vcompany/transport/channel/messages.py contains `HeadMessage = Annotated[`
    - src/vcompany/transport/channel/messages.py contains `WorkerMessage = Annotated[`
    - src/vcompany/transport/channel/framing.py contains `def encode(msg`
    - src/vcompany/transport/channel/framing.py contains `def decode_head(data`
    - src/vcompany/transport/channel/framing.py contains `def decode_worker(data`
    - src/vcompany/transport/channel/framing.py contains `PROTOCOL_VERSION`
    - src/vcompany/transport/channel/framing.py contains `TypeAdapter`
    - src/vcompany/transport/channel/__init__.py contains `encode`
    - src/vcompany/transport/channel/__init__.py contains `HeadMessage`
    - src/vcompany/transport/channel/__init__.py contains `WorkerMessage`
    - tests/test_channel_protocol.py contains `test_head_message_roundtrip`
    - tests/test_channel_protocol.py contains `test_worker_message_roundtrip`
    - tests/test_channel_protocol.py contains `ValidationError`
    - `python -m pytest tests/test_channel_protocol.py -v` exits 0 with all tests passing
    - messages.py does NOT import from discord, anthropic, libtmux, or vcompany.daemon
    - framing.py does NOT import from discord, anthropic, libtmux, or vcompany.daemon
  </acceptance_criteria>
  <done>All 10 message types defined as Pydantic v2 models with discriminated unions. NDJSON encode/decode functions work. All parametrized round-trip tests pass. Cross-direction and malformed input tests pass. Protocol module has no heavy dependencies.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_channel_protocol.py -v` -- all tests pass
2. `python -c "from vcompany.transport.channel import encode, decode_head, decode_worker, HeadMessage, WorkerMessage, PROTOCOL_VERSION; print('imports OK')"` -- no import errors
3. `python -c "from vcompany.transport.channel.messages import StartMessage; import json; s = StartMessage(agent_id='x'); raw = json.loads(s.model_dump_json()); assert raw['type'] == 'start'; print('discriminator OK')"` -- type field present in JSON
4. `grep -r 'discord\|anthropic\|libtmux' src/vcompany/transport/channel/ || echo 'no heavy deps'` -- confirms no heavy dependencies
</verification>

<success_criteria>
- 10 typed Pydantic message models (5 head-to-worker, 5 worker-to-head) with discriminated unions
- NDJSON framing functions (encode, decode_head, decode_worker) following daemon/protocol.py pattern
- Round-trip serialization tests pass for every message type
- Cross-direction decode correctly rejected
- Protocol module importable without discord.py, anthropic, or libtmux
- PROTOCOL_VERSION = 1 constant defined
</success_criteria>

<output>
After completion, create `.planning/phases/29-transport-channel-protocol/29-01-SUMMARY.md`
</output>
