---
phase: 07-autonomy-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/autonomy/delegation.py
  - src/vcompany/supervisor/supervisor.py
  - tests/test_delegation.py
autonomous: true
requirements:
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "ContinuousAgent can create a DelegationRequest and send it to its supervisor"
    - "Supervisor validates delegation requests against DelegationPolicy caps and rate limits"
    - "Approved delegations spawn a TEMPORARY GsdAgent via ChildSpec"
    - "Rate limit rejects requests when max_delegations_per_hour exceeded"
    - "Concurrent cap rejects requests when max_concurrent_delegations reached"
    - "Completed/crashed delegated agents are tracked so rate limits release"
  artifacts:
    - path: "src/vcompany/autonomy/delegation.py"
      provides: "DelegationPolicy, DelegationRequest, DelegationResult, DelegationTracker"
      exports: ["DelegationPolicy", "DelegationRequest", "DelegationResult", "DelegationTracker"]
    - path: "src/vcompany/supervisor/supervisor.py"
      provides: "handle_delegation_request method and delegation cleanup in state change callback"
    - path: "tests/test_delegation.py"
      provides: "Tests for delegation policy enforcement, tracker, and supervisor integration"
  key_links:
    - from: "src/vcompany/supervisor/supervisor.py"
      to: "src/vcompany/autonomy/delegation.py"
      via: "Supervisor imports DelegationTracker, DelegationPolicy, DelegationRequest, DelegationResult"
      pattern: "from vcompany\\.autonomy\\.delegation import"
    - from: "src/vcompany/supervisor/supervisor.py"
      to: "src/vcompany/container/factory.py"
      via: "create_container() to spawn delegated agents"
      pattern: "create_container"
    - from: "src/vcompany/supervisor/supervisor.py"
      to: "src/vcompany/container/child_spec.py"
      via: "ChildSpec with RestartPolicy.TEMPORARY for delegated agents"
      pattern: "RestartPolicy\\.TEMPORARY"
---

<objective>
Implement the delegation protocol enabling ContinuousAgents to request task spawns through their supervisor with policy enforcement.

Purpose: AUTO-03 requires ContinuousAgent delegation with hard caps and rate limits. AUTO-04 requires supervisor validation, policy enforcement, and TEMPORARY agent spawning.
Output: DelegationPolicy/Request/Result/Tracker models, Supervisor.handle_delegation_request() method, delegation cleanup on child termination, fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-autonomy-features/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/vcompany/supervisor/supervisor.py:
```python
class Supervisor:
    def __init__(self, supervisor_id, strategy, child_specs, max_restarts=3,
                 window_seconds=600, parent=None, on_escalation=None,
                 data_dir=None, on_health_change=None) -> None: ...
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    async def _start_child(self, spec: ChildSpec) -> None: ...
    def _make_state_change_callback(self, child_id: str) -> Callable[[HealthReport], None]: ...
    def _get_spec(self, child_id: str) -> ChildSpec | None: ...
    # _child_specs: list[ChildSpec]
    # _children: dict[str, AgentContainer]
    # _restarting: bool
```

From src/vcompany/container/child_spec.py:
```python
class RestartPolicy(str, Enum):
    PERMANENT = "permanent"
    TEMPORARY = "temporary"
    TRANSIENT = "transient"

class ChildSpec(BaseModel):
    child_id: str
    agent_type: str
    context: ContainerContext
    restart_policy: RestartPolicy = RestartPolicy.PERMANENT
    max_restarts: int = 3
    restart_window_seconds: int = 600
```

From src/vcompany/container/context.py:
```python
class ContainerContext(BaseModel):
    agent_id: str
    agent_type: str
    parent_id: str | None = None
    project_id: str | None = None
    owned_dirs: list[str] = []
    gsd_mode: str = "full"
    system_prompt: str = ""
```

From src/vcompany/container/factory.py:
```python
def create_container(spec: ChildSpec, data_dir: Path, ...) -> AgentContainer: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DelegationPolicy, DelegationRequest, DelegationResult, and DelegationTracker</name>
  <files>src/vcompany/autonomy/delegation.py, tests/test_delegation.py</files>
  <read_first>src/vcompany/supervisor/supervisor.py, src/vcompany/container/child_spec.py, src/vcompany/container/context.py</read_first>
  <behavior>
    - Test: DelegationTracker.can_delegate returns (True, "") when under limits
    - Test: can_delegate returns (False, reason) when max_concurrent_delegations reached
    - Test: can_delegate returns (False, reason) when max_delegations_per_hour reached
    - Test: record_delegation tracks active agents per requester
    - Test: record_completion removes agent from active set, releasing capacity
    - Test: rate limit window slides -- old delegations expire after 1 hour
    - Test: DelegationPolicy defaults (max_concurrent=3, rate_limit=10/hr, allowed_types=["gsd"])
    - Test: DelegationRequest validates agent_type against policy allowed_types
  </behavior>
  <action>
    Create `src/vcompany/autonomy/delegation.py` with:

    1. `DelegationPolicy(BaseModel)`:
       - max_concurrent_delegations: int = 3
       - max_delegations_per_hour: int = 10
       - allowed_agent_types: list[str] = ["gsd"]

    2. `DelegationRequest` (dataclass):
       - requester_id: str
       - task_description: str
       - agent_type: str = "gsd"
       - context_overrides: dict = field(default_factory=dict)

    3. `DelegationResult` (dataclass):
       - approved: bool
       - agent_id: str | None = None
       - reason: str = ""

    4. `DelegationTracker`:
       - __init__(self, policy: DelegationPolicy)
       - _active: dict[str, set[str]] (requester_id -> set of delegated agent_ids)
       - _history: list[float] (monotonic timestamps of all delegations)
       - can_delegate(requester_id: str, agent_type: str) -> tuple[bool, str]:
         Check agent_type in policy.allowed_agent_types.
         Check active count for requester < max_concurrent_delegations.
         Filter _history to last 3600s, check < max_delegations_per_hour.
       - record_delegation(requester_id: str, agent_id: str) -> None:
         Add to _active set, append timestamp to _history.
       - record_completion(requester_id: str, agent_id: str) -> None:
         Remove from _active set (discard, no error if missing).

    Write tests in `tests/test_delegation.py`. Use time.monotonic() for rate limiting (inject via optional _clock parameter for testing). RED first, then GREEN.
  </action>
  <acceptance_criteria>
    - grep -q "class DelegationPolicy" src/vcompany/autonomy/delegation.py
    - grep -q "class DelegationRequest" src/vcompany/autonomy/delegation.py
    - grep -q "class DelegationResult" src/vcompany/autonomy/delegation.py
    - grep -q "class DelegationTracker" src/vcompany/autonomy/delegation.py
    - grep -q "def can_delegate" src/vcompany/autonomy/delegation.py
    - grep -q "def record_delegation" src/vcompany/autonomy/delegation.py
    - grep -q "def record_completion" src/vcompany/autonomy/delegation.py
    - grep -q "max_concurrent_delegations" src/vcompany/autonomy/delegation.py
    - grep -q "max_delegations_per_hour" src/vcompany/autonomy/delegation.py
    - grep -q "allowed_agent_types" src/vcompany/autonomy/delegation.py
    - grep -q "test_can_delegate" tests/test_delegation.py
    - grep -q "test_record_completion" tests/test_delegation.py
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_delegation.py -x -v -k "not Supervisor"</automated>
  </verify>
  <done>DelegationTracker enforces concurrent caps and hourly rate limits. Completion tracking releases capacity. All delegation model tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Supervisor delegation handling and cleanup wiring</name>
  <files>src/vcompany/supervisor/supervisor.py, tests/test_delegation.py</files>
  <read_first>src/vcompany/supervisor/supervisor.py, src/vcompany/autonomy/delegation.py, src/vcompany/container/factory.py, src/vcompany/container/child_spec.py</read_first>
  <action>
    Extend `src/vcompany/supervisor/supervisor.py`:

    1. Add optional `delegation_policy: DelegationPolicy | None = None` parameter to __init__.
       If provided, create `self._delegation_tracker = DelegationTracker(delegation_policy)`.
       If None, set `self._delegation_tracker = None`.
       Add `self._delegated_children: dict[str, str] = {}` mapping delegated agent_id -> requester_id.

    2. Add `async def handle_delegation_request(self, request: DelegationRequest) -> DelegationResult`:
       - If _delegation_tracker is None, return DelegationResult(approved=False, reason="Delegation not enabled")
       - Call can_delegate(request.requester_id, request.agent_type)
       - If not approved, return DelegationResult(approved=False, reason=reason)
       - Generate agent_id: f"delegated-{request.requester_id}-{uuid.uuid4().hex[:6]}"
       - Build ChildSpec with:
         child_id=agent_id, agent_type=request.agent_type,
         context=ContainerContext(agent_id=agent_id, agent_type=request.agent_type,
           parent_id=self.supervisor_id, project_id from first child_spec's context or None),
         restart_policy=RestartPolicy.TEMPORARY
       - Apply context_overrides from request (update context fields)
       - Append spec to _child_specs
       - Call await self._start_child(spec)
       - Call _delegation_tracker.record_delegation(request.requester_id, agent_id)
       - Store in _delegated_children: agent_id -> requester_id
       - Return DelegationResult(approved=True, agent_id=agent_id)

    3. In `_make_state_change_callback`, after the existing health report storage, add:
       If child_id is in _delegated_children AND report.state in ("stopped", "destroyed"):
         requester_id = _delegated_children.pop(child_id)
         if _delegation_tracker: _delegation_tracker.record_completion(requester_id, child_id)

    Add integration tests to `tests/test_delegation.py` (in a TestSupervisorDelegation class):
    - Test: handle_delegation_request with no policy returns not approved
    - Test: handle_delegation_request with valid policy spawns child with TEMPORARY restart policy
    - Test: delegated child reaching stopped state triggers record_completion in tracker
    - Test: max concurrent cap reached returns not approved

    Use a minimal Supervisor setup with mock containers (use the factory registry pattern from existing tests -- register a simple stub agent type).
  </action>
  <acceptance_criteria>
    - grep -q "handle_delegation_request" src/vcompany/supervisor/supervisor.py
    - grep -q "_delegation_tracker" src/vcompany/supervisor/supervisor.py
    - grep -q "_delegated_children" src/vcompany/supervisor/supervisor.py
    - grep -q "delegation_policy" src/vcompany/supervisor/supervisor.py
    - grep -q "DelegationRequest" src/vcompany/supervisor/supervisor.py
    - grep -q "DelegationResult" src/vcompany/supervisor/supervisor.py
    - grep -q "RestartPolicy.TEMPORARY" src/vcompany/supervisor/supervisor.py
    - grep -q "record_completion" src/vcompany/supervisor/supervisor.py
    - grep -q "TestSupervisorDelegation" tests/test_delegation.py
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_delegation.py -x -v</automated>
  </verify>
  <done>Supervisor validates delegation requests against policy, spawns TEMPORARY GsdAgents, and automatically cleans up tracking when delegated children terminate. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_delegation.py -x -v` passes all tests
- Delegation policy caps and rate limits enforced
- Delegated agents spawned with TEMPORARY restart policy
- Completion cleanup wired into state change callback
</verification>

<success_criteria>
- DelegationTracker enforces max_concurrent_delegations and max_delegations_per_hour per AUTO-03
- Supervisor.handle_delegation_request validates, spawns TEMPORARY agents, and cleans up per AUTO-04
- No delegation leaks -- terminated agents always release their tracking slot
- Full test coverage for policy enforcement and supervisor integration
</success_criteria>

<output>
After completion, create `.planning/phases/07-autonomy-features/07-02-SUMMARY.md`
</output>
