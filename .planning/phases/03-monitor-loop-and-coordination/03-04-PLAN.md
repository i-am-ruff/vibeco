---
phase: 03-monitor-loop-and-coordination
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/coordination/__init__.py
  - src/vcompany/coordination/sync_context.py
  - src/vcompany/coordination/interfaces.py
  - src/vcompany/coordination/interactions.py
  - src/vcompany/models/coordination_state.py
  - src/vcompany/cli/sync_context_cmd.py
  - src/vcompany/cli/main.py
  - tests/test_sync_context.py
  - tests/test_coordination.py
autonomous: true
requirements:
  - COORD-01
  - COORD-02
  - COORD-03
  - SAFE-03

must_haves:
  truths:
    - "INTERFACES.md is managed at {project}/context/INTERFACES.md as the canonical copy"
    - "Interface change requests are logged to {project}/context/interface_changes.json as append-only Pydantic records"
    - "vco sync-context copies INTERFACES.md, MILESTONE-SCOPE.md, and STRATEGIST-PROMPT.md to all clones via write_atomic"
    - "INTERACTIONS.md documents all known concurrent interaction patterns (monitor reads during writes, simultaneous git ops, PROJECT-STATUS distribution, plan gate, sync-context, multiple monitors)"
  artifacts:
    - path: "src/vcompany/coordination/sync_context.py"
      provides: "sync_context_files function copying context to all clones"
      exports: ["sync_context_files", "SyncResult"]
    - path: "src/vcompany/coordination/interfaces.py"
      provides: "Interface change request logging and application"
      exports: ["log_interface_change", "apply_interface_change"]
    - path: "src/vcompany/coordination/interactions.py"
      provides: "INTERACTIONS.md content generation"
      exports: ["generate_interactions_md"]
    - path: "src/vcompany/models/coordination_state.py"
      provides: "Pydantic models for interface change records and logs"
      exports: ["InterfaceChangeRecord", "InterfaceChangeLog"]
    - path: "src/vcompany/cli/sync_context_cmd.py"
      provides: "vco sync-context CLI command"
      exports: ["sync_context"]
    - path: "tests/test_sync_context.py"
      provides: "Tests for sync-context file distribution"
    - path: "tests/test_coordination.py"
      provides: "Tests for interface change logging and canonical management"
  key_links:
    - from: "src/vcompany/coordination/sync_context.py"
      to: "src/vcompany/shared/file_ops.py"
      via: "write_atomic for all clone file writes"
      pattern: "write_atomic"
    - from: "src/vcompany/coordination/interfaces.py"
      to: "src/vcompany/shared/file_ops.py"
      via: "write_atomic for interface_changes.json"
      pattern: "write_atomic"
    - from: "src/vcompany/cli/main.py"
      to: "src/vcompany/cli/sync_context_cmd.py"
      via: "cli.add_command(sync_context)"
      pattern: "add_command.*sync.context"
---

<objective>
Build the coordination system: INTERFACES.md canonical management with change request logging, the `vco sync-context` CLI command, and the INTERACTIONS.md reference document.

Purpose: Coordination artifacts keep agents aligned on API contracts and aware of concurrency patterns. This is the vertical slice for all non-monitor coordination work.
Output: Coordination modules, sync-context CLI, INTERACTIONS.md content generator, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monitor-loop-and-coordination/03-CONTEXT.md
@.planning/phases/03-monitor-loop-and-coordination/03-RESEARCH.md

<interfaces>
From src/vcompany/shared/file_ops.py:
```python
def write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None: ...
```

From src/vcompany/models/config.py:
```python
class ProjectConfig(BaseModel):
    project: str
    repo: str
    agents: list[AgentConfig]

def load_config(config_path: Path) -> ProjectConfig: ...
```

From src/vcompany/cli/main.py (current state):
```python
@click.group()
@click.version_option(package_name="vcompany")
def cli(): ...

cli.add_command(clone)
cli.add_command(dispatch)
cli.add_command(init)
cli.add_command(kill)
cli.add_command(preflight)
cli.add_command(relaunch)
```

From 03-RESEARCH.md - InterfaceChangeRecord model (to be placed in coordination_state.py):
```python
class InterfaceChangeRecord(BaseModel):
    timestamp: datetime
    agent_id: str
    action: Literal["proposed", "approved", "rejected", "applied"]
    description: str
    diff: str
    reviewer_note: str = ""

class InterfaceChangeLog(BaseModel):
    project: str
    records: list[InterfaceChangeRecord] = []
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interface change logging and sync-context implementation</name>
  <files>
    src/vcompany/coordination/__init__.py,
    src/vcompany/coordination/sync_context.py,
    src/vcompany/coordination/interfaces.py,
    src/vcompany/models/coordination_state.py,
    tests/test_sync_context.py,
    tests/test_coordination.py
  </files>
  <read_first>
    src/vcompany/shared/file_ops.py,
    src/vcompany/models/config.py,
    src/vcompany/models/agent_state.py,
    src/vcompany/orchestrator/crash_tracker.py
  </read_first>
  <behavior>
    - test_sync_copies_existing_files: sync_context_files copies INTERFACES.md, MILESTONE-SCOPE.md, STRATEGIST-PROMPT.md from context/ to each clone
    - test_sync_skips_missing_files: If MILESTONE-SCOPE.md doesn't exist in context/, it's skipped (not an error)
    - test_sync_uses_write_atomic: All writes use write_atomic (not raw open)
    - test_sync_returns_result: SyncResult has clones_updated count, files_synced count, errors list
    - test_sync_handles_missing_clone_dir: Clone dir that doesn't exist is logged as error, other clones still synced
    - test_log_interface_change_appends: log_interface_change adds record to interface_changes.json
    - test_log_interface_change_creates_file: If interface_changes.json doesn't exist, creates it
    - test_log_interface_change_preserves_existing: Appending does not overwrite existing records
    - test_apply_interface_change: apply_interface_change writes new INTERFACES.md content atomically and calls sync_context_files
    - test_interface_change_record_model: InterfaceChangeRecord validates Literal action field
  </behavior>
  <action>
    1. Create `src/vcompany/coordination/__init__.py` (empty).

    2. Create `src/vcompany/models/coordination_state.py` with Pydantic models per D-14 through D-17:
       - `InterfaceChangeRecord(BaseModel)`: timestamp, agent_id, action (Literal["proposed", "approved", "rejected", "applied"]), description, diff, reviewer_note=""
       - `InterfaceChangeLog(BaseModel)`: project str, records list[InterfaceChangeRecord] = []
       - Follow the CrashLog pattern from models/agent_state.py.
       - NOTE: These models live in coordination_state.py (NOT monitor_state.py) to avoid
         file conflicts with Plan 03-01 which owns monitor_state.py.

    3. Create `src/vcompany/coordination/sync_context.py` per D-13:
       - `SYNC_FILES = ["INTERFACES.md", "MILESTONE-SCOPE.md", "STRATEGIST-PROMPT.md"]`
       - `SyncResult` dataclass: clones_updated (int), files_synced (int), errors (list[str])
       - `sync_context_files(project_dir: Path, config: ProjectConfig) -> SyncResult`:
         - Read from `{project_dir}/context/{filename}`.
         - Write to each `{project_dir}/clones/{agent.id}/{filename}` via write_atomic.
         - Skip missing source files (not an error).
         - Catch per-clone errors, append to errors list, continue with other clones.

    4. Create `src/vcompany/coordination/interfaces.py` per D-14 through D-17:
       - Import InterfaceChangeRecord and InterfaceChangeLog from
         `vcompany.models.coordination_state` (NOT monitor_state).
       - `log_interface_change(project_dir: Path, record: InterfaceChangeRecord) -> None`:
         - Read existing `{project_dir}/context/interface_changes.json` or create new InterfaceChangeLog.
         - Append record. Write back via write_atomic.
       - `apply_interface_change(project_dir: Path, config: ProjectConfig, new_content: str, record: InterfaceChangeRecord) -> SyncResult`:
         - Write new_content to `{project_dir}/context/INTERFACES.md` via write_atomic per D-16.
         - Log the record with action="applied".
         - Call sync_context_files to distribute to all clones.
         - Return the SyncResult.

    5. Tests:
       - `tests/test_sync_context.py`: Use tmp_path to create mock project structure with context/ and clones/ dirs. Verify files copied, missing files skipped, write_atomic used.
       - `tests/test_coordination.py`: Test interface change logging (create, append, preserve), apply flow. Import models from `vcompany.models.coordination_state`.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_sync_context.py tests/test_coordination.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SYNC_FILES" src/vcompany/coordination/sync_context.py
    - grep -q "def sync_context_files" src/vcompany/coordination/sync_context.py
    - grep -q "class SyncResult" src/vcompany/coordination/sync_context.py
    - grep -q "write_atomic" src/vcompany/coordination/sync_context.py
    - grep -q "INTERFACES.md" src/vcompany/coordination/sync_context.py
    - grep -q "MILESTONE-SCOPE.md" src/vcompany/coordination/sync_context.py
    - grep -q "STRATEGIST-PROMPT.md" src/vcompany/coordination/sync_context.py
    - grep -q "def log_interface_change" src/vcompany/coordination/interfaces.py
    - grep -q "def apply_interface_change" src/vcompany/coordination/interfaces.py
    - grep -q "interface_changes.json" src/vcompany/coordination/interfaces.py
    - grep -q "class InterfaceChangeRecord" src/vcompany/models/coordination_state.py
    - grep -q "class InterfaceChangeLog" src/vcompany/models/coordination_state.py
    - grep -q "test_sync_copies_existing_files" tests/test_sync_context.py
    - grep -q "test_log_interface_change_appends" tests/test_coordination.py
  </acceptance_criteria>
  <done>
    sync_context_files copies INTERFACES.md, MILESTONE-SCOPE.md, STRATEGIST-PROMPT.md to all clones via write_atomic. Interface change requests are logged to interface_changes.json as append-only records. apply_interface_change writes + syncs + logs. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: INTERACTIONS.md generator and vco sync-context CLI</name>
  <files>
    src/vcompany/coordination/interactions.py,
    src/vcompany/cli/sync_context_cmd.py,
    src/vcompany/cli/main.py
  </files>
  <read_first>
    src/vcompany/cli/main.py,
    src/vcompany/cli/dispatch_cmd.py,
    src/vcompany/coordination/sync_context.py,
    .planning/phases/03-monitor-loop-and-coordination/03-RESEARCH.md (INTERACTIONS.md content section)
  </read_first>
  <action>
    1. Create `src/vcompany/coordination/interactions.py` for SAFE-03:
       - `generate_interactions_md() -> str`: Returns the full INTERACTIONS.md content as a string.
       - Content must document ALL known interaction patterns from Research:
         a. **Monitor reads while agent writes** -- Safe. write_atomic ensures complete reads.
         b. **Simultaneous git operations across clones** -- Safe. Independent repos.
         c. **PROJECT-STATUS.md distribution during agent read** -- Safe. Atomic rename.
         d. **Plan gate approve while agent is idle** -- Safe. Agent at prompt, send_keys delivers.
         e. **sync-context during agent execution** -- Safe. write_atomic protection.
         f. **Multiple monitors (accidental)** -- UNSAFE. Duplicate alerts, duplicate plan gates.
            Mitigation: PID file at `{project}/state/monitor.pid`, check on startup, refuse if alive.
         g. **Hook timeout during context compression** -- Agent is busy with context compression when hook fires. Hook has independent timeout fallback (Phase 5).
         h. **Simultaneous git pushes from multiple agents** -- Safe per branch-per-agent isolation. Each agent pushes to its own branch.
       - Format: Markdown with ## section per pattern, each with Scenario, Safe?, Mitigation fields.
       - Header: `# Known Interaction Patterns\n\nThis document catalogs known concurrent interaction scenarios in vCompany.\nAuto-generated by vco. Review and update as new patterns are discovered.\n`

    2. Create `src/vcompany/cli/sync_context_cmd.py`:
       ```python
       @click.command("sync-context")
       @click.argument("project_dir", type=click.Path(exists=True, path_type=Path))
       def sync_context(project_dir: Path) -> None:
           """Push updated context files to all agent clones."""
       ```
       - Load config via `load_config(project_dir / "agents.yaml")`.
       - Also generate INTERACTIONS.md and write to `{project_dir}/context/INTERACTIONS.md` via write_atomic.
       - Call `sync_context_files(project_dir, config)`.
       - Print result summary using Rich or click.echo: `"Synced {files_synced} files to {clones_updated} clones"`.
       - Print any errors.

    3. Update `src/vcompany/cli/main.py`:
       - Add `from vcompany.cli.sync_context_cmd import sync_context` import.
       - Add `cli.add_command(sync_context)`.
       - NOTE: Plan 03 also adds to main.py (monitor command). Since Plans 03 and 04 are in different waves (Plan 03 is Wave 2, Plan 04 is Wave 1), Plan 04 runs first. Plan 03 will see the sync_context addition and add monitor alongside it. No conflict.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run python -c "from vcompany.coordination.interactions import generate_interactions_md; md = generate_interactions_md(); assert 'Monitor reads' in md; assert 'Multiple monitors' in md; assert 'UNSAFE' in md or 'No' in md; print('OK')" && uv run vco --help | grep -q sync-context && echo "CLI registered"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def generate_interactions_md" src/vcompany/coordination/interactions.py
    - grep -q "Monitor reads while agent writes" src/vcompany/coordination/interactions.py
    - grep -q "Multiple monitors" src/vcompany/coordination/interactions.py
    - grep -q "monitor.pid" src/vcompany/coordination/interactions.py
    - grep -q "Simultaneous git" src/vcompany/coordination/interactions.py
    - grep -q "def sync_context" src/vcompany/cli/sync_context_cmd.py
    - grep -q "sync_context_files" src/vcompany/cli/sync_context_cmd.py
    - grep -q "load_config" src/vcompany/cli/sync_context_cmd.py
    - grep -q "from vcompany.cli.sync_context_cmd import sync_context" src/vcompany/cli/main.py
    - grep -q "add_command(sync_context)" src/vcompany/cli/main.py
  </acceptance_criteria>
  <done>
    INTERACTIONS.md covers all 8 known interaction patterns with scenario/safety/mitigation analysis. vco sync-context command copies context files to all clones and generates INTERACTIONS.md. CLI registered in main.py. All verification passes.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_sync_context.py tests/test_coordination.py -x -v` all pass
- `uv run vco --help` shows `sync-context` command
- INTERACTIONS.md covers all known patterns including the unsafe "multiple monitors" scenario
</verification>

<success_criteria>
- sync_context_files distributes INTERFACES.md, MILESTONE-SCOPE.md, STRATEGIST-PROMPT.md to all clones
- Interface change requests are logged append-only to interface_changes.json
- InterfaceChangeRecord and InterfaceChangeLog importable from vcompany.models.coordination_state
- INTERACTIONS.md documents all known concurrent scenarios with safety analysis
- vco sync-context CLI registered and functional
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitor-loop-and-coordination/03-04-SUMMARY.md`
</output>
