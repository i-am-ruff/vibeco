---
phase: 24-discord-visibility
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/vcompany/agent/fulltime_agent.py
  - src/vcompany/agent/company_agent.py
  - src/vcompany/agent/gsd_agent.py
autonomous: true
requirements: [VIS-01, VIS-03, VIS-05, VIS-06]

must_haves:
  truths:
    - "FulltimeAgent overrides receive_discord_message to parse inbound messages and dispatch PM actions (task_completed, assign task, escalate, review)"
    - "CompanyAgent overrides receive_discord_message to parse inbound messages and dispatch to StrategistConversation"
    - "GsdAgent emits Discord messages on phase transitions instead of calling _on_phase_transition callback"
    - "GsdAgent emits review requests as Discord messages instead of calling _on_review_request callback"
    - "FulltimeAgent._event_queue and CompanyAgent._event_queue are removed (VIS-05)"
    - "All callback fields for inter-agent routing are removed from all three agent types"
  artifacts:
    - path: "src/vcompany/agent/fulltime_agent.py"
      provides: "PM agent with Discord-based event handling"
      contains: "receive_discord_message"
    - path: "src/vcompany/agent/company_agent.py"
      provides: "Strategist agent with Discord-based event handling"
      contains: "receive_discord_message"
    - path: "src/vcompany/agent/gsd_agent.py"
      provides: "GSD agent that emits Discord messages on phase transitions"
      contains: "send_message"
  key_links:
    - from: "src/vcompany/agent/fulltime_agent.py"
      to: "src/vcompany/models/messages.py"
      via: "import MessageContext"
      pattern: "from vcompany\\.models\\.messages import MessageContext"
    - from: "src/vcompany/agent/gsd_agent.py"
      to: "src/vcompany/daemon/comm.py"
      via: "CommunicationPort.send_message for phase transition events"
      pattern: "send_message"
---

<objective>
Replace internal event queues and callbacks with Discord message handling in all three agent types.

Purpose: VIS-05 eliminates post_event() queues. VIS-06 makes task assignment a Discord message. VIS-01/VIS-03 make events visible. This plan transforms FulltimeAgent, CompanyAgent, and GsdAgent from internal-event-driven to Discord-message-driven agents.
Output: Three agent classes with receive_discord_message overrides and Discord emission for phase transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-discord-visibility/24-CONTEXT.md
@.planning/phases/24-discord-visibility/24-RESEARCH.md
@.planning/phases/24-discord-visibility/24-01-SUMMARY.md

@src/vcompany/agent/fulltime_agent.py
@src/vcompany/agent/company_agent.py
@src/vcompany/agent/gsd_agent.py
@src/vcompany/container/container.py
@src/vcompany/models/messages.py
@src/vcompany/daemon/comm.py
@src/vcompany/autonomy/project_state.py

<interfaces>
<!-- From Plan 01 (created) -->
class MessageContext(BaseModel):
    sender: str
    channel: str
    content: str
    parent_message: str | None = None
    message_id: str | None = None
    is_reply: bool = False

class AgentContainer:
    async def receive_discord_message(self, context: MessageContext) -> None: ...

<!-- From src/vcompany/daemon/comm.py -->
class SendMessagePayload(BaseModel):
    channel_id: str
    content: str

class CommunicationPort(Protocol):
    async def send_message(self, payload: SendMessagePayload) -> None: ...

<!-- From src/vcompany/agent/fulltime_agent.py (current, to be modified) -->
# These callback fields will be REMOVED:
self._on_gsd_review: Callable[[str, str], Awaitable[None]] | None
self._on_assign_task: Callable[[str, BacklogItem], Awaitable[None]] | None
self._on_trigger_integration_review: Callable[[], Awaitable[None]] | None
self._on_recruit_agent: Callable[[ChildSpec], Awaitable[None]] | None
self._on_remove_agent: Callable[[str], Awaitable[None]] | None
self._on_escalate_to_strategist: Callable[[str, str, float], Awaitable[str | None]] | None
self._on_send_intervention: Callable[[str, str], Awaitable[None]] | None

# These will be REMOVED:
self._event_queue: asyncio.Queue[dict[str, Any]]
async def post_event(self, event: dict[str, Any]) -> None
async def process_next_event(self) -> bool
async def _handle_event(self, event: dict[str, Any]) -> None

<!-- From src/vcompany/agent/company_agent.py (current, to be modified) -->
# These callback fields will be REMOVED:
self._on_response: Callable[[str, int], Awaitable[None]] | None
self._on_hire: Callable[..., Awaitable[Any]] | None
self._on_give_task: Callable[..., Awaitable[None]] | None
self._on_dismiss: Callable[..., Awaitable[None]] | None

# These will be REMOVED:
self._event_queue: asyncio.Queue[dict[str, Any]]
async def post_event(self, event: dict[str, Any]) -> None
async def process_next_event(self) -> bool
async def _handle_event(self, event: dict[str, Any]) -> None
async def _drain_events(self) -> None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FulltimeAgent event queue with Discord message handler</name>
  <files>src/vcompany/agent/fulltime_agent.py</files>
  <read_first>
    - src/vcompany/agent/fulltime_agent.py
    - src/vcompany/models/messages.py
    - src/vcompany/autonomy/project_state.py
    - src/vcompany/autonomy/backlog.py
    - src/vcompany/container/container.py
    - src/vcompany/daemon/comm.py
  </read_first>
  <action>
1. REMOVE from `__init__`:
   - `self._event_queue: asyncio.Queue[dict[str, Any]] = asyncio.Queue()`
   - `self._events_processed: int = 0`
   - `self._checkpoint_lock = asyncio.Lock()`
   - `self._on_gsd_review` callback field
   - `self._on_assign_task` callback field
   - `self._on_trigger_integration_review` callback field
   - `self._on_recruit_agent` callback field
   - `self._on_remove_agent` callback field
   - `self._on_escalate_to_strategist` callback field
   - `self._on_send_intervention` callback field

2. REMOVE these methods entirely:
   - `post_event()`
   - `process_next_event()`
   - `_handle_event()`

3. KEEP in `__init__`:
   - `self.backlog: BacklogQueue | None = None`
   - `self._project_state: ProjectStateManager | None = None`
   - `self._lifecycle = EventDrivenLifecycle(...)`
   - Stuck detector state (`_agent_state_timestamps`, `_stuck_threshold_seconds`, etc.)
   - `self._stuck_detector_task`

4. ADD import: `from vcompany.models.messages import MessageContext`

5. ADD `async def receive_discord_message(self, context: MessageContext) -> None:` override:
   - Parse `context.content` using prefixed message format conventions
   - Use `self._lifecycle.start_processing()` / `self._lifecycle.done_processing()` around processing

   Message type dispatch based on content prefix:
   - `[Phase Complete]` -- extract agent_id, log transition. Update `_agent_state_timestamps`. Call `self._on_gsd_review_via_discord(agent_id, phase)` if plan review needed.
   - `[Task Completed]` -- extract agent_id, item_id. Call `self._project_state.handle_task_completed(agent_id, item_id)` then `self._auto_assign_next(agent_id)`.
   - `[Task Failed]` -- extract agent_id, item_id. Call `self._project_state.handle_task_failed(agent_id, item_id)`.
   - `[Request Assignment]` -- extract agent_id. Call `self._project_state.assign_next_task(agent_id)`.
   - `[Health Change]` -- extract agent_id, state, inner_state. Update `_agent_state_timestamps`.
   - Otherwise: log as informational message (not every message needs action).

   For any unrecognized prefix, log at INFO level: `"PM received unhandled message from %s: %.100s"`.

6. ADD helper `async def _send_discord(self, channel_name: str, content: str) -> None:`:
   - Uses `self._comm_port` (from parent AgentContainer) if available
   - Constructs `SendMessagePayload(channel_id=channel_name, content=content)`
   - NOTE: channel_id resolution (name -> id) will be wired in Plan 04. For now, use the channel name as a logical identifier. The CommunicationPort adapter handles resolution.

7. KEEP `_auto_assign_next()` method -- it's task distribution logic, not routing. But modify it: instead of calling `self._on_assign_task(agent_id, item)`, it should call `await self._send_discord(f"agent-{agent_id}", f"[Task Assigned] @{agent_id}: {item.title} (item: {item.item_id})")` (per VIS-06, D-05).

8. KEEP the stuck detector methods (`_start_stuck_detector`, `_check_stuck_agents`) -- these are PM-internal monitoring. Modify `_check_stuck_agents` to use `_send_discord` instead of `_on_send_intervention` for sending intervention messages.

Per D-01: No more asyncio.Queue event processing. Discord messages arrive via receive_discord_message().
Per D-14: PM loses its special post_event() path. Becomes a normal agent interacting only through Discord.
Per VIS-06: Task assignment is a Discord message `[Task Assigned] @agent-id: ...` in the agent's channel.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "
from vcompany.agent.fulltime_agent import FulltimeAgent
assert hasattr(FulltimeAgent, 'receive_discord_message'), 'missing receive_discord_message'
assert not hasattr(FulltimeAgent, 'post_event') or FulltimeAgent.post_event is None, 'post_event still exists'
print('FulltimeAgent Discord-ready')
" && grep -c '_event_queue' src/vcompany/agent/fulltime_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/agent/fulltime_agent.py contains `async def receive_discord_message(self, context: MessageContext)`
    - src/vcompany/agent/fulltime_agent.py contains `from vcompany.models.messages import MessageContext`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `_event_queue`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `async def post_event`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `async def _handle_event`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `_on_gsd_review`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `_on_assign_task`
    - src/vcompany/agent/fulltime_agent.py does NOT contain `_on_escalate_to_strategist`
    - src/vcompany/agent/fulltime_agent.py contains `[Task Assigned]`
    - src/vcompany/agent/fulltime_agent.py contains `[Phase Complete]`
    - src/vcompany/agent/fulltime_agent.py contains `_send_discord`
  </acceptance_criteria>
  <done>FulltimeAgent processes events via receive_discord_message. No event queue, no callback fields. Task assignment emits Discord message per VIS-06.</done>
</task>

<task type="auto">
  <name>Task 2: Replace CompanyAgent event queue and GsdAgent callbacks with Discord messaging</name>
  <files>src/vcompany/agent/company_agent.py, src/vcompany/agent/gsd_agent.py</files>
  <read_first>
    - src/vcompany/agent/company_agent.py
    - src/vcompany/agent/gsd_agent.py
    - src/vcompany/models/messages.py
    - src/vcompany/strategist/conversation.py
    - src/vcompany/daemon/comm.py
    - src/vcompany/container/container.py
  </read_first>
  <action>
**CompanyAgent changes:**

1. REMOVE from `__init__`:
   - `self._event_queue: asyncio.Queue[dict[str, Any]] = asyncio.Queue()`
   - `self._events_processed: int = 0`
   - `self._checkpoint_lock = asyncio.Lock()`
   - `self._on_response` callback field
   - `self._on_hire` callback field
   - `self._on_give_task` callback field
   - `self._on_dismiss` callback field
   - `self._drain_task` field

2. REMOVE these methods entirely:
   - `post_event()`
   - `process_next_event()`
   - `_handle_event()`
   - `_drain_events()`

3. KEEP:
   - `self._conversation: StrategistConversation | None = None`
   - `initialize_conversation()`

4. ADD import: `from vcompany.models.messages import MessageContext`
   ADD import (TYPE_CHECKING): `from vcompany.daemon.comm import SendMessagePayload`

5. ADD `async def receive_discord_message(self, context: MessageContext) -> None:` override:
   - Use `self._lifecycle.start_processing()` / `self._lifecycle.done_processing()` around processing
   - If `self._conversation is None`: log warning and return
   - Send `context.content` to `self._conversation.send()` to get response
   - Post response back to Discord via `self._send_discord(context.channel, response)`
   - Log: `"Strategist responded to message from %s (len=%d)", context.sender, len(response)`

6. ADD helper `async def _send_discord(self, channel_name: str, content: str) -> None:`:
   - Same pattern as FulltimeAgent: uses `self._comm_port` with `SendMessagePayload`

Per D-14: Strategist loses its special post_event() path. Becomes a normal agent.

**GsdAgent changes:**

1. REMOVE from `__init__`:
   - `self._on_phase_transition: Callable[...] | None = None`
   - `self._on_review_request: Callable[...] | None = None`

2. KEEP:
   - `self._pending_review: asyncio.Future[str] | None = None`
   - `self._review_attempts: int = 0`
   - `self._max_review_attempts: int = 3`
   - `self._current_assignment: dict[str, Any] | None = None`

3. ADD import: `from vcompany.models.messages import MessageContext`

4. ADD helper `async def _send_discord(self, channel_name: str, content: str) -> None:`:
   - Same pattern as other agents

5. MODIFY `advance_phase()`:
   - Replace `if self._on_phase_transition is not None: await self._on_phase_transition(...)` with:
     `await self._send_discord(f"agent-{self.context.agent_id}", f"[Phase Complete] {self.context.agent_id} finished '{from_phase}', entering '{phase}'. @PM")`
   - Replace `if self._on_review_request is not None: await self._on_review_request(...)` with:
     `await self._send_discord(f"plan-review", f"[Review Request] {self.context.agent_id} requests review for '{phase}' stage")`

6. ADD `async def receive_discord_message(self, context: MessageContext) -> None:` override:
   - Parse for `[Review Decision]` messages: extract decision (approve/reject/modify/clarify)
   - If `self._pending_review is not None and not self._pending_review.done()`:
     `self._pending_review.set_result(decision)`
   - Parse for `[Task Assigned]` messages: extract task details, store in `self._current_assignment` and persist to memory_store
   - Otherwise: log as informational

Per VIS-01: Phase transitions produce Discord messages before the review gate.
Per VIS-03: Review decisions arrive as Discord messages.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "
from vcompany.agent.company_agent import CompanyAgent
from vcompany.agent.gsd_agent import GsdAgent
assert hasattr(CompanyAgent, 'receive_discord_message'), 'CompanyAgent missing receive_discord_message'
assert hasattr(GsdAgent, 'receive_discord_message'), 'GsdAgent missing receive_discord_message'
print('Both agents Discord-ready')
" && grep -c '_event_queue' src/vcompany/agent/company_agent.py && grep -c '_on_phase_transition' src/vcompany/agent/gsd_agent.py</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/agent/company_agent.py contains `async def receive_discord_message(self, context: MessageContext)`
    - src/vcompany/agent/company_agent.py does NOT contain `_event_queue`
    - src/vcompany/agent/company_agent.py does NOT contain `async def post_event`
    - src/vcompany/agent/company_agent.py does NOT contain `async def _drain_events`
    - src/vcompany/agent/company_agent.py does NOT contain `_on_response`
    - src/vcompany/agent/company_agent.py does NOT contain `_on_hire`
    - src/vcompany/agent/gsd_agent.py contains `async def receive_discord_message(self, context: MessageContext)`
    - src/vcompany/agent/gsd_agent.py does NOT contain `_on_phase_transition`
    - src/vcompany/agent/gsd_agent.py does NOT contain `_on_review_request`
    - src/vcompany/agent/gsd_agent.py contains `[Phase Complete]`
    - src/vcompany/agent/gsd_agent.py contains `[Review Request]`
    - src/vcompany/agent/gsd_agent.py contains `[Review Decision]`
    - src/vcompany/agent/gsd_agent.py contains `[Task Assigned]`
  </acceptance_criteria>
  <done>CompanyAgent and GsdAgent use Discord messages instead of internal event queues/callbacks. Phase transitions and review requests are Discord messages. Review decisions arrive via receive_discord_message.</done>
</task>

</tasks>

<verification>
- All three agents import MessageContext
- No `_event_queue` in any agent file
- No `post_event` method in FulltimeAgent or CompanyAgent
- No `_on_phase_transition` or `_on_review_request` in GsdAgent
- GsdAgent.advance_phase() contains `_send_discord` calls
- `python -c "from vcompany.agent.fulltime_agent import FulltimeAgent; from vcompany.agent.company_agent import CompanyAgent; from vcompany.agent.gsd_agent import GsdAgent"` succeeds
</verification>

<success_criteria>
- FulltimeAgent processes inbound Discord messages via receive_discord_message and dispatches PM actions
- CompanyAgent processes inbound Discord messages via receive_discord_message and forwards to StrategistConversation
- GsdAgent emits phase transition and review request messages to Discord
- GsdAgent receives review decisions and task assignments via Discord
- All callback fields for inter-agent routing are removed
- All event queues are removed
</success_criteria>

<output>
After completion, create `.planning/phases/24-discord-visibility/24-03-SUMMARY.md`
</output>
