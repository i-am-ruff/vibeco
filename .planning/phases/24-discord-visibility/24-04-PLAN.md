---
phase: 24-discord-visibility
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-03"]
files_modified:
  - src/vcompany/daemon/runtime_api.py
  - src/vcompany/supervisor/supervisor.py
  - src/vcompany/bot/cogs/plan_review.py
autonomous: true
requirements: [VIS-01, VIS-03, VIS-04, VIS-05, VIS-06]

must_haves:
  truths:
    - "RuntimeAPI has no agent-type-specific routing methods -- all removed per D-10/D-11 (VIS-04)"
    - "RuntimeAPI.new_project() registers agent handles with MentionRouterCog instead of wiring callbacks"
    - "Supervisor has no pm_event_sink -- health changes become Discord messages"
    - "PlanReviewCog posts review decisions as Discord messages instead of calling log_plan_decision"
    - "BacklogQueue on_mutation callback is wired to post to #backlog channel"
    - "signal_workflow_stage() and log_plan_decision() are removed (D-12, D-13)"
  artifacts:
    - path: "src/vcompany/daemon/runtime_api.py"
      provides: "RuntimeAPI with only infrastructure ops, no agent routing"
      contains: "register_agent"
    - path: "src/vcompany/supervisor/supervisor.py"
      provides: "Supervisor without pm_event_sink"
    - path: "src/vcompany/bot/cogs/plan_review.py"
      provides: "PlanReviewCog posting decisions as Discord messages"
  key_links:
    - from: "src/vcompany/daemon/runtime_api.py"
      to: "src/vcompany/bot/cogs/mention_router.py"
      via: "new_project registers agent handles"
      pattern: "register_agent"
    - from: "src/vcompany/daemon/runtime_api.py"
      to: "src/vcompany/autonomy/backlog.py"
      via: "wires on_mutation callback"
      pattern: "on_mutation"
---

<objective>
Remove all internal agent-routing methods from RuntimeAPI, unwire pm_event_sink from Supervisor, and connect the new Discord-based routing infrastructure.

Purpose: This is the final cleanup step (D-10). After Plans 01-03 built the new infrastructure and agent handlers, this plan removes the old internal routing and wires everything through Discord. After this plan, the only agent interaction channel is Discord.
Output: Clean RuntimeAPI with only infrastructure ops. Supervisor without pm_event_sink. PlanReviewCog posting decisions as Discord messages. BacklogQueue wired to #backlog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-discord-visibility/24-CONTEXT.md
@.planning/phases/24-discord-visibility/24-RESEARCH.md
@.planning/phases/24-discord-visibility/24-01-SUMMARY.md
@.planning/phases/24-discord-visibility/24-02-SUMMARY.md
@.planning/phases/24-discord-visibility/24-03-SUMMARY.md

@src/vcompany/daemon/runtime_api.py
@src/vcompany/supervisor/supervisor.py
@src/vcompany/bot/cogs/plan_review.py
@src/vcompany/bot/cogs/mention_router.py
@src/vcompany/autonomy/backlog.py

<interfaces>
<!-- From Plan 01 -->
class MentionRouterCog(commands.Cog):
    def register_agent(self, handle: str, container: AgentContainer, channel_id: str | None = None) -> None
    def unregister_agent(self, handle: str) -> None

class MessageContext(BaseModel):
    sender: str
    channel: str
    content: str
    parent_message: str | None = None
    message_id: str | None = None
    is_reply: bool = False

<!-- From Plan 02 -->
class BacklogQueue:
    def __init__(self, memory_store: MemoryStore, on_mutation: Callable[[str], Awaitable[None]] | None = None)

<!-- From Plan 03 — agents now have receive_discord_message, no post_event -->
class FulltimeAgent(AgentContainer):
    async def receive_discord_message(self, context: MessageContext) -> None
    # No more post_event(), no callback fields

class CompanyAgent(AgentContainer):
    async def receive_discord_message(self, context: MessageContext) -> None
    # No more post_event(), no callback fields

class GsdAgent(AgentContainer):
    async def receive_discord_message(self, context: MessageContext) -> None
    # advance_phase() emits Discord messages directly

<!-- From src/vcompany/daemon/comm.py -->
class SendMessagePayload(BaseModel):
    channel_id: str
    content: str

<!-- Methods to REMOVE from RuntimeAPI (D-10): -->
# _make_pm_event_sink, _make_gsd_cb, _make_briefing_cb
# _on_strategist_response, _on_assign_task, _on_recruit_agent, _on_remove_agent
# _on_escalate_to_strategist, _post_review_request, _dispatch_pm_review
# relay_strategist_message, relay_strategist_escalation_reply
# route_completion_to_pm, handle_pm_escalation
# signal_workflow_stage (D-12), log_plan_decision (D-13)

<!-- Methods to KEEP per D-11: -->
# hire, give_task, dismiss, dispatch, kill, relaunch
# new_project, new_project_from_name, remove_project
# status, health_tree, get_agent_states, get_container_info
# checkin, standup, run_integration
# resolve_review, verify_agent_execution
# relay_channel_message, register_channels, get_channel_id
# _on_escalation, _on_degraded, _on_recovered (system alerts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove agent-routing methods from RuntimeAPI and rewire new_project()</name>
  <files>src/vcompany/daemon/runtime_api.py</files>
  <read_first>
    - src/vcompany/daemon/runtime_api.py
    - src/vcompany/bot/cogs/mention_router.py
    - src/vcompany/autonomy/backlog.py
    - src/vcompany/agent/fulltime_agent.py
    - src/vcompany/agent/company_agent.py
    - src/vcompany/agent/gsd_agent.py
  </read_first>
  <action>
1. REMOVE these methods entirely from RuntimeAPI (D-10, VIS-04):
   - `_make_pm_event_sink()` -- PM event sink factory
   - `_make_gsd_cb()` -- GSD transition callback factory
   - `_make_briefing_cb()` -- Briefing callback factory
   - `_on_strategist_response()` -- Strategist response handler
   - `_on_assign_task()` -- Task assignment handler
   - `_on_recruit_agent()` -- Agent recruitment (NOTE: keep `hire()` which is the infrastructure op)
   - `_on_remove_agent()` -- Agent removal (NOTE: keep `dismiss()`)
   - `_on_escalate_to_strategist()` -- Strategist escalation
   - `_post_review_request()` -- Review request posting
   - `_dispatch_pm_review()` -- PM review dispatch
   - `relay_strategist_message()` -- Strategist message relay
   - `relay_strategist_escalation_reply()` -- Strategist escalation reply relay
   - `route_completion_to_pm()` -- Completion routing to PM
   - `handle_pm_escalation()` -- PM escalation handling
   - `signal_workflow_stage()` -- Dead code (D-12)
   - `log_plan_decision()` -- Replaced by Discord messages (D-13)

2. REMOVE `self._strategist_container: CompanyAgent | None = None` from `__init__` -- no longer needed since Strategist is accessed through generic handle routing.

3. ADD a new attribute to `__init__`:
   - `self._mention_router: Any | None = None` -- reference to MentionRouterCog for agent registration
   - Add method `def set_mention_router(self, router) -> None:` that stores the reference

4. MODIFY `new_project()` method:
   - REMOVE all callback wiring steps:
     - Remove PM event sink creation and assignment to supervisor
     - Remove `_make_gsd_cb` calls for GSD agents
     - Remove `_make_briefing_cb` calls for continuous agents
     - Remove PM action callback wiring (`_on_assign_task`, `_on_escalate_to_strategist`, etc.)
     - Remove review gate callback wiring (`_on_review_request`)
   - ADD agent handle registration:
     - After PM container creation: `if self._mention_router: self._mention_router.register_agent(f"PM{project_name}", pm_container, channel_id=self.get_channel_id("decisions"))`
     - After each GSD agent creation: `if self._mention_router: self._mention_router.register_agent(f"agent-{agent_id}", gsd_container, channel_id=self.get_channel_id(f"agent-{agent_id}"))`
   - ADD backlog on_mutation wiring:
     - Create a callback that posts to #backlog channel:
       ```python
       backlog_channel = self.get_channel_id("backlog")
       async def _backlog_notify(msg: str) -> None:
           if backlog_channel:
               await self._get_comm().send_message(SendMessagePayload(channel_id=backlog_channel, content=msg))
       ```
     - Pass `on_mutation=_backlog_notify` when creating BacklogQueue

5. MODIFY `remove_project()` if it exists:
   - Unregister agent handles via `self._mention_router.unregister_agent(handle)` for each project agent

6. Also register Strategist handle (if Strategist container is set up in startup, not per-project):
   - `if self._mention_router and self._strategist_container: self._mention_router.register_agent("Strategist", self._strategist_container, channel_id=self.get_channel_id("strategist"))`
   - Actually, since we removed `_strategist_container`, the Strategist registration should happen wherever the Strategist container is created/wired. Check the startup flow and add registration there.

7. CLEAN UP imports:
   - Remove any TYPE_CHECKING imports that are no longer used (e.g., if FulltimeAgent/CompanyAgent/GsdAgent were only used in removed methods)
   - Keep imports needed by remaining infrastructure methods

Per D-10: Clean sweep -- all agent-specific internal routing removed in one pass.
Per D-11: Infrastructure ops (hire, dispatch, kill, relaunch, new_project, remove_project, status, etc.) STAY.
Per D-14: No agent-type-specific routing methods remain.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "from vcompany.daemon.runtime_api import RuntimeAPI; print('RuntimeAPI imports OK')" && grep -cE 'def (_make_pm_event_sink|_make_gsd_cb|_make_briefing_cb|_on_strategist_response|_on_assign_task|relay_strategist_message|relay_strategist_escalation_reply|route_completion_to_pm|handle_pm_escalation|signal_workflow_stage|log_plan_decision|_dispatch_pm_review|_post_review_request)' src/vcompany/daemon/runtime_api.py</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/daemon/runtime_api.py does NOT contain `def _make_pm_event_sink`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def _make_gsd_cb`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def _make_briefing_cb`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def _on_assign_task`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def relay_strategist_message`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def relay_strategist_escalation_reply`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def route_completion_to_pm`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def handle_pm_escalation`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def signal_workflow_stage`
    - src/vcompany/daemon/runtime_api.py does NOT contain `def log_plan_decision`
    - src/vcompany/daemon/runtime_api.py contains `register_agent`
    - src/vcompany/daemon/runtime_api.py contains `on_mutation`
    - src/vcompany/daemon/runtime_api.py contains `set_mention_router`
    - src/vcompany/daemon/runtime_api.py contains `def hire` (infrastructure op kept)
    - src/vcompany/daemon/runtime_api.py contains `def status` (infrastructure op kept)
  </acceptance_criteria>
  <done>RuntimeAPI contains only infrastructure operations. All 16 agent-routing methods removed. Agent handles registered via MentionRouterCog. BacklogQueue wired with on_mutation callback to #backlog channel.</done>
</task>

<task type="auto">
  <name>Task 2: Remove pm_event_sink from Supervisor and update PlanReviewCog</name>
  <files>src/vcompany/supervisor/supervisor.py, src/vcompany/bot/cogs/plan_review.py</files>
  <read_first>
    - src/vcompany/supervisor/supervisor.py
    - src/vcompany/bot/cogs/plan_review.py
    - src/vcompany/daemon/runtime_api.py
    - src/vcompany/daemon/comm.py
  </read_first>
  <action>
**Supervisor changes:**

1. REMOVE from `__init__` parameters:
   - `pm_event_sink: Callable[[dict[str, Any]], Awaitable[None]] | None = None`

2. REMOVE from `__init__` body:
   - `self._pm_event_sink = pm_event_sink`

3. REMOVE method: `set_pm_event_sink()`

4. MODIFY `_make_state_change_callback()` (the method that creates on_state_change callbacks for child containers):
   - Remove the section (around lines 281-306) that checks `if self._pm_event_sink is not None and report.state in (...)` and creates `health_event`/`escalation_event` dicts to send via `pm_event_sink`
   - Health changes are now visible through Discord. The supervisor's job is just lifecycle management and restart strategies.
   - Keep the rest of the state change callback logic (health tree updates, delegation tracking, etc.)

5. VERIFY: Search for any other references to `_pm_event_sink` in the file and remove them all.

**PlanReviewCog changes:**

1. FIND any calls to `runtime_api.log_plan_decision()` -- these should be replaced with a Discord message to #decisions channel.

2. Where PlanReviewCog currently calls `log_plan_decision(agent_id, decision, confidence, reasoning)`:
   - Replace with a call to send a plain text message (D-05) to #decisions or #plan-review channel:
   - Use the bot's own send method: `await channel.send(f"[Review] Plan for {agent_id}: {decision.upper()} (confidence: {confidence}). {reasoning}")`
   - The channel should be the #plan-review channel where the review embed was posted (already available in the cog)

3. FIND any calls to `runtime_api.signal_workflow_stage()` and remove them (D-12, dead code).

4. Keep `runtime_api.resolve_review()` calls -- this is the infrastructure op that resolves the GSD agent's pending_review Future. It stays per D-11.

Per D-13: log_plan_decision replaced by Discord message.
Per D-12: signal_workflow_stage removed (dead code).
Per VIS-03: Plan review decisions are visible on Discord before processing.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "from vcompany.supervisor.supervisor import Supervisor; import inspect; sig = inspect.signature(Supervisor.__init__); assert 'pm_event_sink' not in sig.parameters; print('pm_event_sink removed from Supervisor')" && grep -c 'pm_event_sink' src/vcompany/supervisor/supervisor.py && grep -c 'log_plan_decision' src/vcompany/bot/cogs/plan_review.py</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/supervisor/supervisor.py does NOT contain `pm_event_sink` (0 occurrences)
    - src/vcompany/supervisor/supervisor.py does NOT contain `set_pm_event_sink`
    - src/vcompany/bot/cogs/plan_review.py does NOT contain `log_plan_decision`
    - src/vcompany/bot/cogs/plan_review.py does NOT contain `signal_workflow_stage`
    - src/vcompany/bot/cogs/plan_review.py contains `[Review]` (the new Discord message format)
    - Supervisor.__init__ signature does not include pm_event_sink parameter
  </acceptance_criteria>
  <done>Supervisor has no pm_event_sink. PlanReviewCog posts review decisions as Discord messages instead of calling log_plan_decision. All dead code removed.</done>
</task>

</tasks>

<verification>
- `python -c "from vcompany.daemon.runtime_api import RuntimeAPI"` succeeds
- `python -c "from vcompany.supervisor.supervisor import Supervisor"` succeeds
- `python -c "from vcompany.bot.cogs.plan_review import PlanReviewCog"` succeeds
- `grep -cE 'def (_make_pm_event_sink|_make_gsd_cb|_make_briefing_cb|relay_strategist|route_completion|handle_pm_escalation|signal_workflow_stage|log_plan_decision)' src/vcompany/daemon/runtime_api.py` returns 0
- `grep -c 'pm_event_sink' src/vcompany/supervisor/supervisor.py` returns 0
- `grep -c 'post_event' src/vcompany/agent/fulltime_agent.py src/vcompany/agent/company_agent.py` returns 0
</verification>

<success_criteria>
- RuntimeAPI has ONLY infrastructure methods (hire, dispatch, status, etc.) -- zero agent-routing methods
- Supervisor creates no pm_event_sink, has no set_pm_event_sink
- PlanReviewCog posts decisions as plain text Discord messages
- BacklogQueue on_mutation wired to #backlog channel in new_project()
- Agent handles registered with MentionRouterCog in new_project()
- All imports clean -- no unused imports from removed code
</success_criteria>

<output>
After completion, create `.planning/phases/24-discord-visibility/24-04-SUMMARY.md`
</output>
