---
phase: 06-pm-strategist-and-milestones
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/vcompany/bot/cogs/strategist.py
  - src/vcompany/strategist/decision_log.py
  - tests/test_strategist_cog.py
  - tests/test_decision_log.py
autonomous: true
requirements:
  - STRAT-09

must_haves:
  truths:
    - "StrategistCog bridges #strategist channel to persistent conversation per D-11"
    - "Owner messages in #strategist are forwarded to persistent conversation"
    - "Strategist responses stream to Discord with rate-limited edits per Pitfall 1"
    - "All PM and Strategist decisions are logged to #decisions channel per D-18/STRAT-09"
    - "Decision log entries include timestamp, question/plan, decision, confidence, who decided"
    - "Decisions also persisted to state/decisions.jsonl for PM lookback"
  artifacts:
    - path: "src/vcompany/bot/cogs/strategist.py"
      provides: "StrategistCog expanded from placeholder to persistent conversation manager"
      contains: "class StrategistCog"
    - path: "src/vcompany/strategist/decision_log.py"
      provides: "Decision logging to #decisions and local file"
      exports: ["DecisionLogger"]
    - path: "tests/test_strategist_cog.py"
      provides: "Tests for StrategistCog channel bridge"
    - path: "tests/test_decision_log.py"
      provides: "Tests for decision logging"
  key_links:
    - from: "src/vcompany/bot/cogs/strategist.py"
      to: "src/vcompany/strategist/conversation.py"
      via: "StrategistConversation.send() for persistent conversation"
      pattern: "self._conversation\\.send"
    - from: "src/vcompany/strategist/decision_log.py"
      to: "#decisions channel"
      via: "sends embed to decisions channel"
      pattern: "_decisions_channel"
---

<objective>
Expand StrategistCog from empty placeholder to full persistent conversation manager, and build the decision logging system.

Purpose: The StrategistCog bridges the #strategist Discord channel to the persistent Claude conversation (D-11). Owner messages go to the Strategist, responses stream back with rate-limited edits. The DecisionLogger posts all PM/Strategist decisions to #decisions as append-only records (D-18/STRAT-09) and persists to local JSONL for PM lookback.
Output: Expanded StrategistCog with owner channel bridge, streaming responses, and DecisionLogger with dual storage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
@.planning/phases/06-pm-strategist-and-milestones/06-RESEARCH.md
@.planning/phases/06-pm-strategist-and-milestones/06-02-SUMMARY.md

<interfaces>
From src/vcompany/bot/cogs/strategist.py (current placeholder):
```python
class StrategistCog(commands.Cog):
    def __init__(self, bot: VcoBot) -> None:
        self.bot = bot

async def setup(bot: commands.Bot) -> None:
    await bot.add_cog(StrategistCog(bot))
```

From src/vcompany/strategist/conversation.py (created by Plan 02):
```python
class StrategistConversation:
    def __init__(self, client: AsyncAnthropic, persona_path: Path | None = None, model: str = "claude-opus-4-6"): ...
    async def send(self, content: str, role: str = "user") -> AsyncGenerator[str, None]: ...
    @property
    def messages(self) -> list[dict]: ...
    @property
    def token_count(self) -> int: ...
```

From src/vcompany/strategist/models.py (created by Plan 01):
```python
class DecisionLogEntry(BaseModel):
    timestamp: datetime
    question_or_plan: str
    decision: str
    confidence_level: str
    decided_by: str
    agent_id: str
```

From src/vcompany/bot/cogs/alerts.py (established pattern):
```python
# Sync-to-async bridge pattern:
def make_sync_callbacks(self) -> dict:
    loop = self.bot.loop
    def on_event(arg):
        asyncio.run_coroutine_threadsafe(self.handle_event(arg), loop)
    return {"on_event": on_event}
```

Discord streaming pattern from Research:
```python
async def stream_to_discord(channel, stream_iter):
    msg = await channel.send("Thinking...")
    buffer = ""
    last_edit = time.monotonic()
    async for chunk in stream_iter:
        buffer += chunk
        if time.monotonic() - last_edit >= 1.0:
            await msg.edit(content=buffer[:2000])
            last_edit = time.monotonic()
    await msg.edit(content=buffer[:2000])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DecisionLogger with dual storage (#decisions + JSONL)</name>
  <files>
    src/vcompany/strategist/decision_log.py,
    tests/test_decision_log.py
  </files>
  <read_first>
    src/vcompany/strategist/models.py,
    src/vcompany/bot/cogs/alerts.py,
    src/vcompany/shared/file_ops.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test: log_decision appends entry to state/decisions.jsonl as JSON line
    - Test: log_decision posts compact embed to #decisions channel
    - Test: Multiple log_decision calls produce multiple JSONL lines (append-only)
    - Test: Embed includes timestamp, decided_by, confidence_level, and truncated decision text
    - Test: Missing #decisions channel gracefully skipped (no crash)
    - Test: File write uses append mode, not overwrite
    - Test: load_decisions reads all entries from decisions.jsonl
  </behavior>
  <action>
    1. Create `src/vcompany/strategist/decision_log.py` per D-18/STRAT-09:
       - `class DecisionLogger`:
         - `__init__(self, decisions_path: Path, decisions_channel: discord.TextChannel | None = None)`:
           - `self._decisions_path = decisions_path` (project_dir/state/decisions.jsonl)
           - `self._decisions_channel = decisions_channel`
         - `async def log_decision(self, entry: DecisionLogEntry) -> None`:
           - Append entry as JSON line to self._decisions_path (use asyncio.to_thread for file I/O)
           - Post compact embed to self._decisions_channel if available
         - `def _append_to_file(self, entry: DecisionLogEntry) -> None`:
           - `self._decisions_path.parent.mkdir(parents=True, exist_ok=True)`
           - Open file in append mode, write `entry.model_dump_json() + "\n"`
         - `async def _post_to_channel(self, entry: DecisionLogEntry) -> None`:
           - Build compact discord.Embed:
             - Title: f"{entry.decided_by} Decision"
             - Color: green for HIGH, yellow for MEDIUM, red for LOW
             - Fields: "Agent" (entry.agent_id), "Confidence" (entry.confidence_level), "Decision" (truncated to 200 chars)
             - Footer: timestamp
           - Send to channel, catch and log any errors
         - `def load_decisions(self) -> list[DecisionLogEntry]`:
           - Read self._decisions_path, parse each line as JSON
           - Return list of DecisionLogEntry (skip malformed lines with warning)

    2. Write tests in `tests/test_decision_log.py` FIRST (RED), then implement (GREEN).
       - Use tmp_path for decisions file
       - Mock discord.TextChannel for channel posting tests
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_decision_log.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class DecisionLogger" src/vcompany/strategist/decision_log.py` returns 1
    - `grep "decisions.jsonl" src/vcompany/strategist/decision_log.py` or usage in tests confirms JSONL storage
    - `grep "append" src/vcompany/strategist/decision_log.py` confirms append-only pattern
    - `grep "Embed" src/vcompany/strategist/decision_log.py` confirms Discord embed posting
    - `uv run pytest tests/test_decision_log.py -x -v` passes all tests
  </acceptance_criteria>
  <done>DecisionLogger posts compact embeds to #decisions and appends to JSONL file. Append-only, graceful on missing channel. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StrategistCog expansion (owner channel bridge + streaming)</name>
  <files>
    src/vcompany/bot/cogs/strategist.py,
    tests/test_strategist_cog.py
  </files>
  <read_first>
    src/vcompany/bot/cogs/strategist.py,
    src/vcompany/strategist/conversation.py,
    src/vcompany/strategist/decision_log.py,
    src/vcompany/bot/cogs/alerts.py,
    src/vcompany/bot/client.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test: on_message filters: only processes non-webhook, non-bot messages from users with vco-owner role in #strategist
    - Test: Owner message is forwarded to StrategistConversation.send()
    - Test: Streaming response is posted to channel with rate-limited edits (1/sec)
    - Test: Long responses (>2000 chars) are split into multiple messages per Pitfall 1
    - Test: PM escalation adds formatted message to conversation: "[PM Escalation] Agent {id} asks: {question}"
    - Test: Strategist response to escalation is returned for relay to agent
    - Test: StrategistCog provides make_sync_callbacks() for PM escalation from sync context
  </behavior>
  <action>
    1. Expand `src/vcompany/bot/cogs/strategist.py` from placeholder to full Cog per D-02, D-11:
       - Keep existing import pattern (TYPE_CHECKING for VcoBot)
       - Add imports: asyncio, logging, time, discord, anthropic, StrategistConversation, DecisionLogger
       - `class StrategistCog(commands.Cog)`:
         - `__init__(self, bot: VcoBot)`:
           - `self.bot = bot`
           - `self._conversation: StrategistConversation | None = None` (initialized in on_ready)
           - `self._strategist_channel: discord.TextChannel | None = None`
           - `self._decisions_channel: discord.TextChannel | None = None`
           - `self._decision_logger: DecisionLogger | None = None`
           - `self._owner_role_name = "vco-owner"`
         - `async def initialize(self, client: AsyncAnthropic, persona_path: Path | None, decisions_path: Path) -> None`:
           - Create StrategistConversation(client, persona_path)
           - Resolve #strategist and #decisions channels
           - Create DecisionLogger(decisions_path, self._decisions_channel)
           - Store all on self
         - `@commands.Cog.listener() async def on_ready(self)`:
           - Resolve channels
         - `@commands.Cog.listener() async def on_message(self, message: discord.Message)`:
           - Filter per Research Open Question 4:
             - Skip if message.webhook_id is not None (webhook messages)
             - Skip if message.author.id == self.bot.user.id (bot's own messages)
             - Skip if channel is not #strategist
             - Skip if author does not have vco-owner role
           - If conversation not initialized: reply "Strategist not initialized yet" and return
           - Call `_stream_to_channel(message.channel, message.content)`
         - `async def _stream_to_channel(self, channel: discord.TextChannel, content: str) -> str`:
           - Send placeholder message "Thinking..."
           - Stream from self._conversation.send(content)
           - Rate-limit edits to 1/sec using time.monotonic()
           - Handle >2000 char responses: truncate with "..." and send overflow as follow-up messages (per Pitfall 1)
           - Final edit with complete (truncated if needed) response
           - Return full response text
         - `async def handle_pm_escalation(self, agent_id: str, question: str, confidence_score: float) -> str`:
           - Format: `"[PM Escalation] Agent {agent_id} asks: {question}\nPM confidence: {confidence_score:.0%}. Please provide your assessment."`
           - Send to conversation, collect full response (don't stream to channel for escalations -- they're internal)
           - If response indicates low confidence (contains "I'm not sure" or "escalate to owner" etc), return None to signal owner escalation needed
           - Otherwise return response text
         - `def make_sync_callbacks(self) -> dict`:
           - Follow AlertsCog pattern: `run_coroutine_threadsafe` bridge
           - Return `{"on_pm_escalation": sync_wrapper}` for PM tier to call from sync/thread context
         - `@property def decision_logger(self) -> DecisionLogger | None`: expose for other cogs

    2. Keep `async def setup(bot)` at module level.

    3. Write tests in `tests/test_strategist_cog.py` FIRST (RED), then implement (GREEN).
       - Mock StrategistConversation, discord channels, message objects
       - Test message filtering (webhook, bot, non-owner, wrong channel all filtered)
       - Test streaming rate limit
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_strategist_cog.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class StrategistCog" src/vcompany/bot/cogs/strategist.py` matches
    - `grep "on_message" src/vcompany/bot/cogs/strategist.py` matches the listener
    - `grep "webhook_id" src/vcompany/bot/cogs/strategist.py` matches filter per Research Open Question 4
    - `grep "vco-owner" src/vcompany/bot/cogs/strategist.py` matches role check
    - `grep "stream_to_channel\|_stream_to" src/vcompany/bot/cogs/strategist.py` matches streaming method
    - `grep "handle_pm_escalation" src/vcompany/bot/cogs/strategist.py` matches escalation handler
    - `grep "make_sync_callbacks" src/vcompany/bot/cogs/strategist.py` matches callback bridge
    - `grep "Thinking" src/vcompany/bot/cogs/strategist.py` matches placeholder message
    - `uv run pytest tests/test_strategist_cog.py -x -v` passes all tests
  </acceptance_criteria>
  <done>StrategistCog bridges #strategist to persistent conversation. Owner messages forwarded, responses streamed with rate limiting. PM escalation supported via callback. Tests pass.</done>
</task>

</tasks>

<verification>
cd /home/developer/vcompany && uv run pytest tests/test_decision_log.py tests/test_strategist_cog.py -x -v
</verification>

<success_criteria>
- StrategistCog expanded from placeholder to full persistent conversation bridge (D-11)
- Owner messages filtered and forwarded to Strategist (Research Open Question 4)
- Streaming responses with 1/sec rate limit and >2000 char handling (Pitfall 1)
- PM escalation pathway via make_sync_callbacks (D-05)
- Decision logging to #decisions + decisions.jsonl (D-18/STRAT-09)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-pm-strategist-and-milestones/06-04-SUMMARY.md`
</output>
