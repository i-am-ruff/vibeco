---
phase: 06-pm-strategist-and-milestones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/strategist/__init__.py
  - src/vcompany/strategist/models.py
  - src/vcompany/strategist/confidence.py
  - src/vcompany/strategist/context_builder.py
  - tests/test_confidence.py
  - tests/test_context_builder.py
autonomous: true
requirements:
  - STRAT-01
  - STRAT-08
  - MILE-02
  - MILE-03

must_haves:
  truths:
    - "PM-CONTEXT.md can be generated from project documents (blueprint, interfaces, scope, status, decisions)"
    - "Confidence scorer returns HIGH/MEDIUM/LOW based on context coverage and prior decision match"
    - "Confidence thresholds are >90% HIGH, 60-90% MEDIUM, <60% LOW per D-06/D-09"
    - "Three input documents (blueprint, interfaces, milestone-scope) are assembled into PM context per MILE-02"
  artifacts:
    - path: "src/vcompany/strategist/models.py"
      provides: "PMDecision, ConfidenceResult, DecisionLogEntry, KnowledgeTransferDoc Pydantic models"
      contains: "class PMDecision"
    - path: "src/vcompany/strategist/confidence.py"
      provides: "Heuristic confidence scorer with context coverage + prior decision match"
      exports: ["ConfidenceScorer"]
    - path: "src/vcompany/strategist/context_builder.py"
      provides: "PM-CONTEXT.md assembly from project documents"
      exports: ["build_pm_context"]
    - path: "tests/test_confidence.py"
      provides: "Tests for confidence scoring"
    - path: "tests/test_context_builder.py"
      provides: "Tests for PM-CONTEXT.md generation"
  key_links:
    - from: "src/vcompany/strategist/confidence.py"
      to: "src/vcompany/strategist/models.py"
      via: "imports ConfidenceResult"
      pattern: "from vcompany\\.strategist\\.models import ConfidenceResult"
    - from: "src/vcompany/strategist/context_builder.py"
      to: "project context/ directory"
      via: "reads PROJECT-BLUEPRINT.md, INTERFACES.md, MILESTONE-SCOPE.md, PROJECT-STATUS.md"
      pattern: "CONTEXT_SOURCES"
---

<objective>
Create the PM tier foundation: data models, heuristic confidence scorer, and PM-CONTEXT.md generator.

Purpose: These are the building blocks that all PM operations depend on. The confidence scorer implements D-08 (deterministic heuristic, not AI self-assessed). The context builder implements STRAT-01/MILE-03 (PM-CONTEXT.md assembly from project docs per D-20).
Output: New `src/vcompany/strategist/` package with models, confidence scorer, and context builder -- all fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
@.planning/phases/06-pm-strategist-and-milestones/06-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->

From src/vcompany/bot/config.py:
```python
from pydantic_settings import BaseSettings
class BotConfig(BaseSettings):
    discord_bot_token: str
    discord_guild_id: int
    project_dir: str = "."
    model_config = {"env_prefix": "", "env_file": ".env", "env_file_encoding": "utf-8"}
```

From src/vcompany/shared/file_ops.py:
```python
def write_atomic(path: Path, content: str) -> None: ...
```

From src/vcompany/models/config.py:
```python
class ProjectConfig(BaseModel): ...
class AgentConfig(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategist package + Pydantic models + confidence scorer</name>
  <files>
    src/vcompany/strategist/__init__.py,
    src/vcompany/strategist/models.py,
    src/vcompany/strategist/confidence.py,
    tests/test_confidence.py
  </files>
  <read_first>
    src/vcompany/models/config.py,
    src/vcompany/models/agent_state.py,
    src/vcompany/bot/config.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md,
    .planning/phases/06-pm-strategist-and-milestones/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test: ConfidenceScorer.score() returns ConfidenceResult with level="HIGH" when score > 0.9
    - Test: ConfidenceScorer.score() returns level="MEDIUM" when 0.6 <= score <= 0.9
    - Test: ConfidenceScorer.score() returns level="LOW" when score < 0.6
    - Test: Context coverage scoring finds keyword matches in blueprint/interfaces/scope docs
    - Test: Prior decision match returns 1.0 for exact question match, 0.5 for partial, 0.0 for no match
    - Test: Weighted combination is 60% coverage + 40% prior match per Research Pattern 4
    - Test: PMDecision model serializes to JSON with model_dump_json()
    - Test: ConfidenceResult model has score, level, coverage, prior_match fields
    - Test: DecisionLogEntry model has timestamp, question_or_plan, decision, confidence_level, decided_by fields
  </behavior>
  <action>
    1. Create `src/vcompany/strategist/__init__.py` (empty).

    2. Create `src/vcompany/strategist/models.py` with Pydantic models per D-06/D-09:
       - `ConfidenceResult(BaseModel)`: score (float 0-1), level (Literal["HIGH","MEDIUM","LOW"]), coverage (float), prior_match (float)
       - `PMDecision(BaseModel)`: answer (str | None), confidence (ConfidenceResult), decided_by (Literal["PM","Strategist","Owner"]), note (str = ""), escalate_to (str | None = None)
       - `DecisionLogEntry(BaseModel)`: timestamp (datetime), question_or_plan (str), decision (str), confidence_level (str), decided_by (str), agent_id (str)
       - `KnowledgeTransferDoc(BaseModel)`: generated_at (datetime), token_count (int), decisions_summary (str), personality_notes (str), project_state (str), open_threads (list[str])

    3. Create `src/vcompany/strategist/confidence.py` with `ConfidenceScorer` class per D-08:
       - `__init__(self)`: no state needed (deterministic)
       - `score(self, question: str, context_docs: dict[str, str], decision_log: list[DecisionLogEntry]) -> ConfidenceResult`:
         - `_check_context_coverage(question, context_docs) -> float`: tokenize question into keywords (split on whitespace, lowercase, filter stopwords), count how many appear in any context doc. Return fraction found (0.0 to 1.0). Context docs keyed by name: "blueprint", "interfaces", "scope", "status".
         - `_check_prior_decisions(question, decision_log) -> float`: for each decision log entry, compare question similarity. Use simple word overlap ratio (Jaccard similarity on word sets). Return max similarity found (1.0 for exact match, 0.0 for no overlap).
         - Weighted: `raw_score = coverage * 0.6 + prior_match * 0.4`
         - Thresholds per D-06/D-09: raw_score > 0.9 = HIGH, >= 0.6 = MEDIUM, < 0.6 = LOW

    4. Write tests in `tests/test_confidence.py` FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_confidence.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ConfidenceResult" src/vcompany/strategist/models.py` returns 1
    - `grep -c "class PMDecision" src/vcompany/strategist/models.py` returns 1
    - `grep -c "class DecisionLogEntry" src/vcompany/strategist/models.py` returns 1
    - `grep -c "class KnowledgeTransferDoc" src/vcompany/strategist/models.py` returns 1
    - `grep -c "class ConfidenceScorer" src/vcompany/strategist/confidence.py` returns 1
    - `grep "0.6.*0.4" src/vcompany/strategist/confidence.py` matches the weighted combination
    - `grep '"HIGH"' src/vcompany/strategist/confidence.py` confirms threshold usage
    - `uv run pytest tests/test_confidence.py -x -v` passes all tests
  </acceptance_criteria>
  <done>ConfidenceScorer returns correct HIGH/MEDIUM/LOW levels based on heuristic scoring. All Pydantic models serialize correctly. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PM-CONTEXT.md builder + tests</name>
  <files>
    src/vcompany/strategist/context_builder.py,
    tests/test_context_builder.py
  </files>
  <read_first>
    src/vcompany/strategist/models.py,
    src/vcompany/coordination/sync_context.py,
    src/vcompany/shared/file_ops.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test: build_pm_context assembles sections from PROJECT-BLUEPRINT.md, INTERFACES.md, MILESTONE-SCOPE.md, PROJECT-STATUS.md
    - Test: Missing source files are gracefully skipped (no crash)
    - Test: Recent decisions from state/decisions.jsonl are appended to context
    - Test: Output starts with "# PM Context" header
    - Test: Each source file gets its own markdown section header
    - Test: Empty project dir (no context files) returns minimal valid PM context
    - Test: MILE-02 three input documents all appear when present
  </behavior>
  <action>
    1. Create `src/vcompany/strategist/context_builder.py` per D-20 and Research Pattern 5:
       - Constant `CONTEXT_SOURCES`: list of tuples `(filename, section_header)`:
         - ("PROJECT-BLUEPRINT.md", "## Project Blueprint")
         - ("INTERFACES.md", "## Interface Contracts")
         - ("MILESTONE-SCOPE.md", "## Current Milestone Scope")
         - ("PROJECT-STATUS.md", "## Project Status")
       - `build_pm_context(project_dir: Path) -> str`:
         - Start with `"# PM Context -- auto-generated, do not edit\n"`
         - For each source in CONTEXT_SOURCES, read from `project_dir / "context" / filename`. If exists, append `\n{header}\n\n{content}\n`. If missing, skip.
         - Read `project_dir / "state" / "decisions.jsonl"` if it exists. Parse each line as JSON. Append section `"## Recent Decisions\n\n"` with formatted entries (last 50 max): `"- [{timestamp}] {decided_by}: {decision} (confidence: {confidence_level})\n"`
         - Return assembled string.
       - `write_pm_context(project_dir: Path) -> Path`:
         - Calls `build_pm_context`, writes result to `project_dir / "context" / "PM-CONTEXT.md"` using `write_atomic`.
         - Returns the written path.

    2. Write tests in `tests/test_context_builder.py` FIRST (RED), then implement (GREEN). Use `tmp_path` fixture for isolated filesystem.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_context_builder.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_pm_context" src/vcompany/strategist/context_builder.py` returns 1
    - `grep -c "def write_pm_context" src/vcompany/strategist/context_builder.py` returns 1
    - `grep "CONTEXT_SOURCES" src/vcompany/strategist/context_builder.py` matches the constant
    - `grep "PM Context" src/vcompany/strategist/context_builder.py` matches the header
    - `grep "decisions.jsonl" src/vcompany/strategist/context_builder.py` matches the decisions file
    - `uv run pytest tests/test_context_builder.py -x -v` passes all tests
  </acceptance_criteria>
  <done>PM-CONTEXT.md can be generated from project documents. Missing files are gracefully handled. Three input documents (MILE-02) all included when present. Tests pass.</done>
</task>

</tasks>

<verification>
cd /home/developer/vcompany && uv run pytest tests/test_confidence.py tests/test_context_builder.py -x -v
</verification>

<success_criteria>
- New `src/vcompany/strategist/` package exists with __init__.py, models.py, confidence.py, context_builder.py
- ConfidenceScorer implements deterministic heuristic scoring per D-08 (not AI self-assessed)
- Thresholds match D-06/D-09: >90% HIGH, 60-90% MEDIUM, <60% LOW
- PM-CONTEXT.md assembles from blueprint+interfaces+scope+status+decisions per D-20/MILE-03
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-pm-strategist-and-milestones/06-01-SUMMARY.md`
</output>
