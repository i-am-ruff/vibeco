---
phase: 34-cleanup-and-network-stub
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - src/vcompany/agent/ (DELETE entire directory)
  - src/vcompany/handler/ (DELETE entire directory)
  - src/vcompany/container/ (DELETE entire directory)
  - src/vcompany/transport/protocol.py (DELETE)
  - src/vcompany/transport/local.py (DELETE)
  - src/vcompany/transport/docker.py (DELETE)
  - src/vcompany/transport/__init__.py
  - src/vcompany/daemon/runtime_api.py
  - src/vcompany/daemon/daemon.py
  - src/vcompany/supervisor/company_root.py
  - src/vcompany/supervisor/supervisor.py
  - src/vcompany/bot/cogs/mention_router.py
  - tests/ (DELETE dead test files, update surviving tests)
autonomous: true
requirements:
  - HEAD-04
must_haves:
  truths:
    - "No agent/, handler/, or container/ directories exist under src/vcompany/"
    - "No transport/protocol.py, transport/local.py, or transport/docker.py exist"
    - "No isinstance(x, AgentContainer) checks remain in live code"
    - "RuntimeAPI methods have no legacy else-branches for container path"
    - "MentionRouter has no legacy AgentContainer path"
    - "Dead test files are deleted"
    - "python -c 'import vcompany' succeeds"
  artifacts:
    - path: "src/vcompany/transport/__init__.py"
      provides: "Clean exports: ChannelTransport, NativeTransport, DockerChannelTransport only"
      contains: "ChannelTransport"
  key_links:
    - from: "src/vcompany/daemon/runtime_api.py"
      to: "src/vcompany/daemon/agent_handle.py"
      via: "All agents are AgentHandle"
      pattern: "AgentHandle"
    - from: "src/vcompany/bot/cogs/mention_router.py"
      to: "src/vcompany/daemon/agent_handle.py"
      via: "No isinstance check needed"
      pattern: "agent.send"
---

<objective>
Delete all dead code: agent/, handler/, container/ directories, old transport files, isinstance branches, and dead tests.

Purpose: Complete HEAD-04 by removing all daemon-side container dead code that was superseded by vco-worker and the ChannelTransport architecture in Phases 29-33. After Plan 01 migrated live types, everything remaining in these directories is dead code.

Output: Clean codebase with no dead container/agent/handler code. All isinstance dispatch branches simplified. Dead tests removed, surviving tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup-and-network-stub/34-RESEARCH.md
@.planning/phases/34-cleanup-and-network-stub/34-01-SUMMARY.md

@src/vcompany/transport/__init__.py
@src/vcompany/daemon/runtime_api.py
@src/vcompany/daemon/daemon.py
@src/vcompany/supervisor/company_root.py
@src/vcompany/supervisor/supervisor.py
@src/vcompany/bot/cogs/mention_router.py

<interfaces>
<!-- After Plan 01, these are the new import locations for migrated types -->
From src/vcompany/supervisor/health.py (migrated):
```python
class HealthReport: ...
class HealthNode: ...
class HealthTree: ...
class CompanyHealthTree: ...
```

From src/vcompany/supervisor/child_spec.py (migrated):
```python
class ChildSpec: ...
class ChildSpecRegistry: ...
class RestartPolicy: ...
```

From src/vcompany/shared/memory_store.py (migrated):
```python
class MemoryStore: ...
```

From src/vcompany/models/agent_types.py (migrated):
```python
def set_agent_types_config(config: AgentTypesConfig) -> None: ...
def get_agent_types_config() -> AgentTypesConfig | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead directories and files, clean isinstance branches</name>
  <files>
    src/vcompany/agent/ (DELETE),
    src/vcompany/handler/ (DELETE),
    src/vcompany/container/ (DELETE),
    src/vcompany/transport/protocol.py (DELETE),
    src/vcompany/transport/local.py (DELETE),
    src/vcompany/transport/docker.py (DELETE),
    src/vcompany/transport/__init__.py,
    src/vcompany/daemon/runtime_api.py,
    src/vcompany/daemon/daemon.py,
    src/vcompany/supervisor/company_root.py,
    src/vcompany/supervisor/supervisor.py,
    src/vcompany/bot/cogs/mention_router.py
  </files>
  <read_first>
    src/vcompany/daemon/runtime_api.py,
    src/vcompany/daemon/daemon.py,
    src/vcompany/supervisor/company_root.py,
    src/vcompany/supervisor/supervisor.py,
    src/vcompany/bot/cogs/mention_router.py,
    src/vcompany/transport/__init__.py
  </read_first>
  <action>
    **Step 1: Delete entire dead directories:**
    ```
    rm -rf src/vcompany/agent/
    rm -rf src/vcompany/handler/
    rm -rf src/vcompany/container/
    ```

    **Step 2: Delete dead transport files:**
    ```
    rm src/vcompany/transport/protocol.py
    rm src/vcompany/transport/local.py
    rm src/vcompany/transport/docker.py
    ```

    **Step 3: Rewrite `src/vcompany/transport/__init__.py`:**
    Remove all imports of deleted modules. New content:
    ```python
    """Transport abstraction layer for agent execution environments."""

    from vcompany.transport.channel_transport import ChannelTransport
    from vcompany.transport.docker_channel import DockerChannelTransport
    from vcompany.transport.native import NativeTransport

    __all__ = [
        "ChannelTransport",
        "DockerChannelTransport",
        "NativeTransport",
    ]
    ```

    **Step 4: Clean isinstance branches in RuntimeAPI (`src/vcompany/daemon/runtime_api.py`):**

    Find every `if isinstance(handle, AgentHandle):` block. In each case:
    - Keep ONLY the AgentHandle branch (the `if` body)
    - Delete the `else:` branch entirely (legacy container path)
    - Remove the `isinstance` check itself -- just execute the AgentHandle code directly
    - This affects methods: `dispatch()`, `kill()`, `relaunch()`, `give_task()`, `relay_message()`, and the health/status methods

    Remove the `create_strategist()` method's container path if Plan 01 didn't fully remove it. The new path should create StrategistConversation directly with no ChildSpec/ContainerContext.

    Remove `new_project()`'s container-path code: the ChildSpec creation using ContainerContext, the `add_project()` call that creates containers. If `new_project()` is entirely dead (all agents now hired via `vco hire`), consider whether to keep it or remove it. Check if it's called anywhere -- if only from daemon startup for backward compat, it may be dead code too.

    Remove any remaining imports of deleted modules: `AgentContainer`, `ContainerContext`, `create_container`, etc.

    **Step 5: Clean isinstance branches in CompanyRoot (`src/vcompany/supervisor/company_root.py`):**

    - In `stop()`: Remove the isinstance check for AgentHandle vs AgentContainer. All company agents are AgentHandle. Keep only the AgentHandle stop path.
    - Remove `add_company_agent()` method if it only served the old container path (check callers).
    - Remove `_find_container()` method if it exists (dead code from old transport path).
    - Remove `register_defaults()` call from `__init__` or `start()` if present.
    - Remove any remaining imports from deleted modules.

    **Step 6: Clean isinstance branches in Supervisor (`src/vcompany/supervisor/supervisor.py`):**

    - Remove `create_container` import from (now-deleted) `vcompany.container.factory`
    - Remove `AgentContainer` import
    - Remove `ContainerContext` import
    - Remove any container creation code in `start_child()` or similar methods
    - If Supervisor still needs to exist for ProjectSupervisor, keep the class but strip container-specific code. If Supervisor is fully dead (all agents use hire()), delete it.

    **Step 7: Clean MentionRouter (`src/vcompany/bot/cogs/mention_router.py`):**

    - In `_deliver_to_agent()`: Remove the isinstance check. All agents are AgentHandle. Keep only the `agent.send(InboundMessage(...))` path.
    - Remove the `else:` branch with `receive_discord_message()` call.
    - Remove the `from vcompany.container.container import AgentContainer` import.

    **Step 8: Clean daemon.py:**

    - In `_handle_send_file()`: Remove the dead `_find_container` code block that tries old transport path for file resolution. Keep only the AgentHandle path.
    - Remove any remaining container/ imports.

    **Step 9: Verify compilation:**
    Run `python -c "import vcompany"` and `python -c "from vcompany.daemon.runtime_api import RuntimeAPI"` to confirm no import errors.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -c "from vcompany.daemon.runtime_api import RuntimeAPI; from vcompany.supervisor.company_root import CompanyRoot; from vcompany.bot.cogs.mention_router import MentionRouterCog; print('OK')" && test ! -d src/vcompany/agent && test ! -d src/vcompany/handler && test ! -d src/vcompany/container && test ! -f src/vcompany/transport/protocol.py && test ! -f src/vcompany/transport/local.py && test ! -f src/vcompany/transport/docker.py</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/vcompany/agent/` does not exist
    - Directory `src/vcompany/handler/` does not exist
    - Directory `src/vcompany/container/` does not exist
    - File `src/vcompany/transport/protocol.py` does not exist
    - File `src/vcompany/transport/local.py` does not exist
    - File `src/vcompany/transport/docker.py` does not exist
    - `grep "AgentContainer" src/vcompany/transport/__init__.py` returns zero matches
    - `grep "AgentTransport" src/vcompany/transport/__init__.py` returns zero matches
    - `grep "LocalTransport" src/vcompany/transport/__init__.py` returns zero matches
    - `grep "isinstance.*AgentHandle" src/vcompany/daemon/runtime_api.py` returns zero matches (isinstance checks removed, code assumes AgentHandle)
    - `grep "isinstance.*AgentHandle" src/vcompany/bot/cogs/mention_router.py` returns zero matches
    - `python -c "from vcompany.daemon.runtime_api import RuntimeAPI"` exits 0
    - `python -c "from vcompany.supervisor.company_root import CompanyRoot"` exits 0
  </acceptance_criteria>
  <done>All dead code directories and files deleted. isinstance branches cleaned. Live code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead tests, update surviving test imports</name>
  <files>
    tests/
  </files>
  <read_first>
    tests/conftest.py
  </read_first>
  <action>
    **Step 1: Delete dead test files** (tests for deleted modules):
    ```
    rm tests/test_gsd_agent.py
    rm tests/test_fulltime_agent.py
    rm tests/test_company_agent.py
    rm tests/test_continuous_agent.py
    rm tests/test_continuous_lifecycle.py
    rm tests/test_gsd_lifecycle.py
    rm tests/test_event_driven_lifecycle.py
    rm tests/test_container_lifecycle.py
    rm tests/test_container_stopping.py
    rm tests/test_container_blocked.py
    rm tests/test_container_integration.py
    rm tests/test_container_tmux_bridge.py
    rm tests/test_container_factory.py
    rm tests/test_container_context.py
    rm tests/test_communication_port.py
    rm tests/test_discord_comm_port.py
    rm tests/test_comm_port_wiring.py
    rm tests/test_conversation.py
    ```
    Only delete files that exist. Some may not exist -- skip those silently.

    **Step 2: Update surviving test imports.**

    For each surviving test file, grep for `from vcompany.container` and update:

    - `tests/test_child_spec.py`: change `from vcompany.container.child_spec import` to `from vcompany.supervisor.child_spec import`
    - `tests/test_memory_store.py`: change `from vcompany.container.memory_store import` to `from vcompany.shared.memory_store import`
    - `tests/test_container_health.py` or `tests/test_health_tree.py`: change `from vcompany.container.health import` to `from vcompany.supervisor.health import`. If test_container_health.py tests ONLY container-specific behavior (not HealthReport itself), delete it.
    - `tests/test_health_cog.py`: change health imports to `from vcompany.supervisor.health import`
    - `tests/test_backlog.py`: change `from vcompany.container.memory_store import` to `from vcompany.shared.memory_store import`
    - `tests/test_project_state.py`: change `from vcompany.container.memory_store import` to `from vcompany.shared.memory_store import`
    - `tests/test_scheduler.py`: change `from vcompany.container.memory_store import` or `from vcompany.container import` to new locations
    - `tests/test_company_root.py`: update all container imports to new locations. Remove any test cases that test the old container path (add_company_agent with container creation). Keep tests for hire(), health_tree(), etc.
    - `tests/test_supervision_tree.py`: update imports from container to new locations
    - `tests/test_bulk_failure.py`: update imports
    - `tests/test_restart_strategies.py`: update imports
    - `tests/test_degraded_mode.py`: update imports
    - `tests/test_delegation.py`: update imports
    - `tests/test_bot_client.py`: remove GsdAgent/container references
    - `tests/test_runtime_api.py`: update imports, remove test cases that test the legacy container path
    - `tests/conftest.py`: remove any fixtures that create AgentContainer/GsdAgent/CompanyAgent instances. Update HealthReport/ChildSpec/MemoryStore imports.

    For each test file, also check for references to `AgentTransport`, `LocalTransport`, `DockerTransport`, `NoopTransport` and remove/update those.

    **Step 3: Verify test collection:**
    Run `python -m pytest --collect-only 2>&1 | tail -5` to confirm pytest can discover tests without import errors. Some tests may fail (mocking changes, etc.) but collection must succeed.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && python -m pytest --collect-only 2>&1 | grep -c "error" | xargs test 0 -eq || python -m pytest --collect-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f tests/test_gsd_agent.py` succeeds (file deleted)
    - `test ! -f tests/test_container_factory.py` succeeds (file deleted)
    - `test ! -f tests/test_container_lifecycle.py` succeeds (file deleted)
    - `grep -r "from vcompany.container" tests/ --include="*.py"` returns zero matches
    - `grep -r "from vcompany.agent" tests/ --include="*.py"` returns zero matches
    - `grep -r "from vcompany.handler" tests/ --include="*.py"` returns zero matches
    - `python -m pytest --collect-only` exits 0 (no collection errors)
  </acceptance_criteria>
  <done>Dead test files deleted. All surviving test imports updated to new module locations. Test collection succeeds with no import errors.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `find src/vcompany/agent src/vcompany/handler src/vcompany/container -type f 2>&1` returns "No such file or directory" for all three
2. `python -c "import vcompany; from vcompany.daemon.runtime_api import RuntimeAPI; from vcompany.supervisor.company_root import CompanyRoot"` succeeds
3. `grep -r "from vcompany.container" src/vcompany/ tests/ --include="*.py"` returns zero matches
4. `grep -r "from vcompany.agent" src/vcompany/ tests/ --include="*.py"` returns zero matches (except maybe agent_handle which is different)
5. `grep -r "AgentTransport\b" src/vcompany/ --include="*.py"` returns zero matches
6. `python -m pytest --collect-only` exits 0
</verification>

<success_criteria>
All dead code removed. No imports reference deleted modules. isinstance dispatch branches simplified to assume AgentHandle only. Test suite collects without import errors. HEAD-04 requirement fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup-and-network-stub/34-02-SUMMARY.md`
</output>
