---
phase: 01-container-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/container/__init__.py
  - src/vcompany/container/state_machine.py
  - src/vcompany/container/context.py
  - src/vcompany/container/health.py
  - src/vcompany/container/communication.py
  - tests/test_container_lifecycle.py
  - tests/test_container_context.py
  - tests/test_container_health.py
  - tests/test_communication_port.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-06
  - HLTH-01

must_haves:
  truths:
    - "ContainerLifecycle transitions through CREATING -> RUNNING -> SLEEPING -> ERRORED -> STOPPED -> DESTROYED with all valid paths"
    - "Invalid transitions (e.g., STOPPED -> RUNNING, DESTROYED -> anything) raise TransitionNotAllowed"
    - "ContainerContext holds agent_id, agent_type, parent_id, project_id, owned_dirs, gsd_mode, system_prompt and validates on construction"
    - "HealthReport contains state, inner_state, uptime, last_heartbeat, error_count, last_activity fields"
    - "CommunicationPort is an async Protocol with send_message and receive_message — no file IPC, no in-memory callbacks"
  artifacts:
    - path: "src/vcompany/container/state_machine.py"
      provides: "ContainerLifecycle FSM using python-statemachine"
      exports: ["ContainerLifecycle"]
    - path: "src/vcompany/container/context.py"
      provides: "ContainerContext Pydantic model"
      exports: ["ContainerContext"]
    - path: "src/vcompany/container/health.py"
      provides: "HealthReport Pydantic model"
      exports: ["HealthReport"]
    - path: "src/vcompany/container/communication.py"
      provides: "CommunicationPort Protocol and Message dataclass"
      exports: ["CommunicationPort", "Message"]
    - path: "tests/test_container_lifecycle.py"
      provides: "Tests for CONT-01 and CONT-02"
      min_lines: 50
    - path: "tests/test_container_context.py"
      provides: "Tests for CONT-03"
      min_lines: 20
    - path: "tests/test_container_health.py"
      provides: "Tests for HLTH-01"
      min_lines: 20
    - path: "tests/test_communication_port.py"
      provides: "Tests for CONT-06"
      min_lines: 20
  key_links:
    - from: "src/vcompany/container/state_machine.py"
      to: "python-statemachine"
      via: "from statemachine import StateMachine, State"
      pattern: "class ContainerLifecycle\\(StateMachine\\)"
    - from: "src/vcompany/container/__init__.py"
      to: "all container modules"
      via: "re-exports"
      pattern: "from vcompany\\.container\\.(state_machine|context|health|communication) import"
---

<objective>
Install new dependencies (python-statemachine, aiosqlite) and build the core type contracts and lifecycle state machine for the container system.

Purpose: Establishes all type interfaces that Plan 02 (MemoryStore, ChildSpec) and Plan 03 (AgentContainer wiring) depend on, plus the FSM that is the heart of the container lifecycle.
Output: 4 production modules (state_machine, context, health, communication) + 4 test files + package __init__.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-container-foundation/01-RESEARCH.md
@src/vcompany/models/agent_state.py
@src/vcompany/models/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create state machine + data models</name>
  <files>
    src/vcompany/container/__init__.py,
    src/vcompany/container/state_machine.py,
    src/vcompany/container/context.py,
    src/vcompany/container/health.py,
    src/vcompany/container/communication.py,
    tests/test_container_lifecycle.py,
    tests/test_container_context.py,
    tests/test_container_health.py,
    tests/test_communication_port.py
  </files>
  <read_first>
    src/vcompany/models/config.py (Pydantic model patterns — field_validator, model_validator, type hints),
    src/vcompany/models/agent_state.py (existing AgentEntry status enum pattern),
    pyproject.toml (current dependencies list),
    .planning/phases/01-container-foundation/01-RESEARCH.md (architecture patterns, pitfalls)
  </read_first>
  <behavior>
    State Machine (CONT-01, CONT-02):
    - Test: ContainerLifecycle starts in "creating" state
    - Test: creating -> running via start() succeeds
    - Test: running -> sleeping via sleep() succeeds
    - Test: sleeping -> running via wake() succeeds
    - Test: running -> errored via error() succeeds
    - Test: creating -> errored via error() succeeds
    - Test: sleeping -> errored via error() succeeds
    - Test: errored -> running via recover() succeeds
    - Test: running -> stopped via stop() succeeds
    - Test: sleeping -> stopped via stop() succeeds
    - Test: errored -> stopped via stop() succeeds
    - Test: stopped -> destroyed via destroy() succeeds
    - Test: errored -> destroyed via destroy() succeeds
    - Test: stopped -> running raises TransitionNotAllowed
    - Test: destroyed -> any state raises TransitionNotAllowed
    - Test: creating -> sleeping raises TransitionNotAllowed
    - Test: creating -> stopped raises TransitionNotAllowed
    - Test: running -> creating raises TransitionNotAllowed
    - Test: send_event("start") from creating transitions to running (string-based dispatch)

    ContainerContext (CONT-03):
    - Test: ContainerContext accepts all required fields (agent_id="agent-1", agent_type="gsd", parent_id="proj-1", project_id="myproject", owned_dirs=["src/api"], gsd_mode="full", system_prompt="You are an agent")
    - Test: ContainerContext defaults — parent_id=None, project_id=None, owned_dirs=[], gsd_mode="full", system_prompt=""
    - Test: ContainerContext is serializable to dict via model_dump()

    HealthReport (HLTH-01):
    - Test: HealthReport accepts state="running", inner_state=None, uptime=120.5, last_heartbeat=datetime, error_count=0, last_activity=datetime, agent_id="agent-1"
    - Test: HealthReport inner_state defaults to None
    - Test: HealthReport error_count defaults to 0
    - Test: HealthReport is serializable to dict via model_dump()

    CommunicationPort (CONT-06):
    - Test: CommunicationPort is a Protocol (runtime_checkable)
    - Test: A class implementing send_message(target, content) -> bool and receive_message() -> Message | None satisfies the protocol
    - Test: Message dataclass has source, target, content, timestamp fields
    - Test: A class missing send_message does NOT satisfy the protocol
  </behavior>
  <action>
    1. Install dependencies:
       ```
       uv add python-statemachine aiosqlite
       ```
       This adds both to pyproject.toml. aiosqlite is used in Plan 02 but install now to avoid partial installs.

    2. Create `src/vcompany/container/` directory with `__init__.py`.

    3. Create `src/vcompany/container/state_machine.py` — ContainerLifecycle(StateMachine):
       - 6 states: creating (initial=True), running, sleeping, errored, stopped, destroyed (final=True)
       - Transitions per CONT-01:
         - start = creating.to(running)
         - sleep = running.to(sleeping)
         - wake = sleeping.to(running)
         - error = creating.to(errored) | running.to(errored) | sleeping.to(errored)
         - recover = errored.to(running)
         - stop = running.to(stopped) | sleeping.to(stopped) | errored.to(stopped)
         - destroy = stopped.to(destroyed) | errored.to(destroyed)
       - Add `after_transition(self, event, state)` callback that calls `self.model._on_state_change()` if `self.model` has that method (for health report emission in Plan 03).
       - Add `send_event(name: str)` method that delegates to `self.send(name)` for string-based dispatch (Phase 2 supervisors will use this).
       - Import: `from statemachine import StateMachine, State`

    4. Create `src/vcompany/container/context.py` — ContainerContext(BaseModel):
       - Fields per CONT-03: agent_id: str, agent_type: str (one of "gsd", "continuous", "fulltime", "company"), parent_id: str | None = None, project_id: str | None = None, owned_dirs: list[str] = [], gsd_mode: str = "full", system_prompt: str = ""
       - Follow existing Pydantic patterns from models/config.py

    5. Create `src/vcompany/container/health.py` — HealthReport(BaseModel):
       - Fields per HLTH-01: agent_id: str, state: str, inner_state: str | None = None, uptime: float, last_heartbeat: datetime, error_count: int = 0, last_activity: datetime
       - Use `from datetime import datetime` (timezone handling is caller's responsibility)

    6. Create `src/vcompany/container/communication.py`:
       - Message dataclass with fields: source: str, target: str, content: str, timestamp: datetime
       - CommunicationPort as a typing.Protocol (runtime_checkable=True):
         - async def send_message(self, target: str, content: str) -> bool: ...
         - async def receive_message(self) -> Message | None: ...
       - This is interface-only per CONT-06 — no implementation in this phase.

    7. Create `src/vcompany/container/__init__.py` with re-exports:
       ```python
       from vcompany.container.state_machine import ContainerLifecycle
       from vcompany.container.context import ContainerContext
       from vcompany.container.health import HealthReport
       from vcompany.container.communication import CommunicationPort, Message
       ```

    8. Write all 4 test files per the behavior specifications above. Use `from statemachine.exceptions import TransitionNotAllowed` for invalid transition tests. Use pytest-asyncio for any async tests. Follow existing test patterns in tests/ directory.
  </action>
  <verify>
    <automated>uv run pytest tests/test_container_lifecycle.py tests/test_container_context.py tests/test_container_health.py tests/test_communication_port.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains "python-statemachine" and "aiosqlite" in dependencies
    - src/vcompany/container/__init__.py contains "from vcompany.container.state_machine import ContainerLifecycle"
    - src/vcompany/container/__init__.py contains "from vcompany.container.context import ContainerContext"
    - src/vcompany/container/__init__.py contains "from vcompany.container.health import HealthReport"
    - src/vcompany/container/__init__.py contains "from vcompany.container.communication import CommunicationPort"
    - src/vcompany/container/state_machine.py contains "class ContainerLifecycle(StateMachine)"
    - src/vcompany/container/state_machine.py contains "creating = State(initial=True)"
    - src/vcompany/container/state_machine.py contains "destroyed = State(final=True)"
    - src/vcompany/container/state_machine.py contains "def send_event"
    - src/vcompany/container/context.py contains "class ContainerContext(BaseModel)"
    - src/vcompany/container/context.py contains "agent_id: str"
    - src/vcompany/container/context.py contains "agent_type: str"
    - src/vcompany/container/context.py contains "gsd_mode: str"
    - src/vcompany/container/health.py contains "class HealthReport(BaseModel)"
    - src/vcompany/container/health.py contains "inner_state: str | None"
    - src/vcompany/container/health.py contains "error_count: int"
    - src/vcompany/container/communication.py contains "class CommunicationPort(Protocol)"
    - src/vcompany/container/communication.py contains "class Message"
    - src/vcompany/container/communication.py contains "async def send_message"
    - tests/test_container_lifecycle.py contains "TransitionNotAllowed"
    - tests/test_container_lifecycle.py contains "test_invalid_transitions" or "test_stopped_to_running"
    - All 4 test files pass: uv run pytest tests/test_container_lifecycle.py tests/test_container_context.py tests/test_container_health.py tests/test_communication_port.py -x exits 0
  </acceptance_criteria>
  <done>
    ContainerLifecycle FSM transitions through all 6 states correctly, invalid transitions raise TransitionNotAllowed, ContainerContext/HealthReport/CommunicationPort models validated by tests, all 4 test files green.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_container_lifecycle.py tests/test_container_context.py tests/test_container_health.py tests/test_communication_port.py -x` all pass
- `python -c "from vcompany.container import ContainerLifecycle, ContainerContext, HealthReport, CommunicationPort, Message"` succeeds
- python-statemachine and aiosqlite appear in pyproject.toml dependencies
</verification>

<success_criteria>
- All 6 lifecycle states defined and reachable via declared transitions
- Invalid transitions raise TransitionNotAllowed (CONT-02)
- ContainerContext holds all 7 required fields (CONT-03)
- HealthReport holds all 6 required fields (HLTH-01)
- CommunicationPort is async Protocol with no file IPC (CONT-06)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-container-foundation/01-01-SUMMARY.md`
</output>
