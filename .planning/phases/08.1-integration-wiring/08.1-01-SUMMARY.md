---
phase: 08.1-integration-wiring
plan: 01
subsystem: bot
tags: [discord, health, resilience, message-queue, degraded-mode, wiring]

requires:
  - phase: 05-health-tree
    provides: HealthCog with /health command and _notify_state_change
  - phase: 06-resilience
    provides: MessageQueue with priority/debounce, DegradedModeManager with health_check
  - phase: 08-companyroot-wiring-and-migration
    provides: VcoBot client.py with CompanyRoot wiring
provides:
  - HealthCog loaded at bot startup via _COG_EXTENSIONS
  - DegradedModeManager activated with Claude API health_check in CompanyRoot
  - MessageQueue instantiated and started for outbound Discord messages
  - Degraded/recovered notifications routed to alerts channel
affects: [08.1-02, integration-testing]

tech-stack:
  added: []
  patterns:
    - "Claude API ping as health_check callable for DegradedModeManager"
    - "Injectable send_func wrapping discord channel.send with RateLimited on 429"

key-files:
  created: []
  modified:
    - src/vcompany/bot/client.py
    - tests/test_bot_client.py

key-decisions:
  - "Claude API health check uses minimal messages.create ping (1 max_token) to minimize cost"
  - "MessageQueue send_func wraps discord.HTTPException 429 into RateLimited for queue backoff"
  - "on_degraded/on_recovered callbacks send human-readable messages to alerts channel"

patterns-established:
  - "Wiring pattern: define async closures in on_ready that capture self._system_channels for callbacks"

requirements-completed: [HLTH-03, HLTH-04, RESL-01, RESL-03]

duration: 2min
completed: 2026-03-28
---

# Phase 08.1 Plan 01: Bot Integration Wiring Summary

**HealthCog loaded at startup, DegradedModeManager activated with Claude API health_check, MessageQueue wired for outbound notifications**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-28T02:35:41Z
- **Completed:** 2026-03-28T02:37:52Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Closed 3 of 5 integration gaps from the milestone audit (HLTH-03, HLTH-04, RESL-01, RESL-03)
- HealthCog now loads at bot startup enabling /health command and push notifications
- DegradedModeManager activates with Claude API connectivity check, with alerts on state transitions
- MessageQueue handles outbound Discord messages with priority and rate-limit backoff

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire HealthCog, DegradedMode health_check, and MessageQueue into client.py** - `4e440de` (feat)
2. **Task 2: Add tests for HealthCog loading, health_check wiring, and MessageQueue integration** - `6c45fa0` (test)

## Files Created/Modified
- `src/vcompany/bot/client.py` - Added HealthCog to _COG_EXTENSIONS, health_check/on_degraded/on_recovered to CompanyRoot, MessageQueue creation and lifecycle
- `tests/test_bot_client.py` - 5 new tests verifying HealthCog extension, health_check wiring, MessageQueue creation, init state, and close cleanup

## Decisions Made
- Claude API health check uses minimal `messages.create` with max_tokens=1 to minimize API cost while verifying connectivity
- MessageQueue send_func wraps `discord.HTTPException` with status 429 into `RateLimited` to trigger queue backoff
- on_degraded/on_recovered callbacks send descriptive messages to the alerts channel for human visibility

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all wiring points connect to existing, tested implementations.

## Next Phase Readiness
- HealthCog, DegradedModeManager, and MessageQueue are now wired at the bot startup layer
- Remaining integration gaps (FulltimeAgent backlog wiring, GsdAgent consumption loop) are addressed in plan 08.1-02

---
*Phase: 08.1-integration-wiring*
*Completed: 2026-03-28*
