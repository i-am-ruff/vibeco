---
phase: 02-agent-lifecycle-and-pre-flight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/orchestrator/__init__.py
  - src/vcompany/models/agent_state.py
  - src/vcompany/orchestrator/crash_tracker.py
  - tests/test_agent_state.py
  - tests/test_crash_tracker.py
autonomous: true
requirements: [LIFE-05, LIFE-06, LIFE-07]

must_haves:
  truths:
    - "Crash tracker records crashes with timestamps and exit codes"
    - "Exponential backoff returns 30s, 2min, 10min delays based on crash count"
    - "Circuit breaker blocks retry after 3 crashes within a sliding 60-minute window"
    - "Crash classification distinguishes transient from persistent failures"
    - "AgentsRegistry and AgentEntry models validate and serialize agent runtime state"
  artifacts:
    - path: "src/vcompany/models/agent_state.py"
      provides: "AgentEntry, AgentsRegistry, CrashRecord, CrashLog pydantic models"
      exports: ["AgentEntry", "AgentsRegistry", "CrashRecord", "CrashLog"]
    - path: "src/vcompany/orchestrator/crash_tracker.py"
      provides: "CrashTracker with backoff, circuit breaker, classification"
      exports: ["CrashTracker", "CrashClassification", "BACKOFF_SCHEDULE", "MAX_CRASHES_PER_HOUR"]
    - path: "src/vcompany/orchestrator/__init__.py"
      provides: "Orchestrator package init"
  key_links:
    - from: "src/vcompany/orchestrator/crash_tracker.py"
      to: "src/vcompany/models/agent_state.py"
      via: "imports CrashRecord, CrashLog for state persistence"
      pattern: "from vcompany\\.models\\.agent_state import"
    - from: "src/vcompany/orchestrator/crash_tracker.py"
      to: "src/vcompany/shared/file_ops.py"
      via: "uses write_atomic for crash log persistence"
      pattern: "from vcompany\\.shared\\.file_ops import write_atomic"
---

<objective>
Create the runtime state models and crash recovery logic that underpin the entire agent lifecycle system.

Purpose: The crash tracker provides exponential backoff, circuit breaker, and crash classification -- core safety logic that prevents runaway relaunches and API token waste. The state models define the schema for agents.json and crash_log.json that bridge dispatch (Plan 02), monitor (Phase 3), and recovery.

Output: Pydantic models for agent state and crash logs, CrashTracker class with full test coverage.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-lifecycle-and-pre-flight/02-CONTEXT.md
@.planning/phases/02-agent-lifecycle-and-pre-flight/02-RESEARCH.md

<interfaces>
<!-- Existing Phase 1 code the executor needs -->

From src/vcompany/shared/file_ops.py:
```python
def write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None:
    """Write content to path atomically using tmp-then-rename."""
```

From src/vcompany/models/config.py:
```python
class AgentConfig(BaseModel):
    id: str
    role: str
    owns: list[str]
    consumes: str
    gsd_mode: Literal["full", "quick"]
    system_prompt: str

class ProjectConfig(BaseModel):
    project: str
    repo: str
    agents: list[AgentConfig]
    shared_readonly: list[str] = []

def load_config(config_path: Path) -> ProjectConfig:
    """Load and validate agents.yaml."""
```

From src/vcompany/git/ops.py:
```python
@dataclass
class GitResult:
    success: bool
    stdout: str
    stderr: str
    returncode: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State models and crash tracker with TDD</name>
  <files>
    src/vcompany/orchestrator/__init__.py,
    src/vcompany/models/agent_state.py,
    src/vcompany/orchestrator/crash_tracker.py,
    tests/test_agent_state.py,
    tests/test_crash_tracker.py
  </files>
  <read_first>
    src/vcompany/models/config.py,
    src/vcompany/shared/file_ops.py,
    src/vcompany/git/ops.py,
    tests/test_config.py
  </read_first>
  <behavior>
    AgentEntry model tests (tests/test_agent_state.py):
    - AgentEntry with all fields validates and serializes to JSON with ISO datetime
    - AgentEntry with status="running" is valid; status must be one of: starting, running, stopped, crashed, circuit_open
    - AgentsRegistry.agents is a dict[str, AgentEntry], keyed by agent_id
    - AgentsRegistry round-trips through model_dump_json() / model_validate_json()

    CrashRecord model tests (tests/test_agent_state.py):
    - CrashRecord stores agent_id, timestamp (datetime), exit_code (int), classification (str), pane_output (list[str])
    - CrashLog stores project (str) and records (list[CrashRecord])

    CrashTracker tests (tests/test_crash_tracker.py):
    - TestBackoff: recent_crash_count=0 -> delay=30s; count=1 -> delay=120s; count=2 -> delay=600s; count>=3 -> delay=600s (per D-11)
    - TestCircuitBreaker: 3 crashes in 60min -> should_retry=True; 4th crash in same window -> should_retry=False (per D-12)
    - TestCircuitBreaker: 3 crashes in 60min, then 61min passes, new crash -> should_retry=True (sliding window resets)
    - TestCircuitBreaker: reset_circuit(agent_id) clears crash history and allows retry
    - TestClassification: exit_code=0 + checkpoint exists -> "transient_context_exhaustion"
    - TestClassification: exit_code!=0 + no repeated error -> "transient_runtime_error"
    - TestClassification: same error pattern in last 2 crashes -> "persistent_repeated_error"
    - TestClassification: missing STATE.md in .planning/ -> "persistent_corrupt_state"
    - record_crash persists to crash_log.json via write_atomic
    - CrashTracker loads existing crash_log.json on init (state survives process restart)
  </behavior>
  <action>
    1. Create `src/vcompany/orchestrator/__init__.py` (empty, makes it a package).

    2. Create `src/vcompany/models/agent_state.py` with Pydantic v2 models:
       - `AgentEntry(BaseModel)`: agent_id (str), pane_id (str), pid (int | None = None), session_name (str), status (Literal["starting", "running", "stopped", "crashed", "circuit_open"]), launched_at (datetime), last_crash (datetime | None = None). Per D-05.
       - `AgentsRegistry(BaseModel)`: project (str), agents (dict[str, AgentEntry] = {}). Per D-05.
       - `CrashRecord(BaseModel)`: agent_id (str), timestamp (datetime), exit_code (int), classification (str), pane_output (list[str] = []). Per D-13.
       - `CrashLog(BaseModel)`: project (str), records (list[CrashRecord] = []).

    3. Create `src/vcompany/orchestrator/crash_tracker.py`:
       - Constants: BACKOFF_SCHEDULE = [30, 120, 600], MAX_CRASHES_PER_HOUR = 3. Per D-11, D-12.
       - Enum `CrashClassification(str, Enum)`: TRANSIENT_CONTEXT_EXHAUSTION, TRANSIENT_RUNTIME_ERROR, PERSISTENT_REPEATED_ERROR, PERSISTENT_CORRUPT_STATE, UNCLASSIFIED.
       - `CrashTracker.__init__(self, crash_log_path: Path)`: loads existing CrashLog from file if exists, else creates empty.
       - `record_crash(agent_id, exit_code, classification, pane_output)`: appends CrashRecord, persists via write_atomic. Per D-13.
       - `recent_crash_count(agent_id) -> int`: count crashes for agent in last 60 minutes (sliding window). Per D-12.
       - `should_retry(agent_id) -> bool`: True if recent_crash_count < MAX_CRASHES_PER_HOUR. Per D-12.
       - `get_retry_delay(agent_id) -> int`: index into BACKOFF_SCHEDULE by recent_crash_count, cap at last value. Per D-11.
       - `classify_crash(agent_id, exit_code, clone_dir, pane_output) -> CrashClassification`: Per D-10:
         * exit_code == 0 AND (clone_dir / ".planning" / "STATE.md").exists() -> TRANSIENT_CONTEXT_EXHAUSTION
         * Check last 2 crash records for same agent -- if both have same first line of pane_output -> PERSISTENT_REPEATED_ERROR
         * Check (clone_dir / ".planning" / "STATE.md").exists() -- if missing -> PERSISTENT_CORRUPT_STATE
         * Default: TRANSIENT_RUNTIME_ERROR
       - `reset_circuit(agent_id)`: removes all crash records for agent (manual relaunch override). Per D-12.

    4. Write tests FIRST (RED), then implement (GREEN). Follow Phase 1 TDD pattern from tests/test_config.py.
       - Use `tmp_path` fixture for crash_log.json file.
       - Use `freezegun` or manual datetime injection for sliding window tests. If freezegun not available, pass `now` parameter to methods for testability.
       - For classification tests, create minimal directory structures in tmp_path.
  </action>
  <verify>
    <automated>/home/developer/vcompany/.venv/bin/python3 -m pytest tests/test_agent_state.py tests/test_crash_tracker.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class AgentEntry" src/vcompany/models/agent_state.py
    - grep -q "class AgentsRegistry" src/vcompany/models/agent_state.py
    - grep -q "class CrashRecord" src/vcompany/models/agent_state.py
    - grep -q "class CrashLog" src/vcompany/models/agent_state.py
    - grep -q "BACKOFF_SCHEDULE" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "MAX_CRASHES_PER_HOUR = 3" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "class CrashTracker" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "def should_retry" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "def classify_crash" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "def reset_circuit" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "write_atomic" src/vcompany/orchestrator/crash_tracker.py
    - grep -q "TestBackoff" tests/test_crash_tracker.py
    - grep -q "TestCircuitBreaker" tests/test_crash_tracker.py
    - grep -q "TestClassification" tests/test_crash_tracker.py
  </acceptance_criteria>
  <done>
    All Pydantic state models validate, serialize, and round-trip. CrashTracker correctly implements 30s/2min/10min backoff, 3-crashes/hour circuit breaker with sliding window, and 4-category crash classification. All tests pass.
  </done>
</task>

</tasks>

<verification>
- All tests pass: `/home/developer/vcompany/.venv/bin/python3 -m pytest tests/test_agent_state.py tests/test_crash_tracker.py -x -v`
- Full existing suite still passes: `/home/developer/vcompany/.venv/bin/python3 -m pytest tests/ -x -q`
- Models importable: `/home/developer/vcompany/.venv/bin/python3 -c "from vcompany.models.agent_state import AgentEntry, AgentsRegistry, CrashRecord, CrashLog"`
- Crash tracker importable: `/home/developer/vcompany/.venv/bin/python3 -c "from vcompany.orchestrator.crash_tracker import CrashTracker, CrashClassification"`
</verification>

<success_criteria>
- AgentEntry/AgentsRegistry models validate agent runtime state with typed fields
- CrashRecord/CrashLog models store crash history with timestamps
- CrashTracker.get_retry_delay returns 30/120/600 based on crash count
- CrashTracker.should_retry returns False after 3+ crashes in 60 minutes
- CrashTracker.classify_crash distinguishes 4 failure categories per D-10
- All state persists to JSON files via write_atomic (survives process restart)
- 10+ tests cover all backoff, circuit breaker, and classification behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-lifecycle-and-pre-flight/02-01-SUMMARY.md`
</output>
