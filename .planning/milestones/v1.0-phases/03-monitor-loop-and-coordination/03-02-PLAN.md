---
phase: 03-monitor-loop-and-coordination
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/monitor/status_generator.py
  - src/vcompany/monitor/heartbeat.py
  - tests/test_status_generator.py
  - tests/test_heartbeat.py
autonomous: true
requirements:
  - MON-05
  - MON-06
  - MON-07
  - MON-08

must_haves:
  truths:
    - "PROJECT-STATUS.md is generated from each clone's ROADMAP.md and git log every cycle"
    - "PROJECT-STATUS.md matches the VCO-ARCHITECTURE.md format (per-agent phase list with emoji status, Key Dependencies, Notes)"
    - "PROJECT-STATUS.md is distributed to every clone via write_atomic"
    - "Heartbeat file is written every cycle and watchdog detects stale heartbeat"
  artifacts:
    - path: "src/vcompany/monitor/status_generator.py"
      provides: "PROJECT-STATUS.md generation and distribution"
      exports: ["generate_project_status", "distribute_project_status"]
    - path: "src/vcompany/monitor/heartbeat.py"
      provides: "Heartbeat file writing and watchdog checking"
      exports: ["write_heartbeat", "check_heartbeat"]
    - path: "tests/test_status_generator.py"
      provides: "Tests for status generation, ROADMAP parsing, and distribution"
    - path: "tests/test_heartbeat.py"
      provides: "Tests for heartbeat write and watchdog stale detection"
  key_links:
    - from: "src/vcompany/monitor/status_generator.py"
      to: "src/vcompany/git/ops.py"
      via: "git_ops.log() for recent activity"
      pattern: "git_ops\\.log"
    - from: "src/vcompany/monitor/status_generator.py"
      to: "src/vcompany/shared/file_ops.py"
      via: "write_atomic for PROJECT-STATUS.md writes"
      pattern: "write_atomic"
    - from: "src/vcompany/monitor/heartbeat.py"
      to: "src/vcompany/shared/file_ops.py"
      via: "write_atomic for heartbeat file"
      pattern: "write_atomic"
---

<objective>
Build the PROJECT-STATUS.md generator (reads ROADMAP.md + git log from each clone, assembles formatted status, distributes to all clones) and the heartbeat/watchdog system.

Purpose: PROJECT-STATUS.md provides cross-agent awareness -- each agent knows what others are doing. The heartbeat ensures the monitor itself is supervised.
Output: Status generator module, heartbeat module, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monitor-loop-and-coordination/03-CONTEXT.md
@.planning/phases/03-monitor-loop-and-coordination/03-RESEARCH.md

<interfaces>
From src/vcompany/git/ops.py:
```python
def log(cwd: Path, args: list[str] | None = None) -> GitResult: ...
```

From src/vcompany/shared/file_ops.py:
```python
def write_atomic(path: Path, content: str, encoding: str = "utf-8") -> None: ...
```

From src/vcompany/models/config.py:
```python
class AgentConfig(BaseModel):
    id: str
    role: str
    owns: list[str]
    ...

class ProjectConfig(BaseModel):
    project: str
    repo: str
    agents: list[AgentConfig]
    shared_readonly: list[str] = []
```

PROJECT-STATUS.md format from VCO-ARCHITECTURE.md (lines 260-298):
```markdown
# Project Status -- auto-generated by vco, do not edit
# Last updated: 2026-03-25 08:30 UTC

## AGENT-ID (Phase X/Y -- status)
- [emoji] Phase N: description
...

## Key Dependencies
- AGENT Phase X needs: OTHER-AGENT resource (Phase Y -- status)

## Notes
- Free-form notes about stubs, mocks, blockers
```

Emoji mapping:
- Completed phase: checkmark
- In-progress phase: cycle arrows
- Not started phase: hourglass
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PROJECT-STATUS.md generation and distribution</name>
  <files>
    src/vcompany/monitor/status_generator.py,
    tests/test_status_generator.py
  </files>
  <read_first>
    src/vcompany/git/ops.py,
    src/vcompany/shared/file_ops.py,
    src/vcompany/models/config.py,
    VCO-ARCHITECTURE.md (lines 250-300 for PROJECT-STATUS.md format)
  </read_first>
  <behavior>
    - test_parse_roadmap_with_phases: Given a ROADMAP.md with phase lines using "[x]" (complete), "[ ]" (pending), and status text, parser extracts phase number, description, and status
    - test_parse_roadmap_missing: Missing ROADMAP.md returns "Status unknown" for agent
    - test_parse_roadmap_malformed: Malformed ROADMAP.md does not raise, returns partial result with "unknown" for unparseable lines
    - test_generate_status_single_agent: One agent with 3 phases (1 complete, 1 executing, 1 pending) produces correct markdown with emoji markers
    - test_generate_status_multiple_agents: Two agents produce sections for each agent in correct format
    - test_generate_status_includes_git_log: Recent git log included as activity indicator
    - test_generate_status_header: Output starts with "# Project Status" header and timestamp
    - test_distribute_writes_to_all_clones: Status file written to {project}/context/PROJECT-STATUS.md AND each clone's root
    - test_distribute_uses_write_atomic: All writes go through write_atomic (not raw open/write)
  </behavior>
  <action>
    Create `src/vcompany/monitor/status_generator.py` implementing D-10, D-11, D-12:

    1. `parse_roadmap(roadmap_path: Path) -> list[dict]`:
       - Read ROADMAP.md, find phase lines matching pattern like `- [x] **Phase N: Description**` or `- [ ] **Phase N: Description**`.
       - Use defensive regex: `r'- \[(x| )\] \*\*Phase (\d+): (.+?)\*\*'`.
       - For each phase: extract number, description, completed status.
       - Determine current executing phase: first unchecked phase.
       - Return list of dicts: `[{"number": 1, "description": "...", "status": "complete|executing|pending"}]`.
       - Wrap in try/except. On any failure return `[{"number": 0, "description": "Status unknown", "status": "unknown"}]`.

    2. `get_agent_activity(clone_dir: Path) -> str`:
       - Call `git_ops.log(clone_dir, args=["--oneline", "-5"])` per D-10.
       - Return stdout or "No recent activity" if empty/failed.

    3. `generate_project_status(project_dir: Path, config: ProjectConfig, *, now: datetime | None = None) -> str`:
       - For each agent in config.agents:
         - Parse `{project_dir}/clones/{agent.id}/.planning/ROADMAP.md`
         - Get git activity from clone
         - Build section with emoji status markers:
           - Complete: unicode checkmark U+2705
           - Executing: unicode arrows U+1F504
           - Pending: unicode hourglass U+231B
       - Assemble header: `# Project Status -- auto-generated by vco, do not edit\n# Last updated: {timestamp} UTC`
       - Assemble per-agent sections: `## {AGENT_ID.upper()} (Phase {current}/{total} -- {current_status})`
       - Add Key Dependencies section (empty placeholder for now -- Phase 6 Strategist will populate)
       - Add Notes section (empty placeholder)
       - Return the full markdown string.

    4. `distribute_project_status(project_dir: Path, config: ProjectConfig, content: str) -> int`:
       - Write to `{project_dir}/context/PROJECT-STATUS.md` via write_atomic per D-12.
       - Write to each `{project_dir}/clones/{agent.id}/PROJECT-STATUS.md` via write_atomic.
       - Return count of clones updated.

    5. Tests in `tests/test_status_generator.py`:
       - Use tmp_path to create mock clone directories with ROADMAP.md files.
       - Mock git_ops.log for activity.
       - Verify markdown output format matches VCO-ARCHITECTURE.md spec.
       - Verify write_atomic is called for distribution (mock or check file existence).
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_status_generator.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def parse_roadmap" src/vcompany/monitor/status_generator.py
    - grep -q "def generate_project_status" src/vcompany/monitor/status_generator.py
    - grep -q "def distribute_project_status" src/vcompany/monitor/status_generator.py
    - grep -q "write_atomic" src/vcompany/monitor/status_generator.py
    - grep -q "git_ops.log" src/vcompany/monitor/status_generator.py
    - grep -q "Project Status" src/vcompany/monitor/status_generator.py
    - grep -q "test_parse_roadmap_with_phases" tests/test_status_generator.py
    - grep -q "test_generate_status_single_agent" tests/test_status_generator.py
    - grep -q "test_distribute_writes_to_all_clones" tests/test_status_generator.py
  </acceptance_criteria>
  <done>
    PROJECT-STATUS.md is generated from ROADMAP.md + git log per agent, formatted per VCO-ARCHITECTURE.md spec, and distributed to all clones via write_atomic. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heartbeat file and watchdog checker</name>
  <files>
    src/vcompany/monitor/heartbeat.py,
    tests/test_heartbeat.py
  </files>
  <read_first>
    src/vcompany/shared/file_ops.py,
    .planning/phases/03-monitor-loop-and-coordination/03-RESEARCH.md (heartbeat pattern section)
  </read_first>
  <behavior>
    - test_write_heartbeat_creates_file: write_heartbeat creates heartbeat file with ISO timestamp
    - test_write_heartbeat_uses_write_atomic: File written via write_atomic (atomic rename)
    - test_write_heartbeat_correct_path: Written to {project_dir}/state/monitor_heartbeat per D-18
    - test_check_heartbeat_fresh: Heartbeat file < 180s old returns True (healthy)
    - test_check_heartbeat_stale: Heartbeat file > 180s old returns False (stale) per D-19
    - test_check_heartbeat_missing: No heartbeat file returns False
    - test_check_heartbeat_corrupt: Unparseable content returns False
    - test_write_heartbeat_at_cycle_start: Per Pitfall 6, heartbeat written at START of cycle to prevent false watchdog triggers during long cycles
  </behavior>
  <action>
    Create `src/vcompany/monitor/heartbeat.py` implementing D-18, D-19:

    1. `write_heartbeat(project_dir: Path, *, now: datetime | None = None) -> None`:
       - If now is None, use `datetime.now(timezone.utc)`.
       - Write ISO timestamp to `{project_dir}/state/monitor_heartbeat` via write_atomic.
       - Per Pitfall 6 from Research: this should be called at the START of each cycle, not end.

    2. `check_heartbeat(project_dir: Path, max_age_seconds: int = 180, *, now: datetime | None = None) -> bool`:
       - Read `{project_dir}/state/monitor_heartbeat`.
       - If missing, return False.
       - Parse ISO timestamp. If parse fails, return False.
       - If now is None, use `datetime.now(timezone.utc)`.
       - Compare age to max_age_seconds (default 180 = 3 missed cycles per D-19).
       - Return True if fresh, False if stale.

    3. Tests in `tests/test_heartbeat.py`:
       - Use tmp_path for project_dir.
       - Use `now` parameter injection for time-dependent tests.
       - Test all behavior cases listed above.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_heartbeat.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def write_heartbeat" src/vcompany/monitor/heartbeat.py
    - grep -q "def check_heartbeat" src/vcompany/monitor/heartbeat.py
    - grep -q "write_atomic" src/vcompany/monitor/heartbeat.py
    - grep -q "monitor_heartbeat" src/vcompany/monitor/heartbeat.py
    - grep -q "max_age_seconds" src/vcompany/monitor/heartbeat.py
    - grep -q "180" src/vcompany/monitor/heartbeat.py
    - grep -q "test_write_heartbeat_creates_file" tests/test_heartbeat.py
    - grep -q "test_check_heartbeat_stale" tests/test_heartbeat.py
    - grep -q "test_check_heartbeat_missing" tests/test_heartbeat.py
  </acceptance_criteria>
  <done>
    Heartbeat file written at {project}/state/monitor_heartbeat with ISO timestamp via write_atomic. Watchdog checker detects stale (>180s), missing, or corrupt heartbeat files. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_status_generator.py tests/test_heartbeat.py -x -v` all pass
- PROJECT-STATUS.md output matches VCO-ARCHITECTURE.md format
- Heartbeat correctly detects stale/missing/corrupt states
</verification>

<success_criteria>
- generate_project_status and distribute_project_status importable from vcompany.monitor.status_generator
- write_heartbeat and check_heartbeat importable from vcompany.monitor.heartbeat
- PROJECT-STATUS.md format has agent sections with emoji markers, Key Dependencies, Notes
- All test cases green
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitor-loop-and-coordination/03-02-SUMMARY.md`
</output>
