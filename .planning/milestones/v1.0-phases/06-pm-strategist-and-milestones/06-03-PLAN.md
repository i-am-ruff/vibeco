---
phase: 06-pm-strategist-and-milestones
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/vcompany/strategist/pm.py
  - src/vcompany/strategist/plan_reviewer.py
  - tests/test_pm_tier.py
  - tests/test_pm_plan_review.py
autonomous: true
requirements:
  - STRAT-02
  - STRAT-03
  - STRAT-04
  - STRAT-05
  - STRAT-06
  - STRAT-07

must_haves:
  truths:
    - "PM evaluates agent questions and returns HIGH/MEDIUM/LOW confidence answers per D-05"
    - "HIGH confidence answers are delivered directly per STRAT-03"
    - "MEDIUM confidence answers include 'PM confidence: medium -- @Owner can override' per STRAT-04"
    - "LOW confidence returns escalate_to='strategist' per STRAT-05/D-05"
    - "PM reviews plans with three checks: scope alignment, dependency readiness, duplicate detection per D-14"
    - "Plan review checks PROJECT-STATUS.md for dependency readiness per STRAT-07"
  artifacts:
    - path: "src/vcompany/strategist/pm.py"
      provides: "PMTier class with evaluate_question and evaluate_plan methods"
      exports: ["PMTier"]
    - path: "src/vcompany/strategist/plan_reviewer.py"
      provides: "Plan review with scope/dependency/duplicate checks"
      exports: ["PlanReviewer"]
    - path: "tests/test_pm_tier.py"
      provides: "Tests for PM question evaluation"
    - path: "tests/test_pm_plan_review.py"
      provides: "Tests for PM plan review"
  key_links:
    - from: "src/vcompany/strategist/pm.py"
      to: "src/vcompany/strategist/confidence.py"
      via: "ConfidenceScorer for heuristic scoring"
      pattern: "from vcompany\\.strategist\\.confidence import ConfidenceScorer"
    - from: "src/vcompany/strategist/pm.py"
      to: "src/vcompany/strategist/context_builder.py"
      via: "reads PM-CONTEXT.md for system prompt"
      pattern: "build_pm_context"
    - from: "src/vcompany/strategist/plan_reviewer.py"
      to: "PROJECT-STATUS.md"
      via: "reads status to check dependency readiness"
      pattern: "PROJECT-STATUS"
---

<objective>
Build the PM tier: stateless question answering with confidence-based escalation, and plan review with three-check validation.

Purpose: The PM is the first tier of the escalation chain (D-05). It handles agent questions and plan reviews using heuristic confidence and fresh Claude API calls. HIGH confidence = auto-answer, MEDIUM = answer with override note, LOW = escalate to Strategist. Plan review checks scope alignment, dependency readiness, and duplicate detection (D-14).
Output: PMTier and PlanReviewer classes, fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
@.planning/phases/06-pm-strategist-and-milestones/06-RESEARCH.md
@.planning/phases/06-pm-strategist-and-milestones/06-01-SUMMARY.md

<interfaces>
From src/vcompany/strategist/models.py (created by Plan 01):
```python
class ConfidenceResult(BaseModel):
    score: float
    level: Literal["HIGH", "MEDIUM", "LOW"]
    coverage: float
    prior_match: float

class PMDecision(BaseModel):
    answer: str | None
    confidence: ConfidenceResult
    decided_by: Literal["PM", "Strategist", "Owner"]
    note: str = ""
    escalate_to: str | None = None

class DecisionLogEntry(BaseModel):
    timestamp: datetime
    question_or_plan: str
    decision: str
    confidence_level: str
    decided_by: str
    agent_id: str
```

From src/vcompany/strategist/confidence.py (created by Plan 01):
```python
class ConfidenceScorer:
    def score(self, question: str, context_docs: dict[str, str],
              decision_log: list[DecisionLogEntry]) -> ConfidenceResult: ...
```

From src/vcompany/strategist/context_builder.py (created by Plan 01):
```python
def build_pm_context(project_dir: Path) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PMTier question evaluation with escalation chain</name>
  <files>
    src/vcompany/strategist/pm.py,
    tests/test_pm_tier.py
  </files>
  <read_first>
    src/vcompany/strategist/models.py,
    src/vcompany/strategist/confidence.py,
    src/vcompany/strategist/context_builder.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test: evaluate_question with HIGH confidence returns PMDecision with answer and decided_by="PM"
    - Test: evaluate_question with MEDIUM confidence returns PMDecision with note containing "PM confidence: medium"
    - Test: evaluate_question with LOW confidence returns PMDecision with answer=None and escalate_to="strategist"
    - Test: PM makes a fresh Claude API call per question (stateless per D-01)
    - Test: PM uses PM-CONTEXT.md content as system prompt for Claude API call
    - Test: Decision log entries loaded from state/decisions.jsonl for confidence scoring
    - Test: evaluate_question returns PMDecision even when Claude API call fails (graceful degradation)
  </behavior>
  <action>
    1. Create `src/vcompany/strategist/pm.py` per D-01 (stateless), D-05 (escalation), D-08 (heuristic confidence):
       - `class PMTier`:
         - `__init__(self, client: AsyncAnthropic, project_dir: Path)`:
           - `self._client = client`
           - `self._project_dir = project_dir`
           - `self._scorer = ConfidenceScorer()`
         - `async def evaluate_question(self, question: str, agent_id: str) -> PMDecision`:
           - Load context docs from project_dir/context/ (blueprint, interfaces, scope, status)
           - Load decision log from project_dir/state/decisions.jsonl
           - Score confidence via self._scorer.score(question, context_docs, decision_log)
           - If HIGH: call `_answer_directly(question, context_docs)` -> return PMDecision(answer=answer, confidence=confidence, decided_by="PM")
           - If MEDIUM: call `_answer_directly(question, context_docs)` -> return PMDecision(answer=answer, confidence=confidence, decided_by="PM", note="PM confidence: medium -- @Owner can override")
           - If LOW: return PMDecision(answer=None, confidence=confidence, decided_by="PM", escalate_to="strategist")
         - `async def _answer_directly(self, question: str, context_docs: dict[str, str]) -> str`:
           - Build system prompt from PM-CONTEXT.md (call build_pm_context or read cached file)
           - Call `self._client.messages.create(model="claude-sonnet-4-20250514", system=system_prompt, messages=[{"role": "user", "content": question}], max_tokens=2048)`
           - Return response.content[0].text
           - On any exception: return "PM was unable to generate an answer. Escalating."
         - `_load_context_docs(self) -> dict[str, str]`: read the four context files from project_dir/context/
         - `_load_decision_log(self) -> list[DecisionLogEntry]`: read state/decisions.jsonl, parse each line as JSON, validate with DecisionLogEntry model

    2. Write tests in `tests/test_pm_tier.py` FIRST (RED), then implement (GREEN).
       - Mock AsyncAnthropic client
       - Mock ConfidenceScorer to return controlled confidence levels
       - Use tmp_path for project_dir with pre-written context files
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_pm_tier.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class PMTier" src/vcompany/strategist/pm.py` returns 1
    - `grep "evaluate_question" src/vcompany/strategist/pm.py` matches the method
    - `grep "escalate_to.*strategist" src/vcompany/strategist/pm.py` matches LOW confidence path
    - `grep "PM confidence: medium" src/vcompany/strategist/pm.py` matches MEDIUM note per D-05
    - `grep "ConfidenceScorer" src/vcompany/strategist/pm.py` matches scorer usage
    - `uv run pytest tests/test_pm_tier.py -x -v` passes all tests
  </acceptance_criteria>
  <done>PMTier evaluates questions with heuristic confidence, auto-answers HIGH/MEDIUM, escalates LOW to Strategist. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanReviewer with three-check system</name>
  <files>
    src/vcompany/strategist/plan_reviewer.py,
    tests/test_pm_plan_review.py
  </files>
  <read_first>
    src/vcompany/strategist/models.py,
    src/vcompany/strategist/pm.py,
    src/vcompany/monitor/status_generator.py,
    src/vcompany/bot/cogs/plan_review.py,
    .planning/phases/06-pm-strategist-and-milestones/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test: scope_check passes when plan files are within agent's owned_dirs from agents.yaml
    - Test: scope_check fails when plan modifies files outside agent's owned_dirs
    - Test: dependency_check passes when PROJECT-STATUS.md shows dependencies as "complete"
    - Test: dependency_check fails when dependencies show as "executing" or "pending" and plan lacks stubs/mocks
    - Test: dependency_check passes when dependencies not complete BUT plan mentions stubs/mocks
    - Test: duplicate_check passes when no similar plan exists in approved plans list
    - Test: duplicate_check fails when a plan with same objective/files already approved for another agent
    - Test: review_plan returns PMDecision with HIGH confidence when all three checks pass per D-15
    - Test: review_plan returns PMDecision with LOW confidence and escalate_to="strategist" on any check failure per D-16
  </behavior>
  <action>
    1. Create `src/vcompany/strategist/plan_reviewer.py` per D-14, D-15, D-16, D-17:
       - `class PlanReviewer`:
         - `__init__(self, project_dir: Path, config: ProjectConfig)`:
           - `self._project_dir = project_dir`
           - `self._config = config`
         - `def review_plan(self, agent_id: str, plan_content: str) -> PMDecision`:
           - Run three checks: scope_check, dependency_check, duplicate_check
           - If all pass: return PMDecision(answer="Plan approved by PM", confidence=ConfidenceResult(score=0.95, level="HIGH", ...), decided_by="PM")
           - If any fail: return PMDecision(answer=None, confidence=ConfidenceResult(score=0.4, level="LOW", ...), decided_by="PM", escalate_to="strategist", note=f"Failed checks: {failed_check_names}")
           - Note: D-17 says PM does NOT re-check safety tables (Phase 5 handles that)
         - `def _scope_check(self, agent_id: str, plan_content: str) -> tuple[bool, str]`:
           - Extract files_modified from plan frontmatter
           - Find agent in self._config.agents by id
           - Check each file path starts with one of agent.owned_dirs
           - Check requirements in plan frontmatter are mapped to agent's assigned phase
           - Return (True, "Scope aligned") or (False, "Files outside owned dirs: {files}")
         - `def _dependency_check(self, plan_content: str) -> tuple[bool, str]`:
           - Read PROJECT-STATUS.md from project_dir/context/
           - Extract depends_on from plan frontmatter
           - For each dependency, check if the phase it belongs to shows "complete" in status
           - If incomplete: check if plan_content mentions "stub", "mock", "placeholder"
           - Return (True, "Dependencies ready") or (False, "Unmet dependencies: {deps}")
         - `def _duplicate_check(self, agent_id: str, plan_content: str) -> tuple[bool, str]`:
           - Read approved plans from project_dir/state/approved_plans.jsonl (if exists)
           - Extract objective and files_modified from plan
           - Compare against approved plans: check for >70% file overlap or similar objective text
           - Return (True, "No duplicates") or (False, "Possible duplicate of {plan_ref}")
         - Helper: `_extract_frontmatter(content: str) -> dict`: parse YAML frontmatter from plan

    2. Write tests in `tests/test_pm_plan_review.py` FIRST (RED), then implement (GREEN).
       - Use tmp_path for project dir with agents.yaml, PROJECT-STATUS.md, approved_plans.jsonl
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_pm_plan_review.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class PlanReviewer" src/vcompany/strategist/plan_reviewer.py` returns 1
    - `grep "def _scope_check" src/vcompany/strategist/plan_reviewer.py` matches
    - `grep "def _dependency_check" src/vcompany/strategist/plan_reviewer.py` matches
    - `grep "def _duplicate_check" src/vcompany/strategist/plan_reviewer.py` matches
    - `grep "escalate_to.*strategist" src/vcompany/strategist/plan_reviewer.py` matches failed check path
    - `grep -v "safety" src/vcompany/strategist/plan_reviewer.py | grep -c "validate"` confirms PM does NOT validate safety tables per D-17
    - `uv run pytest tests/test_pm_plan_review.py -x -v` passes all tests
  </acceptance_criteria>
  <done>PlanReviewer runs scope/dependency/duplicate checks. All-pass = HIGH confidence auto-approve. Any-fail = LOW confidence escalate to Strategist. Tests pass.</done>
</task>

</tasks>

<verification>
cd /home/developer/vcompany && uv run pytest tests/test_pm_tier.py tests/test_pm_plan_review.py -x -v
</verification>

<success_criteria>
- PMTier answers questions with correct escalation chain (HIGH=direct, MEDIUM=direct+note, LOW=escalate) per D-05/D-06
- PM is stateless per D-01 -- fresh context per call
- PlanReviewer runs three checks per D-14: scope, dependency, duplicate
- Dependency check reads PROJECT-STATUS.md per STRAT-07
- Safety table NOT re-checked per D-17
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-pm-strategist-and-milestones/06-03-SUMMARY.md`
</output>
