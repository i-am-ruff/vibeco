---
phase: 01-foundation-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/vcompany/__init__.py
  - src/vcompany/models/__init__.py
  - src/vcompany/models/config.py
  - src/vcompany/cli/__init__.py
  - src/vcompany/cli/main.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - FOUND-07
  - FOUND-01

must_haves:
  truths:
    - "Running `uv run python -c 'import vcompany'` succeeds"
    - "A valid agents.yaml is parsed into AgentConfig and ProjectConfig Pydantic models"
    - "An invalid agents.yaml raises pydantic.ValidationError with clear messages before any filesystem changes"
    - "Overlapping directory ownership between agents is rejected at parse time"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project configuration with dependencies and entry point"
      contains: "vco = \"vcompany.cli.main:cli\""
    - path: "src/vcompany/models/config.py"
      provides: "Pydantic v2 models for agents.yaml"
      exports: ["AgentConfig", "ProjectConfig", "load_config"]
    - path: "src/vcompany/cli/main.py"
      provides: "Click CLI group entry point"
      exports: ["cli"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
      contains: "sample_agents_yaml"
    - path: "tests/test_config.py"
      provides: "Config validation tests"
      contains: "test_valid_config"
  key_links:
    - from: "pyproject.toml"
      to: "src/vcompany/cli/main.py"
      via: "entry point"
      pattern: "vco.*vcompany\\.cli\\.main:cli"
    - from: "src/vcompany/models/config.py"
      to: "pydantic"
      via: "BaseModel inheritance"
      pattern: "class AgentConfig\\(BaseModel\\)"
---

<objective>
Bootstrap the vCompany Python project with uv and create the Pydantic configuration models for agents.yaml parsing and validation.

Purpose: Establish the project skeleton and the config models that every other module depends on. This is the first code written for the project.
Output: A working Python package installable via uv, with Pydantic models that parse agents.yaml and a click CLI shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-configuration/01-CONTEXT.md
@.planning/phases/01-foundation-and-configuration/01-RESEARCH.md
@VCO-ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap uv project and install dependencies</name>
  <files>pyproject.toml, src/vcompany/__init__.py, src/vcompany/cli/__init__.py, src/vcompany/cli/main.py, src/vcompany/models/__init__.py</files>
  <read_first>
    - VCO-ARCHITECTURE.md (directory structure reference)
    - .planning/phases/01-foundation-and-configuration/01-RESEARCH.md (uv install instructions, pyproject.toml entry point)
  </read_first>
  <action>
    1. Install uv: `curl -LsSf https://astral.sh/uv/install.sh | sh` then source the shell profile so `uv` is on PATH. Verify with `uv --version`.

    2. Initialize the project (per D-03, D-07):
       ```bash
       cd /home/developer/vcompany
       uv init --name vcompany --python 3.12
       ```

    3. Convert to src layout. Create `src/vcompany/` directory structure:
       - `src/vcompany/__init__.py` with `__version__ = "0.1.0"`
       - `src/vcompany/cli/__init__.py` (empty)
       - `src/vcompany/cli/main.py` (click group)
       - `src/vcompany/models/__init__.py` (empty)

    4. Edit `pyproject.toml` to use src layout and add all dependencies:
       - Set `[project.scripts]` to: `vco = "vcompany.cli.main:cli"` (per D-03)
       - Add core deps: `click>=8.1.6`, `pydantic>=2.11,<3`, `pyyaml>=6.0`, `libtmux>=0.55.0,<0.56`, `jinja2>=3.1`, `rich>=14.0`
       - Add `pydantic-settings>=2.13,<3` for env var loading
       - Add dev deps: `pytest`, `ruff`, `pytest-asyncio`
       - Configure `[tool.pytest.ini_options]`: `testpaths = ["tests"]`, `pythonpath = ["src"]`
       - Configure `[tool.ruff]`: `line-length = 100`, `target-version = "py312"`
       - Set `[build-system]` to use hatchling with src layout: `packages = ["src/vcompany"]`

    5. Create the click CLI shell in `src/vcompany/cli/main.py` (per D-01, D-02):
       ```python
       import click

       @click.group()
       @click.version_option(package_name="vcompany")
       def cli():
           """vCompany -- Autonomous Multi-Agent Development System"""
           pass
       ```

    6. Run `uv sync` to install all dependencies and create the venv.

    7. Remove the auto-generated `hello.py` or `main.py` that `uv init` may create at project root.
  </action>
  <behavior>
    - Test 1: `uv run python -c "import vcompany"` exits 0
    - Test 2: `uv run python -c "from vcompany.cli.main import cli"` exits 0
    - Test 3: `uv run vco --version` prints version string
    - Test 4: `uv run vco --help` shows "Autonomous Multi-Agent Development System"
  </behavior>
  <verify>
    <automated>uv run python -c "import vcompany; from vcompany.cli.main import cli; print('OK')" && uv run vco --help</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `vco = "vcompany.cli.main:cli"` in [project.scripts]
    - pyproject.toml contains `click` and `pydantic` in dependencies
    - pyproject.toml contains `[tool.pytest.ini_options]` section with `testpaths = ["tests"]`
    - src/vcompany/__init__.py contains `__version__`
    - src/vcompany/cli/main.py contains `@click.group()` and `def cli():`
    - `uv run vco --help` exits 0
  </acceptance_criteria>
  <done>uv project builds, `uv run vco --help` works, all dependencies installed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic config models with validation and test suite</name>
  <files>src/vcompany/models/config.py, src/vcompany/models/__init__.py, tests/conftest.py, tests/__init__.py, tests/test_config.py</files>
  <read_first>
    - VCO-ARCHITECTURE.md (agents.yaml schema, lines 122-166)
    - .planning/phases/01-foundation-and-configuration/01-RESEARCH.md (Pattern 1: Pydantic validation, lines 174-224)
    - .planning/phases/01-foundation-and-configuration/01-CONTEXT.md (D-04, D-05, D-06 decisions)
  </read_first>
  <action>
    Create `src/vcompany/models/config.py` with Pydantic v2 models (per D-04, D-05, D-06):

    **AgentConfig model:**
    - `id: str` -- field_validator: must be alphanumeric with hyphens/underscores, non-empty
    - `role: str` -- non-empty string describing the agent's role
    - `owns: list[str]` -- field_validator: must not be empty, each entry must be a relative path
    - `consumes: str` -- the file this agent reads (e.g., "INTERFACES.md")
    - `gsd_mode: Literal["full", "quick"]` -- how the agent runs GSD
    - `system_prompt: str` -- path to the agent's system prompt template (e.g., "agents/BACKEND.md")

    **ProjectConfig model:**
    - `project: str` -- project name, non-empty
    - `repo: str` -- git repo URL (SSH or HTTPS)
    - `agents: list[AgentConfig]` -- must have at least 1 agent
    - `shared_readonly: list[str] = []` -- optional list of shared read-only paths

    **model_validator on ProjectConfig (per D-06):**
    - Validate no overlapping directory ownership. Normalize all paths to end with `/`. For each pair of owned directories across all agents, check that neither is a prefix of the other using `startswith()`. Raise ValueError with specific conflict details.
    - Validate agent IDs are unique.

    **load_config function:**
    ```python
    def load_config(config_path: Path) -> ProjectConfig:
        """Load and validate agents.yaml. Raises ValidationError on invalid config."""
        with open(config_path) as f:
            raw = yaml.safe_load(f)
        return ProjectConfig(**raw)
    ```

    **Export from models/__init__.py:**
    ```python
    from vcompany.models.config import AgentConfig, ProjectConfig, load_config
    ```

    **Create tests/conftest.py with shared fixtures:**
    - `sample_agents_yaml` fixture: returns a dict with valid agents.yaml content (2 agents: BACKEND owns ["src/api/", "src/db/"], FRONTEND owns ["src/app/", "src/components/"])
    - `sample_agents_yaml_file` fixture: writes the yaml to a tmp file and returns the path
    - `tmp_project_dir` fixture: creates a temporary directory for project operations

    **Create tests/test_config.py:**
    - `test_valid_config`: parse sample_agents_yaml, assert all fields populated correctly
    - `test_agent_id_validation`: invalid chars in ID raise ValidationError
    - `test_empty_owns_rejected`: agent with empty owns list rejected
    - `test_overlapping_dirs_rejected`: two agents owning "src/api/" and "src/api/routes/" rejected with message containing "Overlapping"
    - `test_exact_duplicate_dirs_rejected`: two agents owning same dir rejected
    - `test_non_overlapping_dirs_accepted`: "src/api/" and "src/app/" pass validation
    - `test_duplicate_agent_ids_rejected`: two agents with same id rejected
    - `test_load_config_from_file`: load_config parses yaml file successfully
    - `test_load_config_invalid_yaml`: missing required fields raise ValidationError
    - `test_gsd_mode_literal`: invalid gsd_mode value rejected
  </action>
  <behavior>
    - Test: valid YAML with 2 non-overlapping agents parses successfully
    - Test: agent ID with spaces raises ValidationError
    - Test: empty owns list raises ValidationError
    - Test: overlapping directory ownership raises ValidationError mentioning "Overlapping"
    - Test: duplicate agent IDs raises ValidationError
    - Test: invalid gsd_mode raises ValidationError
    - Test: load_config reads from file and returns ProjectConfig
  </behavior>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/vcompany/models/config.py contains `class AgentConfig(BaseModel):`
    - src/vcompany/models/config.py contains `class ProjectConfig(BaseModel):`
    - src/vcompany/models/config.py contains `def load_config(`
    - src/vcompany/models/config.py contains `model_validator` decorator
    - src/vcompany/models/config.py contains `field_validator` decorator
    - src/vcompany/models/config.py contains `startswith(` in the overlap check logic
    - src/vcompany/models/__init__.py contains `from vcompany.models.config import AgentConfig, ProjectConfig, load_config`
    - tests/conftest.py contains `sample_agents_yaml` fixture
    - tests/test_config.py contains `test_overlapping_dirs_rejected`
    - `uv run pytest tests/test_config.py -x` exits 0
  </acceptance_criteria>
  <done>All config model tests pass. Valid YAML parses, invalid YAML rejected with clear errors. Overlapping ownership detected.</done>
</task>

</tasks>

<verification>
- `uv run python -c "import vcompany"` succeeds
- `uv run vco --help` shows CLI help
- `uv run pytest tests/test_config.py -x -v` -- all tests pass
- `uv run pytest tests/ -x -q` -- full suite green
</verification>

<success_criteria>
- Python project bootstrapped with uv, all dependencies installed
- Pydantic v2 models parse and validate agents.yaml per VCO-ARCHITECTURE.md schema
- Overlapping directory ownership detected and rejected (per D-06)
- CLI entry point `vco` is functional (per D-01, D-03)
- Test suite covers all validation paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-configuration/01-01-SUMMARY.md`
</output>
