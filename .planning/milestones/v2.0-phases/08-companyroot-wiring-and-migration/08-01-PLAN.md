---
phase: 08-companyroot-wiring-and-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcompany/container/discord_communication.py
  - tests/test_discord_comm_port.py
  - src/vcompany/bot/client.py
  - tests/test_bot_client.py
autonomous: true
requirements: [MIGR-02, MIGR-04]
must_haves:
  truths:
    - "DiscordCommunicationPort satisfies the CommunicationPort Protocol at runtime"
    - "DiscordCommunicationPort routes messages to agent Discord channels without leaking Discord types through the Protocol interface"
    - "VcoBot no longer has command_prefix='!' in its constructor"
  artifacts:
    - path: "src/vcompany/container/discord_communication.py"
      provides: "Discord implementation of CommunicationPort Protocol"
      exports: ["DiscordCommunicationPort"]
    - path: "tests/test_discord_comm_port.py"
      provides: "Tests for DiscordCommunicationPort"
      min_lines: 50
  key_links:
    - from: "src/vcompany/container/discord_communication.py"
      to: "src/vcompany/container/communication.py"
      via: "implements CommunicationPort Protocol"
      pattern: "isinstance.*CommunicationPort"
---

<objective>
Implement DiscordCommunicationPort (MIGR-04) and remove the `!` command prefix (MIGR-02).

Purpose: The communication layer needs a clean abstract interface that Discord implements, preparing for v3 channel abstraction. The `!` prefix is vestigial since all commands are already slash commands.
Output: DiscordCommunicationPort class, tests, VcoBot constructor cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/vcompany/container/communication.py
@src/vcompany/bot/client.py
@tests/test_bot_client.py

<interfaces>
<!-- CommunicationPort Protocol that DiscordCommunicationPort must implement -->

From src/vcompany/container/communication.py:
```python
@dataclass
class Message:
    source: str
    target: str
    content: str
    timestamp: datetime

@runtime_checkable
class CommunicationPort(Protocol):
    async def send_message(self, target: str, content: str) -> bool: ...
    async def receive_message(self) -> Message | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DiscordCommunicationPort with tests</name>
  <files>src/vcompany/container/discord_communication.py, tests/test_discord_comm_port.py</files>
  <read_first>src/vcompany/container/communication.py, src/vcompany/bot/cogs/health.py</read_first>
  <behavior>
    - Test 1: DiscordCommunicationPort passes isinstance(port, CommunicationPort) check
    - Test 2: send_message returns True when target channel exists and send succeeds
    - Test 3: send_message returns False when guild is not found
    - Test 4: send_message returns False when target channel is not found
    - Test 5: send_message returns False when channel.send raises an exception
    - Test 6: receive_message returns None when inbox is empty
    - Test 7: receive_message returns Message from inbox when one has been enqueued
    - Test 8: deliver_message enqueues a Message into the inbox
  </behavior>
  <action>
    Create src/vcompany/container/discord_communication.py with class DiscordCommunicationPort:
    - Constructor takes bot (any object with get_guild method), agent_id: str, guild_id: int
    - Uses asyncio.Queue[Message] as internal inbox
    - send_message(target, content): looks up guild via bot.get_guild(guild_id), finds channel named "agent-{target}" in guild.text_channels, sends "[from:{agent_id}] {content}". Returns True on success, False on any failure.
    - receive_message(): calls self._inbox.get_nowait(), returns None on QueueEmpty
    - deliver_message(msg: Message): puts a message into the inbox queue (used by the bot to route incoming Discord messages to the right port)

    CRITICAL for v3 preparation (MIGR-04): The class must NOT inherit from CommunicationPort (it's a Protocol -- structural subtyping). No discord.py types appear in the Protocol interface. The DiscordCommunicationPort constructor and deliver_message are implementation-specific and NOT part of the Protocol. This is the extension point for v3 -- SlackCommunicationPort etc. would implement the same Protocol differently.

    All Discord imports (discord.utils.get, etc.) stay inside this module only. No container code imports this module directly -- they import CommunicationPort from communication.py.

    Create tests/test_discord_comm_port.py:
    - Mock bot object with get_guild returning a mock guild with text_channels
    - Test isinstance check against CommunicationPort
    - Test all send/receive paths described in behavior section
    - Use pytest-asyncio for async test methods
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run python -m pytest tests/test_discord_comm_port.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class DiscordCommunicationPort" src/vcompany/container/discord_communication.py
    - grep -q "async def send_message" src/vcompany/container/discord_communication.py
    - grep -q "async def receive_message" src/vcompany/container/discord_communication.py
    - grep -q "async def deliver_message" src/vcompany/container/discord_communication.py
    - grep -q "isinstance.*CommunicationPort" tests/test_discord_comm_port.py
    - grep -q "Queue" src/vcompany/container/discord_communication.py
  </acceptance_criteria>
  <done>DiscordCommunicationPort satisfies CommunicationPort Protocol, all send/receive paths tested, no Discord types leak through Protocol interface</done>
</task>

<task type="auto">
  <name>Task 2: Remove command_prefix and update VcoBot tests</name>
  <files>src/vcompany/bot/client.py, tests/test_bot_client.py</files>
  <read_first>src/vcompany/bot/client.py, tests/test_bot_client.py</read_first>
  <action>
    In src/vcompany/bot/client.py:
    - Remove `command_prefix="!"` from the super().__init__() call on line 55. The Bot constructor still needs to be called but without a command_prefix (or set it to an empty/unused value since discord.py requires it -- use `command_prefix=commands.when_mentioned` which makes the bot only respond to @mentions, effectively disabling prefix commands while keeping discord.py happy).
    - Update the class docstring to reflect that all commands are slash commands (no prefix)

    In tests/test_bot_client.py:
    - Find and update any assertion that checks for `command_prefix="!"` to reflect the new value
    - Ensure the test still verifies VcoBot can be constructed

    Per MIGR-02: All Discord commands are already `@app_commands.command` slash commands. This change removes the vestigial `!` prefix that is no longer used.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run python -m pytest tests/test_bot_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -qv 'command_prefix="!"' src/vcompany/bot/client.py
    - grep -q "when_mentioned" src/vcompany/bot/client.py
  </acceptance_criteria>
  <done>VcoBot no longer accepts `!` prefix commands, tests updated to match</done>
</task>

</tasks>

<verification>
- All DiscordCommunicationPort tests pass
- VcoBot tests pass without `!` prefix
- Full test suite: `uv run python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- DiscordCommunicationPort implements CommunicationPort Protocol (isinstance check passes)
- No Discord types (Guild, TextChannel) appear in CommunicationPort Protocol interface
- VcoBot constructor no longer has command_prefix="!"
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-companyroot-wiring-and-migration/08-01-SUMMARY.md`
</output>
