---
phase: 08-companyroot-wiring-and-migration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/vcompany/monitor/loop.py
  - src/vcompany/orchestrator/crash_tracker.py
  - src/vcompany/orchestrator/workflow_orchestrator.py
  - src/vcompany/orchestrator/agent_manager.py
  - src/vcompany/cli/dispatch_cmd.py
  - src/vcompany/cli/kill_cmd.py
  - src/vcompany/cli/relaunch_cmd.py
  - src/vcompany/cli/monitor_cmd.py
  - src/vcompany/bot/cogs/alerts.py
  - src/vcompany/bot/cogs/plan_review.py
  - tests/test_monitor_loop.py
  - tests/test_crash_tracker.py
  - tests/test_workflow_orchestrator.py
  - tests/test_dispatch.py
  - tests/test_kill.py
  - tests/test_relaunch.py
  - tests/test_conflict_resolver.py
  - tests/test_pm_integration.py
  - tests/test_integration_interlock.py
autonomous: true
requirements: [MIGR-03]
must_haves:
  truths:
    - "v1 MonitorLoop, CrashTracker, WorkflowOrchestrator, and AgentManager source files are deleted"
    - "No import anywhere in src/ or tests/ references the deleted modules"
    - "pytest --co collects all tests without ImportError"
    - "CLI commands dispatch, kill, relaunch, monitor still work (updated to use TmuxManager directly or marked deprecated)"
  artifacts:
    - path: "src/vcompany/orchestrator/agent_manager.py"
      provides: "DELETED"
    - path: "src/vcompany/orchestrator/crash_tracker.py"
      provides: "DELETED"
    - path: "src/vcompany/orchestrator/workflow_orchestrator.py"
      provides: "DELETED"
    - path: "src/vcompany/monitor/loop.py"
      provides: "DELETED"
  key_links:
    - from: "src/vcompany/cli/dispatch_cmd.py"
      to: "src/vcompany/tmux/session.py"
      via: "Direct TmuxManager usage instead of AgentManager"
      pattern: "TmuxManager"
---

<objective>
Remove all v1 modules and update every import site across the codebase (MIGR-03).

Purpose: v1 MonitorLoop, CrashTracker, WorkflowOrchestrator, and AgentManager are fully replaced by v2 equivalents. Removing them eliminates dead code and prevents confusion about which system is active.
Output: 4 source files deleted, ~14 import sites updated, v1 test files removed or rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-companyroot-wiring-and-migration/08-RESEARCH.md
@.planning/phases/08-companyroot-wiring-and-migration/08-02-SUMMARY.md

@src/vcompany/cli/dispatch_cmd.py
@src/vcompany/cli/kill_cmd.py
@src/vcompany/cli/relaunch_cmd.py
@src/vcompany/cli/monitor_cmd.py
@src/vcompany/bot/cogs/alerts.py
@src/vcompany/bot/cogs/plan_review.py

<interfaces>
<!-- TmuxManager API that CLI commands can use directly -->

From src/vcompany/tmux/session.py:
```python
class TmuxManager:
    def create_session(self, name: str) -> bool: ...
    def create_pane(self, session: str, name: str) -> str | None: ...
    def send_keys(self, pane_id: str, keys: str) -> bool: ...
    def kill_pane(self, pane_id: str) -> bool: ...
    def kill_session(self, name: str) -> bool: ...
    def is_pane_alive(self, pane_id: str) -> bool: ...
    def get_pane_pid(self, pane_id: str) -> int | None: ...
    def send_command(self, pane_id: str, command: str, wait_for_ready: bool = False) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 source modules and their dedicated tests</name>
  <files>src/vcompany/monitor/loop.py, src/vcompany/orchestrator/crash_tracker.py, src/vcompany/orchestrator/workflow_orchestrator.py, src/vcompany/orchestrator/agent_manager.py, tests/test_monitor_loop.py, tests/test_crash_tracker.py, tests/test_workflow_orchestrator.py, tests/test_dispatch.py, tests/test_kill.py, tests/test_relaunch.py</files>
  <read_first>src/vcompany/orchestrator/__init__.py, src/vcompany/monitor/__init__.py, tests/test_conflict_resolver.py, tests/test_pm_integration.py, tests/test_integration_interlock.py</read_first>
  <action>
    **Phase 1: Delete v1 source files (4 files):**
    ```bash
    rm src/vcompany/monitor/loop.py
    rm src/vcompany/orchestrator/crash_tracker.py
    rm src/vcompany/orchestrator/workflow_orchestrator.py
    rm src/vcompany/orchestrator/agent_manager.py
    ```

    **Phase 2: Delete v1-only test files (6 files):**
    These test files exclusively test the deleted modules and cannot be meaningfully rewritten:
    ```bash
    rm tests/test_monitor_loop.py
    rm tests/test_crash_tracker.py
    rm tests/test_workflow_orchestrator.py
    rm tests/test_dispatch.py
    rm tests/test_kill.py
    rm tests/test_relaunch.py
    ```

    **Phase 3: Update __init__.py files:**
    - Check src/vcompany/orchestrator/__init__.py -- remove any imports of deleted modules
    - Check src/vcompany/monitor/__init__.py -- remove any imports of loop.py
    - If these __init__.py files become empty, leave them (package marker still needed for other modules in the package)

    **Phase 4: Update test files that partially reference v1 modules:**

    1. tests/test_conflict_resolver.py (imports AgentManager, DispatchResult):
       - Read the file to understand what it tests
       - If the test only uses AgentManager as a mock for dispatch, replace with a mock that provides the same interface
       - DispatchResult may need to be kept as a dataclass somewhere or mocked

    2. tests/test_pm_integration.py (imports MonitorLoop):
       - Read the file to understand the import context
       - If it only imports MonitorLoop for a mock, remove the import and mock differently

    3. tests/test_integration_interlock.py (imports MonitorLoop):
       - Same approach -- read, understand, remove v1 import, replace with mock or v2 equivalent

    **Phase 5: Verify no remaining imports of deleted modules anywhere:**
    ```bash
    grep -rn "from vcompany.monitor.loop\|from vcompany.orchestrator.agent_manager\|from vcompany.orchestrator.crash_tracker\|from vcompany.orchestrator.workflow_orchestrator" src/ tests/
    ```
    This MUST return zero results.

    IMPORTANT per Research Pitfall 3: Delete source AND test files in the same operation. pytest collects all test files before running -- if test files import deleted source modules, collection fails even if we never run those tests.
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run python -m pytest --co -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test ! -f src/vcompany/monitor/loop.py
    - test ! -f src/vcompany/orchestrator/crash_tracker.py
    - test ! -f src/vcompany/orchestrator/workflow_orchestrator.py
    - test ! -f src/vcompany/orchestrator/agent_manager.py
    - test ! -f tests/test_monitor_loop.py
    - test ! -f tests/test_crash_tracker.py
    - test ! -f tests/test_workflow_orchestrator.py
  </acceptance_criteria>
  <done>All 4 v1 source files deleted, 6 v1 test files deleted, remaining test files updated to remove v1 imports, pytest collection succeeds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI commands and remaining cogs</name>
  <files>src/vcompany/cli/dispatch_cmd.py, src/vcompany/cli/kill_cmd.py, src/vcompany/cli/relaunch_cmd.py, src/vcompany/cli/monitor_cmd.py, src/vcompany/bot/cogs/alerts.py, src/vcompany/bot/cogs/plan_review.py</files>
  <read_first>src/vcompany/cli/dispatch_cmd.py, src/vcompany/cli/kill_cmd.py, src/vcompany/cli/relaunch_cmd.py, src/vcompany/cli/monitor_cmd.py, src/vcompany/tmux/session.py, src/vcompany/bot/cogs/alerts.py, src/vcompany/bot/cogs/plan_review.py</read_first>
  <action>
    **CLI Commands -- update to use TmuxManager directly (per Research Pitfall 4):**

    The CLI commands are for manual operator use when the bot may not be running. They need to work independently of the supervision tree. Replace AgentManager with direct TmuxManager calls.

    1. src/vcompany/cli/dispatch_cmd.py:
       - Remove `from vcompany.orchestrator.agent_manager import AgentManager`
       - Import TmuxManager instead: `from vcompany.tmux.session import TmuxManager`
       - Replace `manager = AgentManager(project_dir, config)` with `tmux = TmuxManager()`
       - For dispatch: use `tmux.create_session(f"vco-{project_name}")`, `tmux.create_pane(session, agent_id)`, `tmux.send_keys(pane_id, claude_command)`
       - For dispatch_all: iterate agents, dispatch each
       - Keep the same CLI interface and output format
       - The core logic is: create tmux session -> create pane -> send claude command
       - Use the same session naming convention: `vco-{project_name}`

    2. src/vcompany/cli/kill_cmd.py:
       - Remove AgentManager import
       - Import TmuxManager
       - Use `tmux.kill_pane()` to kill agent panes, or `tmux.kill_session()` for the whole project

    3. src/vcompany/cli/relaunch_cmd.py:
       - Remove AgentManager import
       - Import TmuxManager
       - Kill pane then create new one (simple kill + dispatch)

    4. src/vcompany/cli/monitor_cmd.py:
       - Remove MonitorLoop import
       - This command is now deprecated -- the supervision tree handles monitoring via health tree
       - Replace implementation with a message: "The monitor loop has been replaced by the supervision tree health system. Use 'vco up' to start the bot with built-in monitoring, or use the /health slash command in Discord."
       - Keep the Click command so `vco monitor` doesn't error, just prints deprecation notice

    **Cog updates:**

    5. src/vcompany/bot/cogs/alerts.py:
       - Remove any references to MonitorLoop or CrashTracker in docstrings/comments
       - The AlertsCog still serves a purpose: buffering alerts during disconnects
       - Update `make_sync_callbacks()` if it returns callbacks specific to v1 types
       - If make_sync_callbacks references MonitorLoop callback signatures, update to work with supervision tree health notifications instead

    6. src/vcompany/bot/cogs/plan_review.py:
       - Check for any remaining references to v1 modules
       - The TYPE_CHECKING import of WorkflowOrchestratorCog is fine (that cog still exists, adapted in plan 02)
       - Update docstrings that mention MonitorLoop

    **Final verification:**
    Run `grep -rn "from vcompany.monitor.loop\|from vcompany.orchestrator.agent_manager\|from vcompany.orchestrator.crash_tracker\|from vcompany.orchestrator.workflow_orchestrator" src/ tests/` -- must return empty.
    Run full test suite: `uv run python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -qv "from vcompany.orchestrator.agent_manager" src/vcompany/cli/dispatch_cmd.py
    - grep -q "TmuxManager" src/vcompany/cli/dispatch_cmd.py
    - grep -qv "from vcompany.orchestrator.agent_manager" src/vcompany/cli/kill_cmd.py
    - grep -qv "from vcompany.monitor.loop" src/vcompany/cli/monitor_cmd.py
    - grep -q "deprecated\|replaced\|supervision" src/vcompany/cli/monitor_cmd.py
  </acceptance_criteria>
  <done>All CLI commands updated (no v1 imports), monitor command shows deprecation notice, AlertsCog and PlanReviewCog cleaned of v1 references, full test suite passes green</done>
</task>

</tasks>

<verification>
- `pytest --co -q` collects all tests with zero errors (no broken imports)
- `grep -rn "from vcompany.monitor.loop\|from vcompany.orchestrator.agent_manager\|from vcompany.orchestrator.crash_tracker\|from vcompany.orchestrator.workflow_orchestrator" src/ tests/` returns empty
- `uv run python -m pytest tests/ -q` full suite passes
- `vco monitor testproject` prints deprecation notice
</verification>

<success_criteria>
- 4 v1 source files deleted (loop.py, crash_tracker.py, workflow_orchestrator.py, agent_manager.py)
- 6 v1 test files deleted
- Zero import references to deleted modules anywhere in src/ or tests/
- CLI commands work with TmuxManager directly
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-companyroot-wiring-and-migration/08-03-SUMMARY.md`
</output>
