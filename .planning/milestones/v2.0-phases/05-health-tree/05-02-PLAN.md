---
phase: 05-health-tree
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/vcompany/bot/embeds.py
  - src/vcompany/bot/cogs/health.py
  - tests/test_health_cog.py
autonomous: true
requirements: [HLTH-03, HLTH-04]

must_haves:
  truths:
    - "Running /health in Discord renders the full supervision tree with color-coded state indicators"
    - "/health with project parameter filters to that project's subtree"
    - "/health with agent_id parameter shows a single agent's health"
    - "State transitions push notification messages to the alerts channel automatically"
  artifacts:
    - path: "src/vcompany/bot/embeds.py"
      provides: "build_health_tree_embed() function with state color mapping"
      contains: "def build_health_tree_embed"
    - path: "src/vcompany/bot/cogs/health.py"
      provides: "HealthCog with /health slash command and notification wiring"
      contains: "class HealthCog"
    - path: "tests/test_health_cog.py"
      provides: "Tests for embed building and cog behavior"
  key_links:
    - from: "src/vcompany/bot/cogs/health.py"
      to: "src/vcompany/supervisor/company_root.py"
      via: "getattr(self.bot, 'company_root', None) then .health_tree()"
      pattern: "company_root.*health_tree"
    - from: "src/vcompany/bot/cogs/health.py"
      to: "src/vcompany/bot/embeds.py"
      via: "build_health_tree_embed(tree)"
      pattern: "build_health_tree_embed"
    - from: "src/vcompany/bot/cogs/health.py"
      to: "Discord #alerts channel"
      via: "_notify_state_change sends to alerts channel on significant transitions"
      pattern: "_notify_state_change"
---

<objective>
Build the Discord /health slash command and state-change notification delivery.

Purpose: The operator must be able to run `/health` in Discord and see a color-coded tree of the full supervision hierarchy (HLTH-03). State transitions must push notifications to the alerts channel automatically (HLTH-04 -- notification delivery to Discord).

Output: HealthCog with /health command (optional project/agent_id filtering), build_health_tree_embed() in embeds.py, state-change notification method, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-health-tree/05-RESEARCH.md
@.planning/phases/05-health-tree/05-01-SUMMARY.md

@src/vcompany/bot/embeds.py
@src/vcompany/bot/cogs/commands.py
@src/vcompany/bot/permissions.py
@src/vcompany/container/health.py

<interfaces>
<!-- From Plan 01 outputs (health tree models) -->

From src/vcompany/container/health.py (after Plan 01):
```python
class HealthReport(BaseModel):
    agent_id: str
    state: str
    inner_state: str | None = None
    uptime: float
    last_heartbeat: datetime
    error_count: int = 0
    last_activity: datetime

class HealthNode(BaseModel):
    report: HealthReport

class HealthTree(BaseModel):
    supervisor_id: str
    state: str
    children: list[HealthNode] = []

class CompanyHealthTree(BaseModel):
    supervisor_id: str
    state: str
    projects: list[HealthTree] = []
```

From src/vcompany/supervisor/supervisor.py (after Plan 01):
```python
class Supervisor:
    _health_reports: dict[str, HealthReport]
    _on_health_change: Callable[[HealthReport], Awaitable[None]] | None
    def health_tree(self) -> HealthTree: ...
```

From src/vcompany/supervisor/company_root.py (after Plan 01):
```python
class CompanyRoot(Supervisor):
    def health_tree(self) -> CompanyHealthTree: ...
```

From src/vcompany/bot/cogs/commands.py (cog pattern):
```python
# Pattern: Cog class, TYPE_CHECKING import for VcoBot, @app_commands.command, @is_owner_app_check()
from vcompany.bot.permissions import is_owner_app_check
```

From src/vcompany/bot/embeds.py (embed builder pattern):
```python
# Pattern: build_*_embed functions, discord.Embed with title/color/timestamp, add_field, field limit guards
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_health_tree_embed function and HealthCog with /health command</name>
  <files>src/vcompany/bot/embeds.py, src/vcompany/bot/cogs/health.py, tests/test_health_cog.py</files>
  <read_first>
    - src/vcompany/bot/embeds.py
    - src/vcompany/bot/cogs/commands.py
    - src/vcompany/bot/permissions.py
    - src/vcompany/container/health.py
  </read_first>
  <behavior>
    - Test: build_health_tree_embed produces embed with "Health Tree" title
    - Test: embed has one field per project with project supervisor_id as field name
    - Test: each agent line has correct state emoji (green circle for running, red for errored, blue for sleeping, etc.)
    - Test: inner_state is displayed in parentheses when present
    - Test: embed color is green when all projects running, red when any errored
    - Test: embed respects 25-field limit (truncates with "... and N more" message)
    - Test: embed field values respect 1024-char limit
    - Test: build_health_tree_embed handles empty projects list gracefully
    - Test: build_health_tree_embed handles project with no children
    - Test: build_health_tree_embed with project filter returns only that project
    - Test: build_health_tree_embed with agent_id filter returns only that agent
  </behavior>
  <action>
    1. In `src/vcompany/bot/embeds.py`, add `build_health_tree_embed` function:
       - Define `STATE_INDICATORS` dict mapping state strings to Unicode emoji:
         - "running" -> green circle (U+1F7E2)
         - "sleeping" -> blue circle (U+1F535)
         - "errored" -> red circle (U+1F534)
         - "stopped" -> black circle (U+26AB)
         - "creating" -> yellow circle (U+1F7E1)
         - "destroyed" -> black square (U+2B1B)
       - Function signature: `def build_health_tree_embed(tree: CompanyHealthTree, *, project_filter: str | None = None, agent_filter: str | None = None) -> discord.Embed`
       - Embed title: "Health Tree"
       - Embed color: `discord.Color.green()` if all project states are "running", else `discord.Color.red()`
       - Timestamp: `datetime.now(timezone.utc)`
       - For each project in `tree.projects` (optionally filtered by project_filter matching supervisor_id):
         - If agent_filter is set, only include matching agent nodes
         - Build lines: `"{emoji} **{agent_id}**: {state}{inner}" ` where inner = ` ({inner_state})` if present
         - Add field: name=`"Project: {supervisor_id}"`, value=joined lines or "No agents", inline=False
       - Guard: if `len(embed.fields) >= 24`, add a final field "... and N more projects" and break
       - Guard: truncate each field value to 1024 chars
       - If no projects at all, set embed.description = "No projects active"
       - Import `CompanyHealthTree` from `vcompany.container.health` (use TYPE_CHECKING)

    2. Create `src/vcompany/bot/cogs/health.py`:
       - Follow the cog pattern from `commands.py`:
         - `from __future__ import annotations`
         - TYPE_CHECKING import for VcoBot
         - `class HealthCog(commands.Cog):`
         - `__init__(self, bot: VcoBot)` stores `self.bot = bot`
       - `/health` slash command:
         - `@app_commands.command(name="health", description="Show supervision tree health")`
         - `@is_owner_app_check()`
         - Parameters: `project: str | None = None`, `agent_id: str | None = None` (both optional app_commands.describe'd)
         - `await interaction.response.defer()`
         - Get CompanyRoot: `company_root = getattr(self.bot, 'company_root', None)`
         - If None: `await interaction.followup.send("No supervision tree active.", ephemeral=True); return`
         - Call `tree = company_root.health_tree()`
         - Call `embed = build_health_tree_embed(tree, project_filter=project, agent_filter=agent_id)`
         - `await interaction.followup.send(embed=embed)`
       - `_notify_state_change` async method for HLTH-04 notification delivery:
         - Signature: `async def _notify_state_change(self, report: HealthReport) -> None`
         - Only notify on significant transitions: `report.state in ("errored", "running", "stopped")`
         - Get the bot's guild and find "alerts" text channel
         - Send: `"{emoji} **{agent_id}** -> {state}"` with optional inner_state
         - Wrap in try/except to prevent notification failures from breaking the callback chain
       - `setup` async method for wiring notification callback to CompanyRoot:
         - Called after cog is added to bot
         - Checks for `company_root` on bot, if present, sets `company_root._on_health_change = self._notify_state_change`
         - This enables HLTH-04 push notifications

    3. Create `tests/test_health_cog.py`:
       - `TestHealthEmbed` class: test embed building with various tree shapes (empty, single project, multiple projects, with filters)
       - `TestEmbedLimits` class: test 25-field limit guard, 1024-char truncation
       - `TestHealthEmbedIndicators` class: test each state maps to correct emoji
       - Use direct function calls to `build_health_tree_embed` -- no Discord bot mocking needed for embed tests
       - For notification tests, create a mock HealthReport and verify `_notify_state_change` behavior
  </action>
  <verify>
    <automated>cd /home/developer/vcompany && uv run pytest tests/test_health_cog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def build_health_tree_embed" src/vcompany/bot/embeds.py
    - grep -q "STATE_INDICATORS" src/vcompany/bot/embeds.py
    - grep -q "class HealthCog" src/vcompany/bot/cogs/health.py
    - grep -q "name=\"health\"" src/vcompany/bot/cogs/health.py
    - grep -q "_notify_state_change" src/vcompany/bot/cogs/health.py
    - grep -q "is_owner_app_check" src/vcompany/bot/cogs/health.py
    - grep -q "TestHealthEmbed" tests/test_health_cog.py
    - grep -q "TestEmbedLimits" tests/test_health_cog.py
  </acceptance_criteria>
  <done>/health command renders CompanyHealthTree as a Discord embed with color-coded state indicators. Optional project/agent_id filtering works. Notification method sends state changes to #alerts. Embed respects Discord field/char limits. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_health_cog.py -v` -- all embed and cog tests pass
- `uv run pytest tests/test_health_tree.py -v` -- Plan 01 tests still pass
- `uv run pytest tests/ -x -q` -- full test suite green
</verification>

<success_criteria>
- build_health_tree_embed renders CompanyHealthTree with emoji indicators per state
- /health slash command calls CompanyRoot.health_tree() and responds with embed
- /health supports optional project and agent_id filtering (HLTH-02 queryability)
- _notify_state_change pushes significant transitions to #alerts channel
- Embed guards prevent exceeding 25 fields or 1024 chars per field
- All new tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-health-tree/05-02-SUMMARY.md`
</output>
